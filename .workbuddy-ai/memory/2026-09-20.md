# 2026-09-20 — P8c `whatsapp_inbound_ingest` + adversarial audit (P8/P8b/P8c)

## Nima qilindi

### 1. P8c blok: `platform_runtime/whatsapp_inbound.py` (yangi, ~470 qator)
Kiruvchi WhatsApp webhook oqimi. Ikkita **read** tool: `whatsapp.webhook`
(`webhook_status`), `whatsapp.verify` (`verify_webhook`). `INGEST_TOOLS`
tuple. Registry: `build_registry()` 67 → **69**, `known_tool_names()` 70.

Asosiy qarorlar:
- **Transport emas** — baytlar + sarlavhalar oladi, qaror qaytaradi. Route 3 qator
  ish qiladi; funksiya socket'siz testlanadi.
- **Imzo RAW baytlar ustida, parse qilishdan OLDIN**, `hmac.compare_digest`
  (constant-time). `verify_signature(body, signature, app_secret)` `bytes` oladi.
- **Kiruvchi qabul TOOL EMAS** — model webhook qabul qila olsa, yozmagan mijoz
  uchun 24 soatlik oyna ochardi. Bu blokning eng muhim chegarsi.
- Status callback va notanish raqam **sababi bilan TASHLANADI**, raise qilinmaydi
  (Meta non-2xx ni qayta yuboradi → cheksiz retry bo'roni).
- Dedup — engineniki (`accept_event`, `UNIQUE(tenant,channel,event_key)`), modul
  faqat kalitni (WhatsApp message id) beradi.
- `window_until` **faqat Meta timestampidan** hisoblanadi, host soatidan EMAS.
- `sender`/`conversation_id` = operatorning **contact id** (`ali`), telefon emas —
  chunki `whatsapp.send` ning manzil maydoni `contact` va `allowed_recipients`
  `ali` saqlaydi; `runtime_authority` `actor` ni faqat
  `{web,cron,approval,agent}` uchun user id deb biladi (whatsapp yo'q).

### 2. Adversarial audit — foydalanuvchi talab qilgan qism
`docs/development/V05J-IMPLEMENTATION-UZ.md` §2/§3. **11 nuqson**, uchtasi jiddiy,
**hammasi o'zim kiritgan**, hammasi **jimgina**:

| # | Nuqson | Jiddiylik | Holat |
|---|---|---|---|
| 1 | `window_until` yoziladi, **o'qilmaydi** (OUTBOUND_CHANNELS da whatsapp yo'q) | Yuqori | qayd, **ochiq** |
| 2 | `_parse_timestamp` **kasr soniyali ISO ni rad etardi** | Yuqori | tuzatildi +2 test |
| 3 | test fixture'lari **durotka** (to'plam chirigan) | Yuqori | tuzatildi |
| 4 | `MAX_BODY_BYTES` qo'llanmagan | O'rta | tuzatildi |
| 5 | `secret()` o'lik shoxobcha | O'rta | tuzatildi |
| 6 | ixtiyoriy arg majburiy (**P8b xatosining takrori**) | O'rta | tuzatildi |
| 7 | mavjud bo'lmagan `created` ustuni | O'rta | tuzatildi |
| 8 | prefiks registrga sezgir | Kichik | tuzatildi |
| 9 | noaniq raqam jimgina hal qilinardi | Kichik | tuzatildi |
| 10 | o'lik konstantalar | Kichik | tuzatildi |
| 11 | probe **vakuum** edi | Kichik | tuzatildi |

**Eng muhim saboq:** bir blokda o'rganilgan saboq keyingisiga avtomatik
o'tmaydi — P8b da topilgan `obj()` sxema xatosi P8c da **takrorlandi**.

**Ikkinchi saboq:** `failures=2, errors=150` ni ko'rganda avval **o'z
o'zgarishlarimni** gumondor qildim, lekin tekshiruv uni **vaqt bombasi** ekanini
ko'rsatdi: `ISO_OFFSET=2026-09-19T10:00+05:00` + 24h = `2026-09-20T10:00`, to'plam
esa aynan **o'sha kuni 10:24 da** yurgizildi. Yiqilish mening o'zgarishlarimdan
**qat'i nazar** bo'lardi. Ya'ni "mening aybimmi?" degan savolni **o'lchash**
kerak, taxmin qilish emas.

## Yangi/o'zgargan fayllar
- **yangi** `api-python/platform_runtime/whatsapp_inbound.py`
- **yangi** `api-python/runtime_tests/test_whatsapp_inbound.py` (70 test, 1 skip)
- **yangi** `scripts/probe_whatsapp_signature.py` (5 bo'lim, `PROVEN`)
- **yangi** `scripts/check_whatsapp_webhook_example.py` (`OK`)
- **yangi** `config/whatsapp_webhook.example.json` (8 `_note_*`)
- **yangi** `docs/development/V05J-IMPLEMENTATION-UZ.md`
- `platform_runtime/whatsapp.py` — `_parse_timestamp` regexi `(\.\d+)?` oldi
- `platform_runtime/engine.py` — `OUTBOUND_CHANNELS` izohi tuzatildi (eskirgan
  "WhatsApp has no inbound ingest yet" **yolg'oni** olib tashlandi, ochiq gap
  qayd etildi)
- `platform_runtime/tools.py` — `register_whatsapp_inbound_tools(r)`
- `runtime_tests/test_whatsapp.py` — 54 test; parsing/oyna fixture'lari ajratildi,
  `RECENT`/`STALE`/`_iso_offset`, `import datetime`/`import time`
- `docs/development/AUDIT-CORRECTIONS-UZ.md`, `PROGRESS-UZ.md`, `BACKLOG.json`,
  `README.md`

## Holat
- To'liq to'plam: `Ran 1571 tests — failures=1, errors=149` — baseline **uchinchi
  marta aynan o'zgarmadi** (1499 → 1571; +72 yangi test error soniga qo'shilmadi).
- `test_whatsapp_inbound.py` → `Ran 70 tests — OK (skipped=1)`.
- `test_whatsapp.py` → `Ran 54 tests — OK`.
- `probe_whatsapp_signature.py` → `PROVEN` (3 marta bir xil natija, deterministik).
- `check_whatsapp_webhook_example.py` → `OK`; `check_capabilities_example.py` — 69
  tool / 15 agent / 37 havola / 0 noma'lum.

## Keyingi qadam
`next_source_block` = **`whatsapp_window_from_inbound_events`** — auditning 1-topilmasini
yopish: `window_until` ni `whatsapp.send` darvozasiga ulash, **keyin** `whatsapp` ni
`OUTBOUND_CHANNELS` ga qo'shish. **Pretsedent qarori kerak** (kiruvchi hodisa ustunmi
yoki operator jadvali?) — yarim bajarilgan birlashtirish ikki xil zid oynani majburlardi.

Keyin: `wa_window_until` atributi `customer` entity'siga (PRD §2.8), ERP sinxronizatsiyasi,
`task_oversight_escalation` (P6), `manufacturing_bom` (P12), `oee_and_andon` (P13),
`telephony_outbound` (P14). Ochiq: `sheets.py` `allowed_connections` (V05G §4.2).

---

# P8d — `whatsapp_window_from_inbound_events` (auditning 1-topilmasini yopish)

## Nima qilindi
`platform_runtime/whatsapp.py` ga ikkita yangi funksiya:
- `_window_from_events(engine, tenant, contacts)` — `p_events` dan **oxirgi kiruvchi
  vaqt**ni o'qiydi (engineniki read path, yangi store yo'q).
- `window_sources(engine, tenant, agent, entry, step, *, contacts)` →
  `(resolved, sources, status)`; `resolved[c] = (kind, epoch)`, `kind ∈
  {absent, unparsable, at}`, `sources[c] ∈ {event, register}`.

`window()` va `send()` ikkalasi ham endi `window_sources` ni chaqiradi. `window()`
qatorlariga `'source'` maydoni qo'shildi.

**Qaror:** tasdiqlangan kiruvchi hodisa operator jadvalidan **ustun** (yangi emasi
emas — **hodisa**). Qo'lda tahrirlangan jadval Meta o'zi bergan timestamp yopgan
oynani qayta ochmasligi kerak.

## Eng muhim topilma — probe tutdi, testlar tutmadi
`whatsapp_inbound` `window_until` ni **tugash vaqti** deb yozadi (Meta ts + 24h).
`window_state()` esa **oxirgi kiruvchi vaqt** ni olib, 24h ni **o'zi** qo'shadi.
To'g'ridan-to'g'ri ulash 24h ni **ikki marta** qo'shdi: **25 soat oldin** yozgan
mijoz oyna **ochiq** deb o'qilardi — aynan **131047** holati.

- 119 + 70 test yashil edi, chunki har biri **o'z** tasviriga nisbatan izchil edi;
  ularni bog'laydigan **konversiya** hech qayerda tekshirilmagan edi.
- Tuzatish: `latest[contact] = float(stamp) - WINDOW_SECONDS` +
  `test_the_event_window_is_not_double_counted`.

**Naqsh (uchinchi marta):** bir modul ichida izchil bo'lgan ikki tasvir orasidagi
konversiyani **test emas, probe** ushlaydi.

## Ataylab qilinmadi
`whatsapp` `OUTBOUND_CHANNELS` ga **qo'shilmadi**: (a) oyna va manzil ikki xil savol;
(b) qo'shish `whatsapp.send` ni allowlist shoxobchasidan hodisa-bog'lash shoxobchasiga
**ko'chiradi** — qoida qo'shish emas, **almashtirish**; (c) oynadan tashqaridagi
qonuniy shablon yuborishlarida bog'lanadigan hodisa yo'q.

## Holat
- To'liq to'plam: `Ran 1636 tests — failures=1, errors=149, skipped=1` — baseline
  **to'rtinchi marta o'zgarmadi** (1571 → 1636, +65). Yagona `failure` — Windows-only
  `test_all_files_private`.
- `test_whatsapp.py` → **119 OK**; `test_whatsapp_inbound.py` → **70 OK (1 skip)**.
- `probe_whatsapp_window_sources.py` → **PROVEN**.
- Yangi tool **yo'q** (ataylab): registry `build_registry()=69`, `known_tool_names()=70`.
- Hisobot: `docs/development/V05K-IMPLEMENTATION-UZ.md`.

## Keyingi qadam
`next_source_block` = **`wa_window_until_graph_attribute`** — PRD §2.8 talab qilgan
`wa_window_until` atributi Business Graph'ning `customer` entity'sida. Keyin ERP
sinxronizatsiyasi (P8b davomi), `task_oversight_escalation` (P6), `manufacturing_bom`
(P12), `oee_and_andon` (P13), `telephony_outbound` (P14). Ochiq: `sheets.py`
`allowed_connections` (V05G §4.2).

---

# P8L — `wa_window_until` grafiya atributi: **tekshirildi va RAD ETILDI**

PRD v0.5 §2.8 oyna holatini Business Graph'ning `customer` entity'siga
`wa_window_until` atributi sifatida qo'shishni taklif qiladi. Blok **qurilmadi**;
**rad etildi** — lekin "qilmadik" va "qila olmadik" bir xil ko'rinadi, shuning
uchun sabab **o'lchandi** (`probe_wa_window_graph_attribute.py` → `PROVEN`).

## Mexanik sabab (did emas)
Grafda **atribut — bu `row.get(field)`**, ya'ni **ko'chirilgan katak**,
hisoblangan qiymat emas. Shuning uchun `wa_window_until` deklaratsiyasi oynani
**inson qo'lda yozadigan** ustunga bog'lardi — va u mijoz yozganda **o'zi
yangilanmaydi**. Probe ko'rsatdi: **2020-yilda tugagan** oyna `complete=true`,
`source_errors=[]` bilan qaytadi. Graf eski katakni joriydan **ajrata olmaydi**.

## Uch qismli rad etish
1. **Atribut** rad etildi — yuqoridagi sabab.
2. **`observed` dan hisoblash** rad etildi — `observed` **bizning o'qish vaqtimiz**,
   provider'ning yangilanish vaqti emas; qoralama va yuborish **ikki xil surat**
   o'qib, bitta mijoz haqida **kelishib qolmasdi**.
3. **`whatsapp.window` ni graf manbasi qilish** rad etildi — `SAFE_SOURCE_TOOLS`
   to'rtta **qator qaytaruvchi** tool bilan cheklangan; `whatsapp.window`
   `entity_id` dan emas, tenant konfiguratsiyasidan javob beradi (id ni kontaktga
   bog'lash **ikkinchi deklaratsiya** talab qilardi); chiqishi qator ro'yxati emas;
   va bitta savol uchun **uchta ruxsat** kerak bo'lardi.

## Talab bajarilgan, usul rad etilgan
`whatsapp.window` `open` + `closes_at` + **`source`** (`event`/`register`)
qaytaradi — va bu **o'sha** resolver, `send` darvozasi provayder I/O sidan
**oldin** chaqiradigani. Ya'ni §2.8 ning **maqsadi** P8/P8d da bajarilgan.

## Naqsh — uchinchi blok, bir xil sinf
Ikki modul bir tushunchani **ikki xil** ifodalaydi, konversiya esa tekshirilmaydi:
1. P8c: `window_until` yoziladi, o'qilmaydi;
2. P8d: `window_until` (tugash) ↔ `window_state` (oxirgi kiruvchi) aralashadi — 24h **ikki marta**;
3. P8L: oyna **qo'lda yuritiladigan** manbaga ko'chirilardi.

## O'rniga nima qilindi
- `runtime_tests/test_business_graph.py` → `WaLifecycleGuardTests` (**6 metod**;
  kim faqat PRD ni o'qib atributni qo'shsa, **yiqiladi**. GraphTests dan meros → to‘plamda **52** test. Fayl: 46 metod / **98 test OK**.
- `scripts/probe_wa_window_graph_attribute.py` (yangi, 4 bo'lim) → `PROVEN`.
- `docs/development/V05L-IMPLEMENTATION-UZ.md` (yangi hisobot).
- **Yangi tool yo'q** (ataylab): `build_registry()=69`, `known_tool_names()=70`.

## Holat
- To'liq to'plam: `Ran 1688 tests — failures=1, errors=149, skipped=1` — baseline
  **beshinchi marta o'zgarmadi** (1636 → 1688, +52).
- `next_source_block` = **`erp_sync_beyond_mock_post`** (P8b davomi).
- **Saboq:** PRD **talabni** to'g'ri qo'ygan, **usulni** noto'g'ri taklif qilgan.
  Taklif qilingan usulni ko'r-ko'rona bajarmaslik kerak — lekin rad etish ham
  **dalil** talab qiladi, aks holda u e'tiborsizlikka o'xshaydi.

## P8e — ERP posting (`erp_sync_beyond_mock_post`) qurildi va o'lchandi

Foydalanuvchi 4 savolga javob berdi: **"Ikkalasi ham"** (ikkala adapter),
**"faqat aniq buyruqlar bo'lganda, gallyutsinatsiya bilan emas"**,
**"Hujjat to'liq bo'lmasa — rad etish"**, **"1 va 2 ham"** (ikki dedup qatlami).

### Nima qurildi
- `api-python/platform_runtime/erp.py` (~840 qator) — repo ichidagi **eng xavfli
  kod**, chunki posting — boshqa tizim harakat qiladigan moliyaviy yozuv.
- 3 tool: `erp.posting_prepare` (read), `erp.posting_submit` (write),
  `erp.posting_status` (read). Registr **69 → 72**; `known_tool_names()=73`.
- `p_erp_postings` + `p_erp_postings_claim` (`UNIQUE(tenant,claim_key)`).
- `test_erp.py` — **54 test OK**. `probe_erp_posting_boundary.py` — **PROVEN** (5 bo'lim).
- `config/erp_posting.example.json` + `scripts/check_erp_example.py` → `OK`.
- `docs/development/V05M-IMPLEMENTATION-UZ.md` (yangi hisobot).

### Uch qoida, har biri majburlangan
1. **Pul harakatlanmaydi** — posting **qarz** yozadi, to'lash ERP ichida inson
   ishi. `documents` bloki da'vosidan **torroq**, shuning uchun alohida o'lchanadi.
2. **To'liq bo'lmagan hujjat rad etiladi** (nomi bilan). Yetti maydon; `account`
   va `counterparty` operator registridan **alias** sifatida, hujjatdan **hech
   qachon** o'qilmaydi. `missing` va `malformed` **ajratilgan**.
3. **Ko'pi bilan bir marta** — **to'rt qavat**: ERP qidiruvi (fail-closed), I/O dan
   oldingi lokal ledger, `UNIQUE` `claim_key`, va qaytariladigan muvaffaqiyatsizlik.

### Audit tutgan 6 haqiqiy nuqson
1. `_date_text('2026-13-01')` qabul qilinardi → `_real_date()` + `monthrange`.
2. `failed` qator o'z retry'sini to'sardi (abadiy yozilmas!) → `claim_key` faqat
   claiming statuslarda.
3. `posted()` `failed` qatorni qaytarardi → `AND status IN (CLAIMING_STATUSES)`.
4. O'z `PATH_RE` im `/a/../b` va `//evil.example` ni qabul qilardi → o'chirildi,
   `safe_relative_path` ga topshirildi.
5. Yetishmayotgan credential yutilardi → `RuntimeError`/`ValueError` aynan qayta
   ko'tariladi (xabar o'zgaruvchi nomini saqlashi kerak).
6. Buzuq maydon "yetishmayotgan" deb xabar berilardi → absent ≠ malformed.

**Eng muhim saboq:** 4-nuqson — men **o'zim yo'l-gate'ini qaytadan yozdim**,
allaqachon mavjud va testdan o'tgan funksiya o'rniga, va takror **boshqacha**
qaror qabul qildi. Ishlaydigan kodni qayta yozish — uni xato qilish uchun yangi
imkoniyat.

### Qamrov bo'shlig'i yopildi
Foydalanuvchi "ikkalasi ham" degan edi, birinchi versiyada faqat `onec_http`
mashq qilingan edi. `custom_http` qo'shildi — va uning **yagona** xulq-atvor farqi
o'lchandi: flat `id` vs `result.Ref_Key`. Driver hech narsani o'zgartirmasa,
custom ERP **o'zida bor** hujjatni ko'rmay qolardi → ikki marta yozish sharti.
`basic` vs `bearer` auth ham qamrab olindi. +7 test.

### Ataylab qilinmagan ish
`erp.posting_submit` **`engine.OUTBOUND_TOOLS` ga qo'shilmadi**: u gate "agent bu
qabul qiluvchiga murojaat qila oladimi" deb so'raydi, postingning esa **qabul
qiluvchisi yo'q** — qo'riqchisi hujjat identifikatsiyasi. Sabab `engine.py` da
izoh sifatida yozilgan.

## Holat
- To'liq to'plam: `Ran 1742 tests — failures=1, errors=149, skipped=1` — baseline
  **oltinchi marta o'zgarmadi** (1688 → 1742, +54). Yagona `failures` — Windows-only
  `test_all_files_private`.
- `next_source_block` = **`crm_adapters_live_staging_acceptance`**.
- `production_release` = **NO_GO** (live ERP acceptance yo'q).

## Chuqur review sessiyasi (11:43–11:55) — read-only, fayl yozilmadi

Foydalanuvchi so'rovi: loyihani juda chuqur tahlil qilib, hech qanday md
yozmasdan xotirada saqlash. Keyin topshiriq beriladi.

### Tasdiqlangan baseline (haqiqiy o'lchov)
`python -m unittest discover -s runtime_tests -t runtime_tests` →
**Ran 1746 tests, failures=1, errors=149, skipped=1** (314s). Bu hujjatdagi
`1742` dan farq qiladi — 4 test qo'shilgan (bugungi ish). `failures=1` —
Windows-only `test_all_files_private` (POSIX mode 0o077). Error'lar asosan
`cryptography` bog'liqligi yo'qligi (`secret_vault.VaultError`) + Windows host
cheklovlari. Ya'ni doc'dagi da'vo to'g'ri, raqam esa +4.

### Arxitektura xaritasi (xotirada)
- **Control plane**: `app/main.py` → `app/platform_api.py` (`/platform/*`).
  Legacy `/runner/*` middleware orqali **410** bilan o'chirilgan. Yangi runner
  `POST/WS /platform/runner/ws` — **device JWT**, har xabarda generation tekshiriladi.
- **Engine** (`platform_runtime/engine.py`, 42KB): SQLite/WAL, `tx()` = BEGIN
  IMMEDIATE. `p_tasks/p_steps/p_approvals/p_devices/p_events/p_audit/p_quota/
  p_schedules/p_agent_runs`. Lease → `uncertain` (exactly-once yo'q, halol).
  Approval fingerprint'ga bog'langan; `independent_approval` o'z-o'zini tasdiqlamaydi.
- **Tool registry** (`tools.py`): 4 risk klassi (read/write/destructive/physical),
  `validate_schema` unknown keyni rad etadi. **Hech bir tool caller URL/secret
  qabul qilmaydi** — manzillar operator config. `post_json` HTTPS-only, redirect rad.
- **Agent loop** (`agent_loop.py`): `p_agent_runs` + `p_agent_turns`, budget
  (max_steps ≤12, max_seconds ≤86400, max_calls), planner reservation provider'dan
  oldin, deadline → `escalated`. `semantic_fact_check = 'not_performed'` (halol).
- **Pack** (`app/packs.py`): pydantic `extra=forbid`, persona path confinement,
  `unknown_tools()` fail-loud, ladder 3 xil. Core pack mazmuniga bog'lanmaydi.
- **Runner** (`apps/runner/runner.js`): Node ≥22.4, npm dep yo'q. Linux FD
  verification (`/proc/self/fd`, O_NOFOLLOW), journal atomik rename, symlink rad.
  Darwin'da Python helper. **Windows hozircha `fs.*` uchun ishlamaydi** — bu
  hujjatlashtirilgan cheklov.
- **WhatsApp** (`whatsapp.py` + `whatsapp_inbound.py`): 24h oyna **I/O dan oldin**
  rad etiladi (131047 "impossible by design"), template operator config, bitta
  raqam = bitta manzil. `OUTBOUND_CHANNELS` da whatsapp **yo'q** — ataylab.
- **ERP** (`erp.py`): eng xavfli blok. Pul ko'chirmaydi. To'liq bo'lmagan hujjat
  rad etiladi. 4 qavatli dup-prevention. Fail-closed.
- **Auth**: HS256 JWT, `directory_enabled()` production'da opt-out qila olmaydi;
  `runtime_authority` har engine tranzaksiyasi ichida membershipni qayta tekshiradi.
- **Worker** (`app/worker.py`): har 1s paketlar bo'ylab aylanadi —
  run_schedules → reengagement.tick → briefing.tick → process_event → tick → agent_loop.tick.

### Joriy holat
Source **v0.5**, IN_PROGRESS, production **NO_GO**. Business Graph P1 qo'shildi
(6 read-only tool, o'z vakolatini bermaydi). `next_source_block` =
`crm_adapters_live_staging_acceptance`.

**Session yakuni:** hech qanday fayl o'zgartirilmadi (faqat shu memory yozuvi).
Foydalanuvchi keyingi topshiriqni beradi.

## P6 `task_oversight_escalation` — kechikkan ish eskalatsiyasi (yakunlandi)

**Nima qurildi:** `api-python/platform_runtime/escalation.py` (~600 satr) —
P9b (`workforce.workload`, kechikkan ishni ANIQLAYDI) ning **yetkazib berish**
yarmi. `reengagement`/`briefing` koordinator naqshini takrorlaydi:
operator sozlaydi → `tick` chegaralangan manbani o'qiydi → har dedup kalitiga
bitta yetkazish.

**Ulanish:** `engine.py` `Engine.__init__` da `ESCALATION_SCHEMA` executescript;
`tools.py` `build_registry()` da `register_escalation_tools(r)`;
`app/worker.py` da `EscalationLoop(e)` + `escalation.tick(tenant)`;
`app/platform_api.py` da 4 marshrut (GET `/escalation`, PUT `/escalation/{s}`,
GET `/escalation/{s}/ledger`, POST `/escalation/{s}/disable`).

**Registr:** `build_registry()` 72 → **74** (2 read tool: `escalation.preview`,
`escalation.schedules`). Yetkazish **tool emas** — `EscalationLoop.tick` ishi;
aks holda dedup/cooldown faqat promptga tayanardi.

**Ikki chegara (kodda majburlangan):** (1) xodimni **baholamaydi** — `dir(module)`
da score/rank/rate yo'q, yuborilgan matnda ham yo'q; (2) faqat **o'qish + xabar**
— `DELIVERY_TOOLS = {'telegram.send'}`, ishga tegmaydi, approval'siz (menejerga
aytish kanali).

**Audit/probe tutgan 2 haqiqiy nuqson:**
1. **`sent` qator cooldown o'tgach qayta yuborilardi** — modulning o'z
   docstring'iga zid. Menejer o'sha kechikkan vazifani HAR KUNI olardi. Probe
   tutdi, testlar tutmadi. Tuzatish: `sent` — bu kalit uchun **umrbod terminal**;
   faqat `queued` (crash) va `failed` (cooldown o'tgach) qayta uriniladi.
2. **Provider uzilishi tutilmagan istisno edi** — `_read_overdue`
   `except (Conflict, ValueError, LookupError)` bilan o'ralgan edi, lekin Sheets
   `SheetsError` (RuntimeError) ko'taradi. Endi `except Exception`, faqat klass
   nomi yoziladi.

**Test/probe:** `runtime_tests/test_escalation.py` — **45 test OK**;
`scripts/probe_escalation_boundary.py` — 6 bo'lim **PROVEN**;
`config/escalation.example.yaml` + `scripts/check_escalation_example.py`;
`config/agent-capabilities.example.yaml` ga `mgmt.escalator` qo'shildi.

**Baseline (yettinchi marta aynan):** 1742 → **1791 test**,
`failures=1, errors=149, skipped=1` (o'zgarishsiz; yagona failure — Windows-only
`test_all_files_private`).

**Hujjatlar:** `docs/development/V05N-IMPLEMENTATION-UZ.md` (yangi),
`BACKLOG.json` (P6 → LOCAL_CONTRACT_TESTED, offline_tests 1791),
`PROGRESS-UZ.md`, `README.md`, `AUDIT-CORRECTIONS-UZ.md` yangilandi.

**Ochiq:** live Telegram acceptance yo'q → `production_release` **NO_GO**; UI va
per-item acknowledgement yo'q.

---

## P12 — `manufacturing_bom` (V05O) — QURILDI VA O'LCHANDI

**Nima qurildi:** `api-python/platform_runtime/manufacturing.py` — 3 ta read tool
(`manufacturing.bom`, `manufacturing.cycle`, `manufacturing.yield`), yozuv yo'li
**yo'q**. `build_registry()` **74 → 77**. PRD §8 ning to'rt elementidan **uchtasi**
sondan yasaladi, **to'rtinchisi (OEE) ataylab rad etiladi** — P13 egasi.

**Uchta rad etish, kodda:** (1) OEE hisoblanmaydi (rejalashtirilgan ish vaqti va
ideal sikl vaqti registrda yo'q); (2) chiqim bitta sondan hisoblanmaydi — nuqson
o'qilmasa `yield: null, yield_computable: false`, 100% **emas**; (3) xodim
bo'yicha ko'rsatkich yo'q — `operator_column` degan kalit **mavjud emas**.

**Probe tutgan 2 haqiqiy nuqson** (ikkalasi ham modulning **o'z docstring'iga
zid** — P8d/P6 bilan bir sinf):
1. `yield_report` da `setdefault` **o'qiladiganlik tekshiruvidan oldin** ishlardi
   → bo'sh chiqim katagi (`''`) **uydirma `output: 0.0` stansiya** yaratardi.
   Tuzatish: `output is None` bo'lsa yozuv umuman yaratilmaydi.
2. Nuqson tarmog'ida `continue` **yozuv yaratilgandan keyin** turardi → bitta
   matnli katak butun qatorning **o'qilgan chiqimini** summasidan yo'qotardi.
   Tuzatish: `defect_unreadable` belgisi butun stansiyani hisoblanmaydigan qiladi.

**Qo'shimcha tuzatishlar:** `path` faqat haqiqatan bog'langanda qo'shiladi (bo'sh
satr "joyi yo'q" deb o'qilmasligi uchun) + `unbound` soni; `authority` bloki
qo'shildi (oila qoidasi); `_yield_tool` indentation.

**Test/probe:** `runtime_tests/test_manufacturing.py` — **64 test OK**;
`scripts/probe_manufacturing_boundary.py` — 6 bo'lim, **22 o'lchangan xossa,
hammasi PASS**; `config/manufacturing.example.json` +
`scripts/check_manufacturing_example.py` (modulning **o'z** validator'i ishlatiladi);
`config/agent-capabilities.example.yaml` ga `ops.production` qo'shildi.

**Baseline (sakkizinchi marta aynan):** 1791 → **1855 test**,
`failures=1, errors=149, skipped=1` (o'zgarishsiz; yagona failure — Windows-only
`test_all_files_private`).

**Hujjatlar:** `docs/development/V05O-IMPLEMENTATION-UZ.md` (yangi),
`BACKLOG.json` (P12 → LOCAL_CONTRACT_TESTED, offline_tests 1855),
`PROGRESS-UZ.md`, `README.md`, `AUDIT-CORRECTIONS-UZ.md` yangilandi.

**Ochiq:** OEE (P13), multi-level BOM portlashi yo'q, tannarx yo'q, UI yo'q,
live ERP acceptance yo'q → `production_release` **NO_GO**.

**Keyingi blok:** P13 `oee_and_andon`, keyin P14 `telephony_outbound`.

---

## P13 `oee_and_andon` — QURILDI VA O'LCHANDI (T4 ning oxirgi bo'lagi)

**Yangi modul:** `api-python/platform_runtime/oee.py` (~700 satr). Ikki **read**
tool: `oee.report`, `oee.andon`. `build_registry()` **77 → 79**;
`tools.py` da `register_oee_tools(r)` qo'shildi. P12 ning OEE topshirig'i bajarildi
— lekin faqat mijoz **deklaratsiya qilgan me'yorlar** bo'yicha.

**Uch faktor, har biri faqat deklaratsiya qilingan kirishdan:**
`availability = run/planned`, `performance = (ideal*produced)/run`,
`quality = good/produced`. Qisman OEE **raqam sifatida ko'rsatilmaydi** —
`oee: null` + `not_computable: {absent: [...], unusable: [...]}`.
`performance` rejani **talab qilmaydi** (uchta o'lchangan qiymat) — modul
sarlavhasi va test dastlab buni xato yozgan edi, kod to'g'ri edi, **hujjat** tuzatildi.

**Tutgan nuqsonlar (3 ta), va muhimi — uchtadan biri KODDA EMAS, HUJJATDA edi:**
1. `_REGISTER_KEYS` da `planned_run_seconds` yo'q edi → konstanta funksiyasi
   **umuman yetib bo'lmas** edi. Qo'shildi.
2. `good > produced` → OEE **3.4719 (347%)**. `_factor_quality` endi rad etadi +
   `good_exceeds_produced` hisoblagichi.
3. **HUJJAT KODGA ZID EDI:** sarlavha "ikkala reja manbasi rad etiladi" derdi;
   o'lchov aksini ko'rsatdi — **ustun ustunlik qiladi, konstanta bo'sh katak uchun
   zaxira, matnli katak hech qaysisi**. Kod to'g'ri edi; men **o'zim yozgan**
   config misoli + checker ham o'sha noto'g'ri da'voni kodlagan edi. Hujjat
   tuzatildi, checker endi **qabul qilinishini** o'lchaydi, 3 test qo'shildi.
   Rad etish chegarasi **aniq:** ikkita **registr** bir o'qish uchun.

**Andon — faqat ANIQLASH yarmi.** Yetkazish **P6 eskalatsiya koordinatoridan**
qayta ishlatiladi. Chegara deklaratsiya qilinmasa — nomlab rad etadi. OEE'si
hisoblanmaydigan stansiya → `not_evaluated`, **signal emas**.
`run > plan` → availability 100% ga **qisqartirilmaydi**;
performance 1.0 ga **ko'tarilmaydi** (u nisbat emas, tezlik).

**Test/probe:** `runtime_tests/test_oee.py` — **63 test OK**;
`scripts/probe_oee_boundary.py` — 6 bo'lim, **63 o'lchangan xossa, hammasi PASS**
(probe'ning o'z ikki xatosi ham tuzatildi: performance chegarasi + buzilish soni);
`config/oee.example.json` + `scripts/check_oee_example.py`;
`config/agent-capabilities.example.yaml` ga `ops.oee` agenti qo'shildi
(`registry tools: 79`, `unknown: []`).

**Baseline (to'qqizinchi marta aynan):** 1855 → **1918 test**,
`failures=1, errors=149, skipped=1` (yagona failure — Windows-only
`test_all_files_private`).

**Hujjatlar:** `docs/development/V05P-IMPLEMENTATION-UZ.md` (yangi, 12 bo'lim),
`BACKLOG.json` (P13 → LOCAL_CONTRACT_TESTED, offline_tests **1918**,
claim "P1-P13"), `PROGRESS-UZ.md`, `README.md`, `AUDIT-CORRECTIONS-UZ.md`.

**Diqqat (BACKLOG.json tahriri):** bir necha marta ketma-ket skript bilan
tahrirlaganda **eski nusxa xotirda qolib** yangi yozuvni bosib ketdi — har
tahrirdan keyin **faylni qayta o'qib tasdiqlash** kerak. Bir marta
`oee_and_andon` itemining `status`/`remaining` maydonlari shu sabab yo'qolgan edi.

**T4 TUGADI.** Ochiq: live ERP acceptance yo'q, smena kalendari tanlanmaydi,
idle klassifikatsiya qilinmaydi, UI yo'q → `production_release` **NO_GO**.
**Keyingi blok: P14 `telephony_outbound`** (T4 dan keyingi yagona qolgan PRD
yuzasi; og'ir, huquqiy jihatdan ochiq va sifatga sezgir).

## Ultra-audit T4 (P12+P13 + qo'shni modullar) — 5 nuqson, 17 test, 22 xossa

Topshiriq: *"avval qilgan ishlarni ultra-audit + adversary tekshiruv, avvaldan
bo'lgan qismlar bilan kesishmada yangi bug chiqmaganini tekshir, mayda chekka
buglar va algoritm logikasi har xil holatda to'g'rimi, hammasini fix qil."*

**Topilgan va tuzatilgan 5 haqiqiy nuqson (hammasi jimgina):**

1. **`inf`/`nan` raqam sifatida o'tardi — IKKI modulda.** `oee._number` va
   `manufacturing._number` ning `isinstance(value,(int,float))` tarmog'i
   `float(value)` ni **tekshirmasdan** qaytarardi. `inf >= 0` **True**, shuning
   uchun cheksiz `run_seconds` yig'indiga qo'shilib, `availability = inf` chiqardi.
   Fix: `return text if -inf < text < inf else None`. Ya'ni **uch fakt**: `None`
   = yozilmagan, `0` = deklaratsiya, `inf/nan` = **o'qib bo'lmaydigan**.
2. **`andon` ko'rsatish oynasidan narini ko'rmasdi — KRITIK.** `andon`
   `report(..., limit=limit)` chaqirib, faqat **qaytgan** (kesilgan)
   stansiyalarni baholardi va `complete: True` ni **qo'lda** yozardi → 60
   stansiyali zavodning 51-si buzsa `breach_count: 0`. Fix: `report(...,
   limit=MAX_STATIONS)` (hammasini baholaydi), `'stations'` = `[:limit]` (faqat
   qaytarish oynasi), `'evaluated_count'`, `'complete': not result['truncated']`
   (da'vo emas — **hosil**). *Chegara ≠ baholash ≠ da'vo.*
3. **`not_evaluated` yalang'och son edi.** Fix: `not_evaluated_metrics` +
   `not_evaluated_stations` (`{station, metrics}`) — son qoldi, nomlar qo'shildi.
4. **BOM `truncated` chegarada yolg'on gapirardi — latent.**
   `len(lines) >= MAX_BOM_LINES` aynan 200 da `True` berardi, hech nima
   tashlanmagan bo'lsa ham. Fix: bayroq faqat haqiqiy qator tashlanganda
   ko'tariladi. **Muhim:** `sheets.MAX_ROWS = 200` va `max_rows` ni 200 dan
   **oshirish rad etiladi**, shuning uchun nuqson joyida **ko'rinmaydi** — test
   **chegarani pasaytiradi** (`patch(MAX_BOM_LINES, 4)`), oshirishga urinish esa
   o'zi xato beradi. *"Latent" ≠ "yo'q".*
5. Sog'lom topildi: `assets.parse_path`, `sheets` (`len(values) > limit`),
   `vision`, `escalation`, `speech`, `engine` darvozasi; butun
   `platform_runtime` kirill-lookalike bo'yicha toza.

**Regressiya:** test_oee **63→73**, test_manufacturing **63→70** (+17);
probe_oee **63→78** (+15, §7), probe_manufacturing **25→32** (+7, §7).
**Baseline (o'ninchi marta):** **1934 test**, `failures=1, errors=149,
skipped=1` — **o'zgarmadi**.

**Hujjatlar:** `AUDIT-CORRECTIONS-UZ.md` ga "Ultra-audit, 2026-09-20 — P12 va P13
ni qo'shni modullarga qarshi tekshirish" bo'limi; `BACKLOG.json`
(`offline_tests: 1934`, probe 63→78, audit topilmalari bilan);
`PROGRESS-UZ.md`, `README.md`, `V05P-IMPLEMENTATION-UZ.md` sonlari yangilandi.

**Saboq (uch tomonlama):** (a) *docstring — dalil emas* **to'rtinchi marta**
tasdiqlandi; (b) **latent nuqsonni ham o'lchash kerak** — chegara pasaytirilsa
ko'rinadi; (c) **"baholash" va "ko'rsatish"ni ajratish shart** — chegara, son,
da'vo (`complete`) uch mustaqil narsa.

**Keyingi: P14 `telephony_outbound`** — A bosqich (qo'ng'iroq hodisalari +
qattiq rozilik darvozasi: deklaratsiya + kodda rad etish, audio **hech qachon**
platformaga yetmaydi).

## P14 `telephony_outbound` B bosqich — QURILDI VA AUDIT QILINDI

A bosqich yakunlandi (probe 69, test 57, baseline 1991). Keyin **B bosqich**
qurildi: chiqish navbati + tezlik shifti + saqlash oynasi.

- `telephony.py` ga 2 yangi read tool: `telephony.queue`, `telephony.retention`
  (registry 82 → 84). Yangi config kalitlari: `throughput`
  (`per_window`/`window_seconds`), `retention_days` (consent registri bo'lsa
  **majburiy**, VO-07).
- **Navbat**: faqat `outbound`; kiruvchi hech qachon element emas; roziliksiz
  qator **ko'rsatiladi** (`consented: false` + sabab).
- **Tezlik shifti IJRO ETILADI**: ortiqcha `over_capacity` da sanaladi, callable
  deb ko'rsatilmaydi. Sur'at e'lon qilinmasa **sig'im 0**. `limit` elementni
  cheklaydi, baholashni emas.
- **Saqlash — hisobot, harakat emas**: `deleted` doim 0; uchta holat
  `in_window`/`past_window`/`unaged`; `unaged` **uchtinchi** holat (sana xatosi
  yozuvni yo'q qilmasligi uchun).

### Audit topgan haqiqiy nuqson (oltinchi "docstring — dalil emas")
`queue()` har chiqish qatori uchun `consent()` chaqirardi; `consent()` rozilik
registrini **qaytadan o'qirdi** → 50 qatorli navbat = 50 provayder GET.
**To'g'ri natija, testdan o'tgan, lekin yashirin O(N).** Probe hop sonini
o'lchagani uchun topdi. Tuzatish: ichki `_index` tikuv (tool yuzasida yo'q).
`_scan()` ham tuzatildi. Probe endi 12 va 60 qator uchun bir xil **2 hop**
o'lchaydi.

### Raqamlar
- test_telephony.py **57 → 137** (`TelephonyStageBTests` — ota-sinfdan 57 meros
  + 24 yangi; meros ataylab, B A ning invariantini buzsaydi → A testi yiqiladi).
- probe **69 → 98** o'lchangan xossa (8-bo'lim +29), hammasi PASS.
- check_telephony_example.py **12 → 22** PASS.
- **Baseline 2071 test**, `failures=1, errors=149, skipped=1` — 13-marta aynan.

### Hujjatlar
`V05R-IMPLEMENTATION-UZ.md` (yangi), `V05Q` (raqamlar tuzatildi),
`AUDIT-CORRECTIONS-UZ.md` (B auditi), `BACKLOG.json`
(`STAGE_B_LOCAL_CONTRACT_TESTED`, 2071, next = C bosqich), `PROGRESS-UZ.md`,
`README.md` (2071).

### Keyingi: C bosqich
Eskalatsiya yetkazish **P6 koordinatori orqali** — ikkinchi bildirishnoma yo'li
**ochilmaydi**. Audio saqlash hech qachon, hech qanday bosqichda.

## P14 `telephony_outbound` C bosqich — QURILDI, AUDIT QILINDI, P14 YAKUNLANDI

C bosqich: telefon navbati **P6 koordinatori orqali** eskalatsiya qilinadi —
**ikkinchi bildirishnoma yo'li ochilmadi**.

### Nima qo'shildi
- `escalation.py`: `SOURCES = ('workforce', 'telephony')`,
  `SOURCE_TOOLS = {'workforce': 'workforce.workload', 'telephony':
  'telephony.queue'}`. **`DELIVERY_TOOLS` O'ZGARMADI** =
  `frozenset({'telegram.send'})` — bitta element ham qo'shilmadi.
- `SCHEMA` ga `source TEXT NOT NULL DEFAULT 'workforce'` (eski yozuvlar
  buzilmaydi).
- `configure(..., source='workforce')` — manbaning **o'quvchisi** ham,
  `telegram.send` ham talab qilinadi (ikki tomonlama ruxsat).
- `_read_overdue` manba-ko'lamli; yangi `_telephony_items()` navbatni element
  shakliga normallashtiradi (faqat rozilikli qatorlar element bo'ladi;
  `blocked_count`/`over_capacity`/`capacity`/`callable_count`/`truncated` →
  `self._telephony_meta`).
- `_overdue_items` da **eskilik faqat `workforce` ga** — raqam "eskirmaydi".
  Aks holda navbat qatorlari jimgina va sababsiz yo'q bo'lardi.
- `_digest` rad etilgan son, sur'at ortiqchasi va qisqartirish qatorlarini
  yozadi. `_preview_tool` ga `source` qo'shildi.

### Audit natijasi — modul nuqsoni TOPILMADI
C **kod qo'shmadi, yo'l tanladi**; shuning uchun savol "xatti-harakat" emas,
"tuzilish" edi. Tekshirildi: bitta yuboruvchi, yangi kanal yo'q, ikki tomonlama
ruxsat, sxema orqaga mos. Topilgan 3 test xatosi va 1 probe xatosi — hammasi
**o'lchov tomonida**:
- assert juda qo'pol (dayjest oyog'idagi qonuniy inkor iborasini ism deb bildi);
- kapital harf (`Rad etilgan` ↔ `rad etgan`);
- preview haqiqiy Sheets yo'lini chaqirdi → skriptlangan hop qo'yildi;
- probe 8-bo'limi 7-bo'limning **jadval holatini** o'lchab qolgan edi
  (`tick()` bir qadam = bitta jadval, `ORDER BY next_due, id LIMIT 1`) →
  oldingi jadval `disable()` qilindi.

Saboq: *o'lchov asbobi o'z holatini o'zi e'lon qilishi shart* — B bosqichdagi
payload xatosining ikkinchi ko'rinishi (endi jadval holatida).

### Raqamlar (yakuniy)
- test_escalation.py **45 → 106** (`EscalationTelephonySourceTests`, 16 yangi).
- test_telephony.py **137** (A: 57 + B: 80).
- probe_telephony_consent **98**, probe_escalation_boundary **36**,
  probe_oee **78**, probe_manufacturing **32** — hammasi PASS.
- check_telephony_example **22** PASS; check_escalation_example ikkala manba;
  check_capabilities_example `registry tools: 84`, `unknown: []`.
- **Baseline: 2132 test**, `failures=1, errors=149, skipped=1` — **o'n
  to'rtinchi marta aynan o'zgarmadi** (yagona yiqilish Windows-only
  `test_all_files_private`).

### Hujjatlar
`V05S-IMPLEMENTATION-UZ.md` (yangi), `AUDIT-CORRECTIONS-UZ.md` (C auditi),
`BACKLOG.json` (`offline_tests: 2132`, `telephony_outbound` →
`STAGE_C_LOCAL_CONTRACT_TESTED`, `completion_claim: "P1-P13 + P14 stages A, B and
C"`), `PROGRESS-UZ.md` (V05S), `README.md` (2132).

### P14 yakuni — uchta invariant, uch bosqichda ham
1. **Audio hech qachon** platformaga yetmaydi, saqlanmaydi (P11b vision
   qoidasining davomi). Rozilik registri — **deklaratsiya**; roziliksiz raqam
   **kodda nom bilan rad etiladi**; yozib olish **alohida** rozilik talab qiladi.
2. **Dialer yozilmadi** — modul faqat **o'qiydi** (5 read-only tool).
3. **Ikkinchi bildirishnoma yo'li ochilmadi** — yetkazish faqat P6 koordinatori,
   faqat `telegram.send`.

`production_release` **NO_GO** — jonli qabul (live acceptance), STT/TTS sifati
(`NOT_RUN`), VO-06 budjet simi, haqiqiy yozuv o'chirish, SIP/kodek,
parallel slotlar, provayder rate-limit retry — hammasi **ataylab ochiq**.

### Keyingi ochiq ishlar (foydalanuvchi so'ramagan, ataylab kechiktirilgan)
Haqiqiy dialer / SIP / kodek / parallel slotlar / provayder rate-limit retry /
haqiqiy yozuv o'chirish / VO-06 `usage_budget` simi / STT-TTS jonli sifat
o'lchovi / jonli provayder qabuli.

## P14 yakuniy tekshiruv — kirill-lookalike skani (yopildi)

Oldingi sessiyada uzilib qolgan skan yakunlandi. P14 ning **13 fayli** bo'yicha
`grep -P '[\x{0400}-\x{04FF}]'` → **0 natija** (toza). Ya'ni P12/P13 ultra-auditida
butun `platform_runtime` bo'yicha olingan natija P14 fayllarida ham tasdiqlandi —
lotin harflari ostiga yashiringan kirill o'xshash harflari **yo'q**. P14 to'liq
yopilgan holda qoladi.

### Sessiya yakuni
Fayl o'zgartirilmadi (faqat shu memory yozuvi). Foydalanuvchi keyingi topshiriqni
beradi. `next_source_block` = `telephony_live_acceptance`; `production_release`
= **NO_GO**.

---

# Sessiya 2 (2026-09-20, davomi) — keyingi blok tanlash auditi

Buyruq: *"keyingi bosqichga o'tishing kerak va u tugagandan keyin butun loyiha va bu
qilingan ish ustida ultra audit o'tkazasan"*.

## Jonli o'lchangan holat (o'zgartirilmagan)

```
completion_claim : P1-P13 + P14 stages A, B and C
offline_tests    : 2132
next_source_block: telephony_live_acceptance (P14 open: dialer, budget wiring, live quality)
production       : NO_GO
build_registry() : 84 tools
known_tool_names : 85
```

`roadmap_v05.phases`: 15 ta — 14 tasi `LOCAL_CONTRACT_TESTED`, `P8L` =
`INVESTIGATED_REFUSED`, `P14` = `STAGE_C_LOCAL_CONTRACT_TESTED`. **Barcha roadmap
fazalari yopilgan.**

`IMPLEMENTED_CRM_DRIVERS = {amocrm, bitrix24, custom_webhook, kommo, onec}` —
`ADAPTERS` ham xuddi shu. `CRM_DRIVERS` esa 12 ta: `moysklad` **deklarativ
placeholder**, native adapteri yo'q (`Forbidden: CRM driver moysklad has no
executable adapter yet`).

## Keyingi blok nima uchun `inventory_moysklad`

Tracker'ning o'z `next_source_block`i = `telephony_live_acceptance`, lekin bu
**jonli provayder ishi** (dialer, SIP, kodek, parallel slot, rate-limit retry,
STT/TTS sifati, budget simi) — bu muhitda o'lchab bo'lmaydi. Xuddi shu sabab
`production_operations`, `windows_desktop` (POSIX-only xavfsizlik kontrakti),
`billing` uchun ham.

`inventory_moysklad`:
* `BACKLOG.json` items → `TODO`; `roadmap_v04` → `P4`, qiymat: *"Stock, price and
  cost for the seller without Excel round-trips"*.
* `docs/development/V04-IMPLEMENTATION-UZ.md` §7 (o'sha paytdagi reja):
  *"Keyin **`inventory_moysklad`** (P4), chunki mijoz narx/qoldiq og'rig'ini ikki
  marta aytdi."*
* `roadmap_v04.open_risks[0]`: *"price/source conflicts between MoySklad, 1C and
  Sheets need an operator-declared source priority"*.
* Platformada to'liq tayyor pattern bor: `onec_adapter.py` (typed adapter
  shabloni), `crm_gateway.py` (tools + fail-closed dispatch),
  `crm_contract.py` (`validate_crm_config`, capability ladder, `plan_crm_fingerprint`),
  `connectors.py` (`describe` dispatch).

## Audit topilmasi: `source_priority` — jimgina tashlab yuborilgan kalit

`business_graph.graph_config()` (108–115-satr):

```python
unknown = set(raw) - {'conflict_policy', 'entities', 'source_priority'}
if unknown:
    raise ValueError(f'business_graph has unsupported keys: {sorted(unknown)}')
```

`'source_priority'` **faqat shu qatorda** uchraydi — butun repoda boshqa hech qayerda
o'qilmaydi (`grep -rn source_priority api-python/ docs/ config/ apps/` → 5 natija,
4 tasi boshqa narsa, 1 tasi PRD misoli). Ya'ni:

* `graph_config()` qaytaradigan dict'ga **kirmaydi** — u faqat `conflict_policy` va
  `entities` ni qaytaradi;
* shuning uchun `search()`/`conflicts()`/`explain()`/`timeline()` uni **hech qachon
  ko'rmaydi**;
* natijada: `business_graph.source_priority` hujjatlashtirilgan blok kaliti
  (boshqa bloklarda hujjatlashtirilmagan kalit **raise** qiladi), lekin bu yerda
  jim qabul qilinadi va **jimgina yo'qoladi** — aynan o'sha modul docstring'i
  *"configuration errors raise instead of being skipped"* deb va'da qilgan narsaga
  zid. Operator ziddiyat qoidasini e'lon qiladi, platform hech narsa qilmaydi va
  xato bermaydi.

Bu **`oee_and_andon` darsi bilan bir sinf**: *"the defect class this block is most
exposed to — documentation that contradicts the code"*.

## Jonli o'lchangan yana bir fakt (hujjat bilan ziddiyat)

`config/business-graph.example.json` **kalit tartibiga tayanadi**: manba
prioriteti `priority: ["moysklad", "onec_docs", "finance"]` massividan kelib
chiqadi, `sources` obyektining kalit tartibi emas.

* `_entity()` prioritetni `entry.get('priority', list(clean))` bilan oladi —
  `sources.items()` tartibiga **fallback qiladi**;
* `list(clean)` manba obyektlarining **hujjatdagi tartibi**; `dict` Python 3.7+ da
  tartibni saqlaydi, ya'ni hozir ishlaydi, **lekin JSON'da tartib semantik emas**;
* `priority` esa **ixtiyoriy**.

Ya'ni `priority` yozilmasa, natija JSON tahrirlovchi/o'qish yo'lining tartibiga
bog'liq bo'ladi. Bu hujjatlashtirilmagan.

## Boshqa drift (bu sessiyada tasdiqlangan)

`docs/CRM-ECOSYSTEM-AND-INTEGRATIONS-UZ.md` §2.1/§3 hali ham **qo'ng'iroq audiosini
S3'ga saqlash** va CRM'ga `audio_url` yozishni tasvirlaydi — bu v0.5 qarori
(*"Video frames never reach the platform"*), P14 ning strukturaviy invariantlari va
foydalanuvchining *"Hech qachon audio"* qaroriga **zid**. Tarixiy mijoz hujjati,
lekin yolg'iz o'qigan kelajakdagi contributor audio yo'lini qayta quradi.

## Keyingi qadam

Blok: `inventory` modul oilasi + `moysklad` typed adapteri (o'qish yo'li), PRD §6.3
`source_priority` ni haqiqiy qilish, topilgan 2 ta jimgina yo'qolish nuqsonini
tuzatish, `probe_inventory_boundary.py`, config misoli + checker, to'liq suite'da
baseline **o'zgarmasligi** (failures=1, errors=149, skipped=1).

## Qadam 1 BAJARILDI — `source_priority` jimgina yo'qolishi tuzatildi

**Tuzatish** (`api-python/platform_runtime/business_graph.py`):
* `graph_config()` endi `source_priority` ni o'z natijasiga **soladi** (ilgari
  whitelist'da bor edi, lekin qaytarilmasdi).
* `_priority_table()` — yangi validator: per-attribute `{primary, fallback}` yoki
  bare string shorthand. `MAX_PRIORITY_ATTRIBUTES = 40`.
* `_entity()` endi entity-darajali `source_priority` ni ham qabul qiladi va
  **shared (top-level) + own (entity)** ni birlashtiradi — entity ustun.
* Tekshiruv: e'lon qilingan sumber entity'da yo'q bo'lsa → `ValueError`;
  attribute hech bir manbada map qilinmagan bo'lsa → `ValueError`. Ikkalasi ham
  **jimgina no-op** bo'ladigan typo sinfi.
* `_primary()` endi per-attribute order'ni ishlatadi (entity order fallback).
  Primary topilmasa → entity order'ga **davom etadi** (yo'qolmaydi).
* `explain()` endi `attribute_priority` + `priority_source` qaytaradi.
* Latent hazard yopildi: `priority` e'lon qilinmasa, order endi **sorted(clean)**
  — JSON kalit tartibiga bog'liq emas (deterministik).

**Testlar:** `test_business_graph.py` ga `SourcePriorityTests` (12 test) qo'shildi.
98 → **157 test** (o'sha faylda), OK.

**Probe:** `scripts/probe_inventory_boundary.py` — 34 xossa, 34 pass, 0 fail.
(1-qadamda 26 xossa edi, 2 ta xossam o'zi xato chiqib tuzatildi.)

**Baseline tasdiqlandi (o'zgarishdan OLDIN):** `Ran 2132 tests` →
`failures=1, errors=149, skipped=1` — 15-sessiya ketma-ket o'zgarmagan.

## Qadam 2 BAJARILDI — `inventory` modul oilasi

**Yangi fayl:** `api-python/platform_runtime/inventory.py` (~600 qator).
4 ta **read-only** tool: `inventory.product`, `inventory.stock`, `inventory.price`,
`inventory.margin`. `INVENTORY_TOOLS` tuple, `inventory_config(tenant)` validator,
`MAX_*` boundlar, `InventoryError`, idempotent `register_inventory_tools`.

**Modul oilasi patterni to'liq bajarildi:**
* yangi transport **yo'q**, yangi ulanish **yo'q**, yangi registr **yo'q**;
* har bir o'qish `business_graph.resolve()` orqali — shuning uchun register
  deklaratsiyasi, A1 allowlist, agent tool ruxsati va connection allowlist
  o'zgarishsiz amal qiladi;
* `tools.py` da `register_inventory_tools(r)` graph'dan keyin chaqiriladi.

**Chegaralar (hammasi o'lchangan):**
1. Narx **manbasi bilan** keladi — manbasiz raqam sotuvchi himoya qila olmaydigan raqam.
2. Ziddiyat **ko'rsatiladi**, jim tanlanmaydi (`report` da hech narsa tanlanmaydi).
3. Blank / nol / o'qib bo'lmaydigan — **uch xil fakt**; `nan`/`inf` rad etiladi.
4. Foyda faqat **ikkala** o'qilgan qiymatdan; aks holda `None` + sabab nomi.
5. Ombor soni **"sotuvga tayyor" degani emas** (strukturaviy: `available` kaliti yo'q).
6. Odam bo'yicha ko'rsatkich **yo'q** (strukturaviy).
7. Yozish yo'li **umuman yo'q** (source matnida o'lchangan).

**Ultra-audit o'zimning kodimda 2 ta haqiqiy nuqson topdi:**
* **A:** `report` siyosatida narx ziddiyatli bo'lsa `selected: null` → `margin` `None`
  bo'lardi, va bu "hisoblab bo'lmadi" bo'lib ko'rinardi. Aslida ma'lumot **to'liq**
  o'qilgan, platforma shunchaki narx **tanlashdan bosh tortgan**. Tuzatildi: `_reason()`
  funksiyasi 4 xil sababni ajratadi (`not_declared`/`blank`/`unread`/`conflict`) va
  chiqishda `not_computable` (ma'lumot) va `margin_blocked_by_conflict` (siyosat)
  **alohida** ro'yxatlar. Operator ikki xil joyga yuboriladi.
* **B:** `product_id` yo'li `margin_reasons`, katalog yo'li `not_computable`
  qaytarardi — **mos kelmaydigan kalit**. Tuzatildi, ikkalasi bir xil.

**Testlar:** `api-python/runtime_tests/test_inventory.py` — **38 test**, OK.
**Config misoli:** `config/inventory.example.json` + `scripts/check_inventory_example.py`
(module validator'ining o'zi bilan tekshiriladi; checker 2 ta haqiqiy
nomuvofiqlikni topdi — `category` va `currency` grafikda e'lon qilinmagan edi).
**Probe:** `scripts/probe_inventory_boundary.py` — **68 xossa, 68 pass, 0 fail**
(6 bo'lim; 6-bo'lim inventory modulining o'z chegarasini manba matnida o'lchaydi).

## Qadam 3 — ULTRA-AUDIT: Unicode raqamlar (butun platforma) — HAQIQIY NUQSON

**Qanday topildi:** `inventory._number` ning chekka holatlari skani paytida
`_number('१२')` (devanagari) → `12.0` chiqdi. Savol tug'ildi: bu xato? O'lchandi.

**Ildiz sabab:** Python'da `re` **standart holatda Unicode-aware**, ya'ni `\d`
faqat `[0-9]` emas — **`Nd` (decimal digit) kategoriyasining hammasi**. Repo'da
`re.ASCII` **hech qayerda** ishlatilmaydi (tekshirildi). `float()` ham Unicode
raqamlarni **normalizatsiya qiladi**.

**O'lchangan ta'sir — 4 modul, hammasi jimgina:**

| Modul | Kirish | Natija | Nega muhim |
|---|---|---|---|
| `erp._date_text` | `'१३.01.2026'` | **`'2026-01-13'`** | Sanani **jimgina qayta yozadi** — tizimda umuman bo'lmagan ASCII qator qaytadi. Bu *"platforma sanani to'qimaydi"* va'dasiga zid; noto'g'ri davrga o'tkazilgan hujjat — **soliq muammosi**. |
| `erp._date_text` | `'२०२६-01-02'` | qabul | ISO shoxobchasi; dublikat-ledger kaliti shu qatordan |
| `documents.py` 564 | `'२०२६-01-03'` | weekend signal | `int(part)` → `date(2026,1,3)` **shanba**. O'lchandi: `documents` **2026-01-02 ni bir marta ham ishlatgan**, ya'ni signal *o'sha* sanaga tushadi. |
| `escalation._is_stale` | `'२०२०-01-01'` | `AGEABLE` | `int(group)` → sana **yaroqli** deb hisoblanadi. Modul va'dasi: *"o'qib bo'lmaydigan sana **hech qachon** eskirmaydi"* — buzildi. |
| `inventory`/`oee`/`manufacturing` `._number` | `'１２'` | `12.0` | o'lchovli miqdor/narx |

**Nima uchun bu "kosmetik" emas:** `erp.py` — repo'dagi **eng xavfli kod**
(moliyaviy yozuv). Uning docstring'i *"hech qanday maydon **to'qilmaydi**"* deb
va'da qiladi; `'१३.01.2026'` → `'2026-01-13'` esa **aynan to'qish** (qayta
yozish). Uchta mustaqil shoxobcha (`erp`, `documents`, `escalation`) bitta
kutilmagan kirishga **uch xil** javob beradi — bu tasodif emas, **umumiy
o'zgarmasning yo'qligi**.

**Holat:** tasdiqlandi, tuzatish ustida ish boshlandi.

## Qadam 3 (davomi) — TUZATILDI: `platform_runtime/cells.py`

**Yangi umumiy modul:** `api-python/platform_runtime/cells.py`. Ikki funksiya,
**ataylab boshqacha**:
* `is_ascii_number(text)` — qiymat o'quvchilar uchun ("raqam sifatida o'qish
  mumkinmi?"). `-5`, `1,5` qabul; `\d` oilasi, `1e5`, bo'sh, non-string rad.
* `is_ascii_digit_run(text)` — sana/qiymat parserlari uchun ("parsing mumkinmi?").
  `int()` **normalizatsiya qilishidan oldin** rad etish kerak.

Ajratish **funksional zarur**: birinchisi belgi/vergulni qabul qiladi, ikkinchisi
faqat toza raqamlar ketma-ketligini.

**Tuzatilgan 11 modul:** `erp`, `documents` (ikki joy + hafta kuni yo'li),
`escalation`, `workforce` (`_hours` + `_iso_day_text`), `telephony`
(`_duration` + `DAY_RE`), `vision` (`DAY_RE`), `inventory`, `oee`,
`manufacturing`, `tools` (Instagram `account_id`).

### Ikkinchi skan — naqsh bo'yicha (muhim saboq)
**1-skan modul bo'yicha:** 4 modul. **2-skan naqsh bo'yicha:** yana 4 modul
(`workforce`, `telephony`, `vision`, `tools`). Ya'ni birinchi skan **yetarli
emas edi**. Nuqson modulning ishi emas — **hammasining umumiy odati**.

### Uchinchi nuqson — o'lik konstantalar (yana `source_priority` sinfi)
`inventory.MAX_NAMES` va `oee.MAX_NAMES` — e'lon qilingan, **hech qayerda
o'qilmaydigan**. Oilada `MAX_NAMES` *odamlar ro'yxatini* chegaralaydi;
`inventory` mahsulot, `oee` stansiya qaytaradi — chegaralanadigan `names` yo'q.
Ishlatilmaydigan chegara **yo'q chegaradan yomonroq**. Ikkalasi izoh bilan
olib tashlandi.

### Ikkinchi, alohida nuqson — `inf`/`nan` teshigi `workforce._hours` da
P12/P13 auditi bu teshikni `manufacturing._number` va `oee._number` da yopdi,
lekin **`workforce` ga yetib bormadi** — o'sha skan ham *modul bo'yicha* yurilgan
edi. Bu safar u **umumiy o'zgarmas** sifatida yozildi.

### Qo'riqchilar (ikki qavat)
1. **Xulq-atvor:** `api-python/runtime_tests/test_cells.py` — **39 test**, har
   bir modul **tashqarisidan** rad etishni o'lchaydi.
2. **Struktura:** `NoUnicodeDigitPatternRemainsTests` — **11 kuzatilayotgan
   modulning manba matnini** o'qiydi, yalang'och `\d` qaytsa **yiqiladi**.

**Qo'riqchi diskriminatsiya qilishini o'lchadim:** `oee.NUMBER_RE` ni buzib
qaytardim → **4 test yiqildi** (`12.0 is not None`). Tikladim → `OK`. Ya'ni
test haqiqatan o'lchaydi.

### Test xatosi (mening, o'lchov tomonida)
`telephony._duration('12,5')` → `12.5` deb kutgandim, aslida `None`. Sabab:
vergul **naqshdan oldin** nuqtaga almashtiriladi, keyin `[0-9]{1,6}` uni rad
etadi. Bu **mening tuzatishimdan oldin ham** shunday edi — regressiya emas.
Test **o'lchangan holat** sifatida qayta yozildi (`12.5` qabul, `12,5` rad).

### Uchinchi skan — konversiya bo'yicha
`whatsapp_inbound._epoch` — `str.isdigit()` ham **Unicode-aware**. `'१२३'` →
`int('१२३')` = `123` → oyna **1970** dan hisoblanardi. Tuzatildi.

**Tekshirilgan va TOZA topilgan (o'lchangan, taxmin emas):**
* **`business_graph.canonical`** — `_NUMERIC` **har doim** `[0-9]` ishlatgan,
  ya'ni ziddiyat detektori **allaqachon to'g'ri** edi. Eng nozik joy: Unicode
  raqam na raqam, na ASCII egizakning ikkinchi yozilishi bo'lishi kerak — aks
  holda buzilgan katak haqiqiy qiymat bilan *kelishgan* deb ko'rsatilardi (bu
  xabar qilingan ziddiyatdan **yomonroq**). Probe xossasi qo'shildi.
* `database/*` (`validate_endpoint`), `sheets.bounded_int` (operator konfigi),
  `crm_contract.bounded_price` (string qabul qilmaydi) — katak o'qimaydi.

**Yakuniy qamrov: 13 modul, 11 xil yo'l.** Uch skan: modul → naqsh → konversiya,
har biri oldingisidan kengroq.

### O'lchovlar (yakuniy)
| Nima | Oldin | Hozir |
|---|---|---|
| Offline to'plam | 2229 | **2276** |
| Signatura | `failures=1, errors=149, skipped=1` | **aynan o'zgarmadi** |
| `test_cells.py` | — | **45, OK** |
| `probe_inventory_boundary` | 68 xossa | **128 xossa** (+60, §7) |
| Ta'sirlangan suite'lar | — | **642 OK** |
| Config checker'lar | 7 | **7 OK** |
| Boshqa probe'lar | 4 | **4 PROVEN** |
| Registry | 88 | **88** (o'zgarmadi) |

**Aritmetika aniq:** 2229 + 2 (`test_business_graph.py` da avvalgi qadamning 2 ta
`graph.entities` testi) + 45 (`test_cells`) = **2276**. Izohlanmagan farq yo'q
(avval 2268 chiqqan edi — farq shu 2 ta testdan, topildi).

**17-marta** ketma-ket signatura o'zgarmadi. Yagona `failures=1` — Windows-only
`test_all_files_private` (`0 != 63`).

### Hujjatlar
`docs/development/ULTRA-AUDIT-ASCII-CELL-UZ.md` (yangi, 10 bo'lim);
`BACKLOG.json` (`offline_tests: 2276`, claim'ga `+ ASCII-cell invariant (cells)`,
`roadmap_v04.open_risks` ga 6-chi yozuv); `README.md` (2276).

**`BACKLOG.json` tahriri saboqi (takrorlandi):** skript `open_risks` ni **noto'g'ri
kalitda** qidirdi (`d['open_risks']`) → `KeyError`, va **yozuv qisman** qo'llandi
(`offline_tests` yozildi, risk yo'q). To'g'ri joy — `roadmap_v04.open_risks`.
Qayta o'qib tasdiqlandi: 38 item / 15 faza / 8 v0.4 qator / 6 risk.

### Saboq (uch tomonlama)
1. **"Docstring — dalil emas"** — 17-marta. Bu safar aksi: kod **va'da qilgan**
   narsani (`erp` "to'qimaydi") kod **buzayotgan** edi.
2. **Modul bo'yicha skan yetarli emas — naqsh bo'yicha skanerlash kerak.**
3. **Umumiy o'zgarmas umumiy modulda yashashi kerak.** Alohida tuzatilgan qoida
   qo'shni modulga yetmaydi.

### Keyingi qadam
Ultra-audit davom etadi: `_reason()` barcha siyosatlarda, `limit`/truncation
o'zaro ta'siri, `assets`/`briefing`/`supervisor` kesishmalari.

---

## Ultra-audit II faza — `truncated` haqiqatni aytmaydi (2026-09-20, kech)

**Yangi nuqson sinfi topildi: "jimgina ishonchli noto'g'ri signal".**
`truncated` bayrog'i `len(result) >= limit` dan hisoblanardi — bu ifoda **ikki xil
holatda ham rost**: ro'yxat kesilganda **va** aynan `limit` da tugaganda. Ular
qarama-qarshi ma'no bildiradi ("kattaroq limit bilan qayta so'ra" vs "bu hammasi").

**O'lchandi (taxmin emas):** `graph.search`, haqiqiy tool + config bilan,
`10 matching, limit 10` → `truncated=True` — **kesilmagan**, lekin operatorni
bir xil ro'yxatni qaytaradigan so'rovni qayta yuborishga yuboradi.

**7 joy tuzatildi:**
- `business_graph`: `identifiers()` (ikki yo'l), `search()`, `conflicts()`, `timeline()`
- `oversight.activity()` — SQL `LIMIT ?` o'zi kesadi, uzunlikdan **umuman** bilib
  bo'lmaydi → **`limit + 1`** so'raladi, ortiqchasi tashlanadi
- `workforce`: `shifts()`, `workload()`

Predikat endi **rad etish joyida** o'rnatiladi (`cut = True; break`), keyin
uzunlikdan o'qilmaydi. `search()` to'lgan ro'yxatdan keyin ham skanerlaydi —
`by_id` xotirada, qo'shimcha provider o'qishi yo'q.

**Test nuqsonni qulflab qo'ygan edi:** `test_oversight.
test_activity_truncation_is_reported` `limit=1 → True` deb da'vo qilardi. Seed'da
bu agent uchun **aynan 1 ta** qator bor → javob **to'liq**, `False` kerak.
Eski predikat (`1 >= 1`) **tasodifan** True bergan, test **yashil** edi, lekin
**noto'g'ri javobni qulflagan**. Tuzatish uni ochdi; test to'g'rilandi + haqiqiy
kesish holati qo'shildi.

**To'g'ri bo'lganlar tegmadi:** `inventory` (`>=` **va** `len(ids) > len(out)`),
`assets` (`continue` + `seen` — eng yaxshi naqsh), `sheets`, `manufacturing`,
`oee`, `telephony`, `vision`, `workforce.attendance`.

### O'lchovlar
- Offline to'plam: **2334** (`Ran 2334 tests`, `failures=1, errors=149, skipped=1`
  — signatura **aynan o'zgarmadi**, 18-marta ketma-ket)
- `test_business_graph` 165 → **168**; `test_workforce` 32 → **36**;
  `test_oversight` 33 → **34**; `test_inventory` 38 → **82**
- `probe_ident_trunc` (yangi, `scripts/`) — **15 xossa, 15 pass**
- Ta'sirlangan suite'lar **320 OK**; registry **88/89**; 7 config checker **OK**;
  4 probe **PROVEN**
- Qo'riqchi diskriminatsiyasi: predikat qaytarildi → suite **3 failures, 15
  errors**; tiklandi → OK

### Hujjatlar
`ULTRA-AUDIT-ASCII-CELL-UZ.md` ga **II faza** (11-19 bo'limlar) qo'shildi;
`BACKLOG.json` — 7-chi risk (truncated sweep) + `inventory_moysklad.remaining`
ga 3 yangi nuqson; `README.md` → **2334**.

### Aritmetika yopildi (o'lchov asbobidagi nuqson edi)
`2276 + 44 + 8 = 2328` ≠ **2334**. Sabab topildi: **modul-modul yig'indi** (2201)
va **bitta discovery** (2334) farqi **133** — besh modul (`test_google_adapters`,
`test_google_reconcile`, `test_google_sync`, `test_managed_graph_search`,
`test_portable_fs`) yakka holda **import bo'lmaydi**, shuning uchun yakka holda 0,
discovery ostida to'liq. **2334 ning hammasi haqiqatan ishlaydi.** Da'vo uchun
**2334** (runner shuni o'lchaydi); 2201 — modul haqidagi raqam.
Tasdiqlangan yakuniy run: `Ran 2334 tests` / `failures=1, errors=149, skipped=1`.

### Saboq
1. **Predikat — bu savol.** `>=` va `>` bu yerda bir xil ko'rinadi, lekin biri
   **yolg'on gapiradi**. Nuqson modulda emas, **ifodada** edi.
2. **Naqsh bo'yicha skan modul bo'yicha skandan kuchliroq** — **ikkinchi marta**
   tasdiqlandi (`\d` edi, endi `>= limit`).
3. **Yashil test dalil emas.** Test bor edi, o'tardi, lekin **nuqsonni qulflagan**
   edi.
4. **Bir xil signal ikki xil ma'no bersa, u signal emas.**

---

## Ultra-audit III faza — `count` inventar o'rniga sahifa o'lchamini aytadi

**Nuqson (`erp.posting_status`):**
```python
'postings': rows, 'count': len(rows)     # count — SAHIFA, truncated YO'Q
```
O'lchandi: 20 posting va 100 posting **bir xil** `count=20` beradi. `truncated`
nuqsonidan bir qadam narida — u yerda bayroq **yolg'on**, bu yerda bayroq **yo'q**
va son **jami** deb o'qiladi.

**Tuzatish:** yangi `erp.ledger_page()` — davr va populyatsiya **bir o'qishda**
(`SELECT count(*)` + `LIMIT ?`), `(rows, total, truncated)` qaytaradi. Tool endi
uchta alohida fakt beradi: `count` (jami), `returned` (sahifa), `truncated`.
Predikat — `total > len(rows)`, ya'ni populyatsiyaga nisbatan (aynan `limit` ta
qatorli daftar **to'liq** deb hisoblanadi).

**Test nuqsonni o'lchamagan edi:** ikkala mavjud test **bo'sh** daftarda
`count == 0` ni tekshirardi — u yerda eski va yangi bir xil. `test_erp` 54 → **56**.
Qo'riqchi tekshirildi: eski shaklga qaytarildi → `AssertionError: 25 != 10`.

**Bir xil naqsh, tegilmadi (tuzoq, tirik nuqson emas):** `briefing.ledger()`,
`escalation.ledger()`, `reengagement.ledger()`, `supervisor.history()` —
tekshirildi: **runtime'da hech kim chaqirmaydi** (faqat testlar). Har biriga
docstring ogohlantirish yozildi.

**`assets` — namuna:** `'count': len(ordered)` **slice'dan oldin**, va
`'count': seen, 'returned': len(out)`. Nuqson yo'q.

### O'lchovlar
- Offline to'plam: **2336** (`Ran 2336 tests`, signatura **aynan o'zgarmadi**)
- `probe_truncation_verdict` 17 → **32 xossa** (6 bo'lim), 32 pass, exit 0
- Ta'sirlangan suite'lar **664 OK**; briefing+escalation+reengagement+supervisor
  **232 OK**; registry **88/89**

### Hujjatlar
`ULTRA-AUDIT-ASCII-CELL-UZ.md` — **III faza** (20-26 bo'limlar);
`BACKLOG.json` — 8-chi risk + `offline_tests: 2336`; `README.md` → 2336.

### Saboq
1. **"Bayroq yo'q" — "bayroq yolg'on" dan kam emas.**
2. **Bir xil nom ikki xil ma'no bersa — nom yolg'on.** `count`/`returned`/`truncated`.
3. **Test bo'sh holatda yozilsa, nuqsonni ko'rmaydi** — farq faqat chegaradan
   oshganda ko'rinadi.
4. **Tirik nuqson bilan tuzoqni ajrat:** 4 joy bir xil shakl edi, faqat **bittasi**
   operatorga ochiq. O'zgarish yuzasi = o'zgarish **ehtiyoji**.

---

## Ultra-audit IV fazasi — `documents.fraud_signals` arifmetikasi (17:30)

**Ikki nuqson bir funksiyada, ikkalasi ham o'lchandi.**

**1. `median = amounts[len(amounts) // 2]` — bu mediana emas.** Juft sonda
**yuqori-o'rta** element; xato **har doim yuqoriga**, ya'ni bayroqni ko'tarishni
**qiyinlashtiradi**. O'lchandi: `[100,200]` → chop etildi 200, haqiqiy 150;
`[100,300]` → 300 vs 200.

**2. `prior` faqat valyuta bo'yicha filtrlanardi** — hujjat oqimi
receive → store → check, shuning uchun **sinov hujjati o'z populyatsiyasida**.
O'lchandi: bir xil hujjat, `history_considered` = 2 (saqlashdan oldin) va **3**
(saqlangandan keyin).

**Oqibat (asosiy o'lchov):** `posting_plan` material signalni `needs_attention`
ga ko'taradi va `ready_for_approval` → `review_fraud_signals` almashtiradi.
Tarix `[100k, 300k]` → haqiqiy mediana 200k, 3x = **600k**. Eski kodda
**600,000–899,999 so'm** (haqiqiy 3x–4.5x!) hujjatlar **`ready_for_approval`**
qaytardi — tekshiruv butun shu oraliqda **o'tkazib yuborilgan**.

**Tuzatish:** yangi `_median()` (`statistics.median`, butun minor birlikka
yaxlitlangan) + `prior` dan `document_key` bo'yicha o'z-o'zini chiqarib tashlash +
`history_rows` / `history_considered` ikki xil nom.

**Testlar:** `test_documents` 83 → **90** (+7). Eski test **tasodifan** yashil edi:
fixture 3 ta (toq) hujjat, upper-middle = haqiqiy mediana. Yangi testlar juft
sondan **ikkala tomondan** o'lchaydi. Qo'riqchi tekshirildi: qaytarilganda
**3 failures, 1 error**.

**Probe:** `probe_truncation_verdict` 32 → **48 xossa** (7-bo'lim qo'shildi),
48 pass, exit 0.

**Probe'dagi o'z xatoyim:** `record()` **`is`** ishlatardi, `==` emas. Ilgari
hamma bo'limlar boolean solishtirardi (singleton), shuning uchun yashirin qolgan.
Birinchi marta **hisoblangan butun son** solishtirilganda (150000, keshdan
tashqarida) soxta 4 FAIL berdi. `==` ga tuzatildi + izoh yozildi. Boshqa
probe'lar **boolean shart** qabul qiladi — ularda bu tuzoq yo'q.

**To'plam:** 2336 → **2341** (`Ran 2341 tests`), signatura **aynan**:
`failures=1, errors=149, skipped=1`.

### Saboq
1. **"Median" deb chop etilgan son mediana bo'lishi shart** — aks holda approver
   bayroqni tekshirib bo'lmaydi. (I-III faza "nom yolg'on" saboqi, 2-marta.)
2. **Xatoning yo'nalishi muhim:** yuqoriga og'ish = signalni zaiflashtirish.
   Zaif signal — nuqson, chunki u **jimgina** ishlamaydi.
3. **"Populyatsiya" — bu e'lon qilinishi kerak bo'lgan tanlov.** O'z-o'zini
   kiritish arifmetik emas, **semantik** xato.
4. **Probe'ning o'zida ham nuqson bo'ladi:** `is` vs `==`. O'lchov asbobini
   ham tekshirish kerak.

---

## Ultra-audit V va VI fazalari — `documents.py` birlik va hisobot (18:00)

**V FAZA — `round_number` noto'g'ri birlikda.**

Shart `total_minor % 10 ** MINOR_UNITS[currency] == 0` — "butun major birlikmi?"
deb so'raydi. UZS (faktor 1) uchun dumaloqlikka to'g'ri keladi; **USD/EUR/RUB
(faktor 100) uchun faqat "sentlar nolmi?"** — deyarli har invoice'da rost.
Ya'ni signal **har valyutada o'lik, faqat UZS'dan tashqari**. Docstring esa
**teskarisini** da'vo qilardi.

O'lchandi: **USD 1000.00 → bayroq YO'Q** (mukammal dumaloq),
**UZS 100050 → bayroq HA** (umuman dumaloq emas).

Son ham yolg'on edi: `len(str(major).rstrip('0'))` — muhim raqamlar soni,
lekin matn uni dumaloqlik deb chiqaradi. `UZS 100050` → **5** ("very round"),
`UZS 1000000` → **1** ("barely round") — eng dumaloq summa eng kichik son.

**Tuzatish:** savol major birlikda (`% factor`), son esa **oxiridagi nollar**.
Yangi e'lon qilingan chegara `ROUND_TRAILING_ZEROS = 3` — 1 nol UZS'da oddiy
(123450, 123460), chegarasiz signal har uchinchi invoice'da yonardi.

**VI FAZA — `duplicates()` hisobot shakli.**

Mantiq to'g'ri edi (`altered` summa **va** valyutani solishtiradi), **hisobot**
noto'g'ri edi: `amount_changed` ikki xil valyutada formatlangan juftlik
berardi — **1000 UZS → 1000 USD** uchun `{'from': '1000', 'to': '1000.00'}`.
Ya'ni **summa o'zgarmagan**, lekin approver summa o'zgargan deb o'qiydi.

**Tuzatish:** `amount_changed` / `currency_changed` ajratildi;
`amount_delta_minor` **faqat** valyutalar bir xil bo'lganda (kurs yo'q →
`None`, taxmin emas). `_changed_amount()` nomlangan funksiyaga chiqarildi.

**Tekshirilgan, nuqson YO'Q:** `format_amount`/`normalize` aylanma yo'li —
1600 tasodifiy qiymat, 4 valyuta, **0 nomuvofiqlik**. `MINOR_UNITS` bitta joyda.
`weekend_date` — o'z naqshidan chetlashishga qarab ishlaydi, sanani o'qib
bo'lmasa jim turadi (to'g'ri).

**Natijalar:** `test_documents` 90 → **96**; probe 48 → **76 xossa** (9 bo'lim);
offline to'plam 2341 → **2345** (`failures=1, errors=149, skipped=1`, aynan).
Ikkala qo'riqchi qaytarib tekshirildi — ushladi.

### Saboq
1. **Birlik — spetsifikatsiya.** "Minor birlikda hisoblash" to'g'rilikni
   kafolatlamaydi. Docstring o'lchov emas (18-chi tasdiq).
2. **Ko'p valyutali kod bir valyutada sinaladi.** Hamma testlar UZS edi;
   USD yo'li hech qachon yugurmagan.
3. **Hisobot ham kod.** Mantiq to'g'ri bo'lsa ham, o'qiladigan natija yolg'on
   gapirishi mumkin.
4. **Solishtirib bo'lmaydiganini solishtirma.** Kurs yo'q → `None`.

## Ultra-audit VII faza — satr arifmetikasi va o'lik akkumulyator

**Skanerlash o'lchovi:** kod **o'likmi** va **qo'riqchi nima qilishga va'da
bergan** — modul bo'yicha emas.

**Nuqson 1 — teskari qo'riqchi (`_line_items`).** `quantity` tekshirilar va
saqlanar, lekin yig'indi `sum(item['amount'])` edi — ya'ni ko'paytirilmasdi.
Natija: `2 x 5000 = 10000` **RAD** etilardi ("o'ziga zid"), `2 x 5000 = 5000`
esa **QABUL**. Qo'riqchi aynan o'zi ushlashi kerak bo'lgan ziddiyatni
kafolatlar edi. Tuzatish: `amount` = **birlik narxi**,
`line_total = quantity * amount`, va `normalize()` chaqiradigan yagona nomlangan
`line_items_total()`. `test_documents` 96 → **100** (+4).

**Nuqson 2 — o'lik akkumulyator (`match()` ichida `compared`).** Ikki shoxobchada
to'ldirilar, hech qachon o'qilmasdi. AST bilan isbotlandi (`.append` — atribut
murojaati, o'qish emas) va qaytariladigan kalitlar ro'yxati bilan solishtirildi:
`compared` yo'q. Olib tashlandi.

**Probe:** yangi 10-bo'lim `section_line_item_arithmetic()` — 22 ta o'lchov,
ikkala tomon (ko'paytiriladigan va ko'paytirilmaydigan miqdor). Probe 76 → **98**.

**Qo'riqchi tekshirildi:** eski shakl qaytarilganda → probe **2 FAIL**,
`test_documents` **3 error**. Ikkalasi ham ushladi.

**Signatura aynan:** `failures=1, errors=149, skipped=1`, to'plam 2345 → **2349**.

**Agentlik xulosasi:** hujjat o'z arifmetikasiga zid bo'lsa rad etilishi kerak;
kod esa **to'g'ri** hujjatni rad etib, **noto'g'risini** qabul qilardi.

**IV saboq:**
1. To'ldirilgan akkumulyator ham o'lik bo'lishi mumkin — "o'lik chegara yo'q
   chegaradan yomon" qoidasining yana bir shakli.
2. Qo'riqchi ham kod; uning **teskari** ishlashini faqat **teskari tomondan**
   o'lchash aniqlaydi.
3. Tekshirilgan lekin ishlatilmagan maydon — eng jim nuqson: hech qanday xatolik
   chiqmaydi.
4. Satr o'z-o'zidan o'qilsa, kod ham **aynan shunday** hisoblashi shart.
5. `amount` atamasi ikki ma'noni ko'tarishi mumkin; `line_total` nomi muammoning
   yarmini yopadi.

## Ultra-audit VIII faza — sukut chegaralari va boshqa yuzalar

**Skanerlash o'lchovi:** AST bilan butun runtime bo'ylab **standart argument
qiymatlari** yig'ildi + savol "bu o'quvchini **kim** chaqiradi" — bu safar
**mahsulot** bo'yicha (HTTP ham chaqiruvchi).

**Nuqson 1 — `knowledge.search` kesilganini aytmasdi.** Javob shakli
`['collection','matches','retrieval','semantic_fact_check']`, `limit` 5 bilan
cheklangan, bayroq **yo'q**. O'lchandi: 50 ta mos bo'lak va 5 ta mos bo'lak
**bir xil** javob beradi. Bu retrieval natijasi — agent shu matnga tayanadi,
ya'ni 5/50 "o'ndan birini butun" deb o'qiladi. Tuzatish: `matched` / `returned` /
`truncated`, bunda `truncated = matched > returned` (populyatsiya bilan, limit
bilan emas), shuning uchun aynan to'g'ri kelgan korpus kesilgan deb
hisoblanmaydi. `test_knowledge` 31 → **34** (+3).

**Nuqson 2 — to'rt o'quvchi HTTP orqali ochiq edi.** `briefing.ledger`,
`escalation.ledger`, `reengagement.ledger`, `supervisor.history` — to'rttasi
yalang'och ro'yxat qaytarar, to'rttasining docstringida "runtime chaqirmaydi,
demak jonli nuqson emas" deb yozilgan edi. **Noto'g'ri savol**: HTTP marshrutlari
(`/briefing/{s}/ledger`, `/escalation/{s}/ledger`, `/reengagement/{p}/ledger`,
`/supervisor/history`) ularni `entries`/`routes` sifatida ochib beradi. Tuzatish:
`with_total=True` — sahifa va populyatsiya **bir o'qishda** sanaladi; standart
shakl **o'zgarmadi**, shuning uchun 247 mavjud test buzilmadi.

**Probe:** 11- va 12-bo'limlar qo'shildi. 98 → **143**. Probe endi **yorilib
emas, hisobot berib** yiqiladi — bayroqning **yo'qligi** ham o'lchov natijasi.

**Qo'riqchilar tekshirildi:** knowledge + supervisor qaytarilganda → probe
**9 FAIL**; to'rtta o'quvchi yalang'och ro'yxatga qaytarilganda → suite
**1 failure, 9 error**.

**Signatura aynan:** `failures=1, errors=149, skipped=1`, to'plam 2349 → **2370**.

**V saboq:**
1. "Runtime chaqirmaydi" — xavfsizlik dalili **emas**; yuzani mahsulot bo'yicha
   o'lchash kerak.
2. Bir sinf bir joyda tugamaydi: yetti chaqiruv nuqtasidan keyin yana **besh**
   joyda chiqdi. Sinfni **shakli** bo'yicha qidir.
3. Retrieval natijasini kesish — eng qimmat kesish: qaytarilgan matn **dalil**.
4. Probe yorilib emas, hisobot berishi kerak.
5. Standart shaklni saqlash — tuzatishning yarmi (`opt-in`).

## Ultra-audit IX faza — bayroq hosilasi va chegaraviy test joylashuvi

**Skanerlash o'lchovi:** butun runtime bo'ylab `truncated`/`cut`/`more`
bayroqlari AST bilan yig'ilib, **hosilasi** bo'yicha tasniflandi:
populyatsiya bilan (`len(to'liq) > limit`) → to'g'ri; faqat chegara bilan
(`len(natija) >= limit`) → nuqson.

**Natija: 30/30 to'g'ri.** Audit boshlangan nuqson sinfi **butun kod bo'ylab
yopilgan**. To'rttasi "shubhali" ko'rindi (nomlar evristikaga tushmadi) —
o'lchab tekshirildi, to'rttasi ham to'g'ri (`ordered`, `values`, `absent` —
to'liq to'plam; qaytariladigani esa kesim).

**Haqiqiy topilma — test bir tomonni o'lchardi.** Bayroq to'g'ri, lekin uni
o'lchaydigan test ko'p joyda faqat **kesilgan tomonni** tekshirardi. Kesilgan
tomon ikki predikatda ham **bir xil** javob beradi — hech narsani isbotlamaydi.
O'lchandi: `telephony` (2 kesilgan, 0 aynan mos) va `vision` (1, 0) — faqat
kesilgan tomon; `assets.children` da umuman test yo'q.

**Tuzatish:** `test_assets` 57 → **59**, `test_telephony` 138 → **139**,
`test_vision` 48 → **49**. Qo'riqchi tekshirildi: `> limit` → `>= limit`
qaytarilganda **4 failure**.

**O'z xatom — chegarada turmagan test.** Birinchi urinishda testlar `limit=99`
va `limit=MAX_EVENTS` bilan yozildi — ular **teskari kodda ham yashil** o'tdi.
Sabab: `5 >= 99` — False, ya'ni chegara populyatsiyadan uzoq, ikki predikat bir
xil. Test chegaraviy **ko'rinardi, lekin emas edi**. Chegara aynan populyatsiyaga
qo'yildi (`limit=3`, `limit=5`) — shundan keyin to'rttasi ham yiqildi.

**Signatura aynan:** `failures=1, errors=149, skipped=1`, to'plam 2370 → **2376**.
Probe 143/143.

**VI saboq:**
1. Chegaraviy test chegarada turishi shart; "yashil o'tdi" ≠ "o'lchadi".
2. Testni **teskari kodda** sinab ko'r — ishlamaydigan kodda ham yashil
   o'tadigan test — qo'riqchi emas, bezak.
3. Bayroq to'g'ri bo'lishi yetarli emas; uni o'lchaydigan test ham to'g'ri
   bo'lishi kerak.
4. Nom evristikasi yolg'onchi — "shubhali" to'rttaning hammasi to'g'ri edi.
5. Bir sinf yopilganini e'lon qilish uchun uni **butun kod bo'ylab sanash**
   kerak: 30/30 — sanalgan natija, "ko'rinadi to'g'ri" emas.

---

## X faza — istisno turlari va mapper chetlab o'tilishi (2026-09-20)

**Yangi o'lchov o'qi:** qaysi **istisno turi** bilan rad etamiz. API
qatlamining `call()` mapperi oltita turni kodga aylantiradi (`RateLimited`→429,
`Forbidden`/`AuthenticationError`→403, `NotFound`→404, `Conflict`→409,
`ValueError`/`LookupError`→422). **`TypeError` ro'yxatda yo'q.** Ya'ni
o'qish to'g'riligi uchun yozilgan qo'riqchi `TypeError` ko'tarsa,
chaqiruvchining oddiy xatosi **500** bo'lib chiqadi.

**Uch o'lchov:**
1. Butun runtime bo'ylab **867** ta `raise`, **26 xil tur** — `ValueError`
   **477** (hukmron, ya'ni bu **konventsiya**), `Forbidden` 148, `Conflict` 54.
2. 53 marshrutdan **15** tasi `call(...)` ni ishlatmaydi va mahalliy `try` ham
   yo'q.
3. Har bir chetlab o'tuvchi marshrutning runtime nishoni **haydaldi**:
   to'rtta ledger va `list_tasks` — **qisqartiradi**; `sync_ledger` — har qanday
   policy id ni qabul qiladi. Demak **bugun haqiqiy 500 yo'q**.

**Muhim ajratish:** "mapper chetlab o'tilgan" — **zaiflik emas, imkoniyat**.
Zaiflik bo'lishi uchun ko'tariladigan istisno ham kerak. Ikkalasini alohida
o'lchash shart.

**Yagona chetga chiqish:** `engine.list_tasks('t','x')` →
`TypeError: '>' not supported between instances of 'str' and 'int'`
(`min(200,max(1,limit))` ichida). AST skani: `list_tasks` **faqat** tenant
bilan chaqiriladi (`/tasks` `limit` parametrini umuman olmaydi) → bugun
**yetib borilmaydi**. Tuzoq, tirik nuqson emas; **baribir tuzatildi**.

**Tuzatish:** `list_tasks` endi runtime konventsiyasiga bo'ysunadi —
butun son bo'lmagan limit → `ValueError`; oraliqdan tashqari butun son →
**rad etilmaydi, qisqartiriladi**. Ikkala uchi **ikki xil qaror**: yuqorisi
**shift** (500 → 200), pastkisi **pol** (0, -5 → 1).

**Kutilmagan topilma:** `bool` — `int` ning vorisi, shuning uchun
`min(200,max(1,True))` **xato bermaydi** — jimgina 1 qatorli sahifa qaytaradi.
Qo'riqchi `isinstance(limit,bool)` ni **nomma-nom** rad etadi.

**Tuzatish o'lchandi:** `test_engine` 55 → **57**; probe 143 → **159**
(13-bo'lim). Qo'riqchi `if False:` ga aylantirilganda → test **1 error**,
probe **5 FAIL** (148 o'tadi) — probe **yiqilmaydi, hisobot beradi**.
Tiklangach: test yashil, probe **159/159**.

**VII saboq:**
1. Mapper chetlab o'tilgani — zaiflik emas, **imkoniyat**; ko'tariladigan
   istisno ham kerak.
2. "Yetib borilmaydi" — tuzatmaslik uchun sabab emas, agar **shakl** noto'qri
   bo'lsa: u qo'shni qator qo'shilganda tirik bo'ladi.
3. Konventsiya — **o'lchov birligi**: 477 `ValueError` bitta `TypeError` ni
   chetga chiqish deb ko'rsatadi.
4. `bool` — `int` ning vorisi; turni tekshirganda bool ni **nomma-nom**
   ayirish kerak, aks holda jimgina noto'g'ri natija olasan.
5. Chegaraning ikki uchi — ikki xil qaror; ikkalasini **alohida** sinash kerak.

---

## XI faza — test tomonidagi ko'zgu bo'shlig'i (2026-09-20)

**Boshlanish nuqtasi:** IX fazadagi qamrov jadvalida bitta qator
**"faqat aynan mos"** deb qolgan edi — `business_graph` (kesilgan tomon **0**).
IX fazada `telephony`/`vision` (teskari holat) tuzatilgan, bu qator
qoldirilgan edi. Teskari holat ham xuddi shunday ko'r.

**O'lchov:** `business_graph` chegaraviy testlari haydaldi va qaysi tomonda
turishi yozildi:
- `test_search_truncation_...` — fixture populyatsiyasi `stock==0` → **1**;
  limitlar 1/2/3 → hammasi **aynan mos yoki ortiqcha**. Kesilgan tomon yo'q.
- `test_conflicts_truncation_...` — populyatsiya **1**, limit 1 → aynan mos.
- `test_timeline_truncation_...` — populyatsiya **2**, limitlar 1/2/3 → **ikki
  tomon** (yagona to'g'ri yozilgani).

Test **nomi** "only when a match was left out" deb va'da beradi, lekin kod
hech qachon matchni tashlab ketmaydi. `False` da qotib qolgan predikat ham
o'tadi.

**Ikki tuzoq:**
1. Sheets transportidan qurilgan fixture **populyatsiyani o'zgartirmaydi** —
   `search` identifikatorlarni **ulangan ERP jadvalidan** oladi. Faqat
   haqiqiy jadvalni seed qilish ta'sir qiladi.
2. Birinchi `search` testi `population=2` deb **qabul qildi**; aslida **3**,
   chunki `setUp` ning o'zi bitta `stock==0` mahsulot beradi. Kod to'g'ri edi,
   kutilmam noto'g'ri — IX fazadagi xatoning **aynan takrori**.

**Tuzatish:** ikki yangi test — `test_search_reports_a_cut_when_a_match_is_
actually_left_out` (populyatsiya `limit=100` bilan **o'lchanadi**) va
`test_conflicts_reports_a_cut_when_a_conflict_is_actually_left_out` (3
ziddiyatli mahsulot, 4 limit).

**Revert xatom:** uchta predikatdan (`_collect_identifiers`, `search`,
`conflicts`) birinchi urinishda faqat **ikkitasi** qaytarildi — `search`
(8 bo'shliqli indent) o'tkazib yuborildi. Shu sababli "search testi
ajratmaydi" degan **yolg'on** xulosaga keldim. `grep` bilan sanash shart.

**Natija:** `test_business_graph` 172 → **174**; probe **159** (o'zgarmadi);
to'plam 2380 → **2384** (2380 + 2 engine + 2 business_graph). Revert: uchala predikat `>=` → `>` → **6 failure**.

**VIII saboq:**
1. Ko'zgu bo'shlig'i ham bo'shliq — faqat bir tomonni o'lchash ko'rlik.
2. **Probe o'lchashi suite o'lchashini bildirmaydi.** Bu fazada **kodda nuqson
   yo'q edi**; yetishmagan narsa repo suite'idagi **dalil** edi.
3. Populyatsiyani **o'lcha, qabul qilma** — fixture arifmetikasi eng ko'p
   takrorlanadigan xatoyim.
4. Transport fixture'i populyatsiyani har doim ham o'zgartirmaydi; manba
   **ulangan jadval** bo'lsa, jadvalni seed qilish kerak.
5. Revertni `grep` bilan tasdiqla — yarim revert yolg'on xulosa beradi.

---

## XII faza — vaqt va soat izchilligi (2026-09-20)

**Yangi o'lchov o'qi: vaqt.** `Engine(..., clock=...)` — in'ektsiya qilinadigan
soat, va u bezak emas: deadline, lease, approval muddati, escalation oynasi
hammasi shu soatni o'qiydi.

**O'lchov:** butun runtime bo'ylab xom devor soati qidirildi. Faqat **bitta**
qolgan edi — `whatsapp._now()` default'i. Lekin savol: production yo'llari
uni ishlatadimi?

**Real nuqson:** ikki production chaqiruv nuqtasi bor edi:
- `whatsapp_window_reports` → `window_state(last, now)` ✅ engine soati
- **`whatsapp_text` (send gate)** → **`window_state(last)`** ❌ **devor soati**

Ya'ni **yuborish darvozasi** — 131047 ning oldini olish uchun qurilgan joy —
"mijoz 24 soatlik oynada-mi" savoliga platforma nazorat qilmaydigan soat bilan
javob berardi.

**Decisive o'lchov:** faqat engine soatini 10 oyna suramiz, devor soatiga
tegmaymiz → kod **baribir yubordi**. Tuzatishdan keyin → **refused**.

**Tuzatish:** `_engine_now(engine)` — engine soatini o'qiydi, enginesiz stub
uchun devor soatiga qaytadi. Ikki chaqiruv nuqtasi uzatadi. `window_state`
ning o'zi **o'zgarmadi** — uning `now=None` default'i to'g'ri edi; nuqson
**chaqiruv nuqtasida** edi.

**Eng qimmatli topilma — testlar nuqsonni QADAB QO'YGAN edi.** Tuzatishdan
keyin 2 test yiqildi. O'lchandi: `RECENT` (import vaqtida) va `time.time()`
hozir orasidagi siljish **aynan 60.0 s**; testlar hodisani
`time.time() - (WINDOW + 60)` bilan joylashtirardi — margin ham **aynan 60 s**.
Ular faqat darvoza engine soatini e'tiborsiz qoldirgani uchun o'tardi.
Fixture'lar `self.clock.now` ga o'tkazildi.

**Probe'ning o'z xatosi:** birinchi versiya `_engine_now` ni to'g'ridan-to'g'ri
chaqirdi va **teskari kodda ham yashil** o'tdi — u **yordamchini** o'lchadi,
**chaqiruv nuqtasini** emas. Endi `send` ning manba matnini tekshiradi →
teskari kodda **2 FAIL**.

**Natija:** `test_whatsapp` 119 → **120**; probe 159 → **173**; to'plam
2384 → **2385**. Sinf bo'ylab: to'rtta modul (`escalation`, `usage_budget`,
`workforce`, `whatsapp`) engine soatini o'qiydi, e'lon qilinmagan xom soat
qolmadi.

**IX saboq:**
1. In'ektsiya qilinadigan soat — **shartnoma**: har bir vaqtga bog'liq javob
   uni o'qishi shart, aks holda javob qayta olinmaydi.
2. **Test nuqsonni qadab qo'yishi mumkin.** Tuzatish testni sindirsa — test
   **xatoni himoya qilgan**.
3. Sof funksiyaning default'i qonuniy; **chaqiruv nuqtasi** javobgar.
4. Yordamchini o'lchash ≠ chaqiruv nuqtasini o'lchash; o'lchov **nosozlik
   yo'lini** bosishi shart.
5. Ikki soat farqi test marginiga teng bo'lsa — yashirin bog'liqlik.

---

## XIII faza — muddat chegarasi va testning turgan joyi (2026-09-20)

**O'lchov o'qi:** IX/XI saboqlarini **boshqa qatlamga** qo'llash — `app/`
(identity, auth, session). `identity_store` da **15** xom `time.time()` —
`app/` dagi eng zich joy; bu yerda soat xatosi **xavfsizlik** masalasi.

**Muddat o'quvchilari (4 ta):** `_invitation`, `validate_session`,
`rotate_session` — `expires <= now` bilan **rad etadi**; `list_sessions` —
`expires > now` bilan **qabul qiladi**. Ikkalasi **aynan to'ldiruvchi**;
tenglikda sessiya o'lgan (xavfsizlik uchun to'g'ri yo'nalish). **Kod to'g'ri.**

**Haqiqiy topilma — test chegaradan 57 yil narida.** Mavjud
`test_invitation_expired_rejected` `expires=1` (epoch 1 sekund) qo'yadi.
O'lchandi: `<=` ham, `<` ham rad etadi → test **qaysi predikat kuchda
ekanini ajratmaydi**. IX fazadagi `limit=99` xatosining **aynan takrori**,
boshqa qatlamda. Ajratuvchi yagona holat — **tenglik**.

**Tuzatish:** muzlatilgan soat bilan 4 chegara testi, har biri **ikki
tomonni** tekshiradi (tenglikda rad + bir sekund oldin qabul):
invitation, `validate_session`, `rotate_session`, `list_sessions`.

**Revert:** to'rt predikat (`<=`→`<`, `>`→`>=`) → **4 failure**; eski
`expires=1` testi **yashil qoldi** (hech narsa o'lchamaydi).

**O'z xatolarim:**
1. `s.authenticate(raw)` — u **login** funksiyasi (parol talab qiladi);
   to'g'risi `validate_session`.
2. Bitta tokenni ikki marta ishlatdim — `rotate_session` muddat o'tganda
   **butun oilani bekor qiladi**; ikki alohida sessiya kerak.
3. Probe jadvalim xato: `expires = now - 1` da ham `<=`, ham `<` rad etadi —
   farq qilmaydi; ajratuvchi **faqat tenglik**.
4. Probe satr mosligi mo'rt — bitta literalga bog'lanib **"qo'riqchi yo'q"**
   deb yolg'on xabar berdi; endi **sanaydi**.

**Natija:** `test_identity_hardening` 27 → **31**; probe 173 → **181**;
to'plam 2385 → **2389**.

**X saboq:**
1. Chegara qoidasi **qatlamga bog'liq emas** — runtime saboqi `app/` da ham
   aynan takrorlandi.
2. "Rad etildi" ≠ "chegara to'g'ri": uzoq o'tmishdagi fixture rad etishni
   isbotlaydi, chegara **joyini** emas.
3. Xavfsizlik chegarasida tenglik — **o'lgan** (ataylab).
4. Bir faktni ikki o'quvchi o'qisa — **ikkalasi** o'lchanishi kerak.
5. Probe o'z xatosini ham ko'rsatadi: "FAIL" kodda emas, mening jadvalimda
   bo'lishi mumkin.

## XIV faza — tasdiq muddati (approval deadline): bitta chegara, ikki o'quvchi

Runtime qatlamidagi muddat chegaralari supurildi (`platform_runtime/*.py` bo'ylab
`expires`/`deadline`/`lease`/`expiry`). Eng keskin joy — `engine.py` dagi tasdiq
muddati.

**Topilma.** Tasdiq `now+86400` bilan yaratiladi, lekin shu bitta muddatni **ikki
mustaqil yo'l** o'qiydi: `approve` (431-qator) qarorni **qabul qilishni**,
`claim`/tick (474-qator) **bajarishni** so'raydi. Ikkalasi ikki xil metodda, ikki
xil literal — ularni bog'laydigan konstantа ham, tip ham yo'q. Bittasi bir sekundga
surilsa: qaror qabul qilinadi-yu **hech qachon bajarilmaydi** (tasdiq `consumed`,
qadam abadiy kutadi), yoki aksi.

**Nega mavjud sinov ushlamaydi.** `test_approval_expires` soatni `+86401` —
chegaradan **bir sekund nariga** suradi; u yerda `<=` ham, `<` ham rad etadi.
Ajratuvchi **faqat tenglik**, ya'ni `now == expires`.

**Nima qilindi.**
- `test_engine.py` 57 → **60**: uchta sinov, ikkitasi aynan chegarada
  (`deadline-1` → `succeeded`; `now==deadline` → `Conflict` va
  `approval_expired_or_invalid`).
- Yana bir sayt: `expires` ni ikki saytda ham ag'darib o'lchandi (qaytarish
  matritsasi): faqat `approve` ag'darilsa → 1 yiqilish; faqat `tick` ag'darilsa →
  2 yiqilish; ikkalasi → 2; hech biri → 60/60 OK. Tiklandi, to'g'ri holatda yashil.
- Probe: 16-bo'lim qo'shildi (181 → **193** xossa). Ag'darilganda 3 FAIL,
  tiklanganda 0 — probe mustaqil ajratadi.

**Muhim yon topilma.** `test_..._cannot_be_decided` da `assertRaises(Conflict)`
**kuchsiz da'vo**: `approve` predikati uch haddan iborat (`status`, `step_status`,
`expires`), u qaysi had otilganini aytmaydi. Sinov endi qatorni **qayta o'qib**,
`('pending','','queued')` — ya'ni "tegilmagan" holatni da'vo qiladi.

**Xato xulosam ham o'lchov natijasi edi.** Dastlab "bu sinov ajratmaydi" deb
o'yladim — chunki ikki saytni **birga** ag'dargan edim. Saytlarni **yolg'iz**
ag'darish haqiqiy javobni berdi. Saboq: o'lchovni ajratmasang, xulosa ham
aralashadi.

**Saboqlar.**
1. Bitta faktni ikki o'quvchi o'qisa — **ikkalasi** ham o'lchanishi shart; ularning
   mosligini kod kafolatlamaydi.
2. **`assertRaises` — kuchsiz da'vo.** Ko'p hadli predikatda qatorni qayta o'qib,
   qaysi had otilganini **nomlash** kerak.
3. Qaytarishni **sayt-sayt** qil, birga emas — aks holda xulosa aralashadi.
4. **Sinov o'z saytini qaytarish matritsasida ko'rsatishi shart**, aks holda u
   "biror narsa"ni o'lchaydi.

## XV faza — lease chegarasi: ikki shakl **to'ldiruvchi** bo'lishi shart

XIV fazaning egizagi, lekin xatosi **og'irroq**. Egalik tokenining (`claim`)
lease muddatini **ikki xil metod** so'raydi:

- `finish` (497) va dispatch fence (273) — **strict**: `lease>?`
- `claim` sweep (442) va event dequeue (609) — **inclusive**: `lease<=?`

**Xavfsizlik xossasi.** Bu ikki shakl bir-birining **aniq to'ldiruvchisi**:
har lahzada, tenglik ham kiradi, **aynan bittasi** ishlaydi. Kod buni
kafolatlamaydi — metodlar ham, literallar ham boshqa-boshqa.

**Ikki xil buzilish, ikkalasi ham jimgina:**
1. sweep **strict** bo'lsa (`lease < now`) → tenglikda **hech biri** ishlamaydi
   (**bo'shliq**) → qadam `running` da **o'lik claim** ostida qoladi.
2. finish **inclusive** bo'lsa (`lease >= now`) → tenglikda **ikkalasi**
   ishlaydi (**ustma-ust**) → sweep allaqachon `uncertain` qilgan qadamni worker
   "tugatadi".

**Bo'shliqni o'lchadim** (farz qilmadim): sweep ni strict qildim → qator
`{'status':'running','claim':'7385e4bf...'}` bo'lib qoldi, ikkinchi worker
ololmadi, qadam **abadiy tiqildi**.

**Nega mavjud sinov ushlamagan.** `test_stale_claim_cannot_finish` `+91`
suradi (lease 90) — chegaradan **bir sekund nari**, u yerda ikkala shakl ham
rozilik bildiradi. Chegara 90 da, 91 da yoki o'rtada bo'lsa ham sinov o'tadi.

**Nima qilindi.** `test_engine.py` 60 → **63**:
- `test_lease_is_live_one_second_inside_the_bound` — `now=lease-1`, lease tirik,
  finish qabul qiladi (aks holda "hammani rad etadi" bilan o'tib ketardi).
- `test_lease_bound_is_exclusive_on_both_sides` — `now=lease`, **ikki yo'nalish**:
  finish rad etadi **VA** sweep `error='lease_expired'` bilan o'ldiradi.
- `test_lease_overrun_is_still_refused_further_out` — `now=lease+3600`,
  "bir sekundlik hiyla" emasligini isbotlaydi.

**Qaytarish matritsasi:** sweep-strict → 1 yiqilish; finish-inclusive → 1;
ikkalasi → 1; hech biri → 63/63 OK. Probe 17-bo'lim: 193 → **213** xossa;
ag'darilsa 2 FAIL, tiklanganda 0.

**Saboqlar.**
1. **To'ldiruvchilik — o'lchanadigan xossa**, farz qilinadigan narsa emas.
2. **Ikki xil buzilish bor** — bo'shliq ham, ustma-ust ham. Bittasini tekshirish
   yarim tekshirish.
3. **Oqibatni o'lcha, assertion yiqilishini emas.** `status='running'` + o'lik
   `claim` — dalil; "sinov yiqildi" — dalil emas.
4. **Probe ning o'zi ham xato qiladi.** Bu fazada 5 marta yiqildi, hammasi
   **mening** xatolarim (noto'g'ri truth qiymati, takrorlangan yorliq, satr
   mosligi). Har biri o'lchov natijasi edi va tuzatildi.

## XVI faza — event lease: **bitta** o'quvchi va jimgina tayanch xossa

`p_events` lease i XV fazadagi qadam lease idan **tuzilish jihatdan boshqa**:

| | Qadam | Event |
|---|---|---|
| Muddat | `now+lease_seconds` | `now+120` (qattiq) |
| Sweep | `status='running' AND lease<=?` | `status='processing' AND lease<=?` |
| Tugatish | `claim=? AND status='running' AND lease>?` | `claim=?` — **lease yo'q** |

**Poyga tekshirildi.** Dequeue (609) + claim (612) — ikki alohida bayonot,
lekin `tx()` **`BEGIN IMMEDIATE`** ochadi (yozuv qulfini boshidan oladi),
shuning uchun parallel yozuvchi orasiga kira olmaydi. **Poyga yo'q.**

**Tayanch xossa (adversarial o'lchandi).** Event tugatilishi faqat token bilan
to'siladi, `lease>?` yo'q. Bu **xavfsiz**, chunki dequeue **har claimda yangi
token yasaydi**: worker A `TOKEN_A` bilan ushlab turadi → lease o'tadi → B qayta
claim qiladi va tugatadi → A ning kechikkan `UPDATE ... AND claim='TOKEN_A'` i
**hech bir qatorga mos kelmaydi**, B ning natijasi ustiga yozilmaydi.

Bu xossa **hech qayerda da'vo qilinmagan edi**. Endi qilinadi — chunki kelajakda
token qayta ishlatilsa yoki tugatish qo'riqchisi tokendan boshqa joyga ko'chsa,
event yo'li qadam yo'lida bor **ikki qavatli to'siqni** jimgina yo'qotadi.

**Nega mavjud sinov ushlamagan.** `test_event_crash_recovery_after_submission`
`lease=0` qiladi (~57 yil o'tmish) — inclusive ham, exclusive ham rozilik
bildiradi. Chegara `lease-1`/`lease`/`lease+1` da o'lchandi: ajratuvchi yana
**faqat tenglik**.

**Nima qilindi.** `test_engine.py` 63 → **65**:
- `test_event_lease_boundary_is_the_only_recovery_instant` — `now=lease-1`:
  event tegilmasligi va `TOKEN_A` saqlanishi shart; `now=lease`: tiklanadigan.
- `test_event_reclaim_mints_a_new_token` — yangi claim **yangi** token yasashi,
  superseded token hech bir qatorga mos kelmasligi.

**Qaytarish:** dequeue strict → 1 yiqilish; token qayta ishlatildi (hazard
kiritildi) → 1 yiqilish; hech biri → 65/65 OK. Probe 18-bo'lim: 213 → **227**;
ag'darilsa 2 FAIL, tiklanganda 0.

**Saboqlar.**
1. **Hamma chegara egizak emas.** Ikki o'quvchi bor joyda **to'ldiruvchilik**
   tekshiriladi; bitta o'quvchi bor joyda — **yo'q to'siqning o'rnini nima
   bosadi**.
2. **Yo'q to'siq ham dalil talab qiladi.** Uni oqlaydigan xossa o'lchanishi shart.
3. **Poyga bo'lmaganini ham isbotla.** `BEGIN IMMEDIATE` — o'qib bilib bo'ladi,
   endi yozib ham qo'yildi.
4. **Eng qimmatli sinov — eng ko'rinmas xossani qo'riqlaydigani.**

## XVII faza — inbox oynalari: **uch** chegara, **uch** xil shakl

`accept_event` uchta limit qo'yadi, shakllari boshqa-boshqa:

| Limit | Shakl | Chegara | Samara |
|---|---|---|---|
| Kunlik kvota | `(tenant,day)` hisoblagich | `count>=10000` | aniq **10000**/kun |
| Backpressure | jonli o'lchagich | `pending>=1000` | ochiq **1000** |
| Kun oynasi | `int(clock()//86400)` | — | yangi kun → yangi hisoblagich |

**Kvota off-by-one TO'G'RI.** Hisoblagich **oshirishdan oldin** o'qiladi:
`count==9999` → qabul, `10000` bo'lib yoziladi; keyingisi `10000` ni ko'rib rad
etadi. Samarali chegara — **aniq 10000**. O'lchandi: 9998→qabul(9999),
9999→**qabul(10000)**, 10000→RateLimited.

**Mavjud sinov bu safar haqiqatan ajratdi** — `count=10000` ni to'g'ridan yozadi,
ya'ni fixture **aynan chegarada**. Lease/tasdiq hollaridan farqli. Lekin faqat
**tashqarisini** qadaydi.

**Nima qadalgan edi:** tashqarisi (`10000`→rad) ✅.
**Nima qadamagan edi:** ichkari (`9999`→qabul va aniq `10000`) ❌;
backpressure (`pending>=1000`) ❌ **umuman**; kun oynasi ❌.

**Tartib ham xossa.** Guard (599) **INSERT dan oldin** ko'tariladi → rad etilgan
accept kvotani **sarflamaydi**. O'lchandi: conflicting replay → count o'zgarmadi.
Aks holda chaqiruvchi bitta yaroqsiz kalitni qayta urib kvotani tugatardi.

**Nima qilindi.** `test_engine.py` 65 → **68**: uch sinov (ichi, backpressure
ikki tomoni, kun oynasi reset lahzasi).

**Qaytarish:** kvota `>=`→`>` → 3 yiqilish; backpressure `>=`→`>` → 1; kun
indeksi `+1` → 1; hech biri → 68/68 OK. Probe 19-bo'lim: 227 → **242**.

**Saboqlar.**
1. **Bu fazada XATO TOPILMADI** — topilgan narsa: **ikki chegara umuman
   o'lchanmagan**, bittasining ichi ochiq. "Kod to'g'ri ko'rinadi" — dalil emas.
2. **`>=`/`>` farqi har doim "chegarada sinov" demak emas** — bu safar mavjud
   sinov ajratdi, chunki fixture aynan chegarada edi. Qoida o'zgarmadi:
   **ajratuvchi — tenglik**.
3. **Tartib ham o'lchanadigan xossa** — `raise` `INSERT` dan oldin turishi
   "rad etilgan accept bepul" xulqini beradi; satrlarni almashtirish uni buzadi.
4. **Uch xil shakl — uch xil sinov.** Hisoblagich ≠ jonli o'lchagich ≠ oyna.

## Fazza yigirmanchi — qurilma kaskadi va `generation` panjarasi (ultra-audit)

`engine.device(...)` (engine.py 563-581) va `platform_api.py:231` tekshirildi.
Uch sirt, uchtasi ham **to'g'ri**, lekin **uchtasi ham o'lchanmagan** edi.

**O'lchangan xossalar:**
1. **Bosqich kaskadi tor:** `queued`/`waiting_approval` → `cancelled`,
   `running` → `uncertain`; `succeeded`/`failed`/`cancelled`/`uncertain`
   **tegilmaydi**. `running → uncertain` — halollik: ish qurilmada bajarilgan
   bo'lishi mumkin, `cancelled` deb belgilash yolg'on bo'lardi.
2. **Tasdiq kaskadi — boshqa to'plam:** faqat `pending`, `approved`;
   `consumed` **tegilmaydi** (uni qayta rad etish tarixni buzardi).
3. **`generation` — panjara, hisoblagich emas:** `platform_api.py:231`
   **tenglik** bilan tekshiradi (`!=`), ya'ni `revoked=0` bo'lsa ham bump
   lahzasidan eski token mos kelmaydi.

**Sinovlar:** test_engine 68 → **71**
- `test_device_cascade_fences_exactly_the_in_flight_states` (butun 7-holat matritsasi)
- `test_device_cascade_rejects_only_undecided_approvals` (`consumed` saqlanadi)
- `test_device_generation_is_a_fencing_token_not_a_counter`

**Revert matritsasi (har biri yakka):** cascade_wide → 1, cascade_narrow → 2,
approval_wide → 1, gen_no_bump → 2, **hech narsa → 71/71 OK**.

**Probe:** 20-bo'lim `section_device_cascade` qo'shildi; 242 → **259 xossa**,
revert'da 2 FAIL, tiklanishda 0. Literal status to'plami engine.py da
**4 marta** uchraydi (532/569/573/575) — probe to'rttasini ham qadaydi.

**Hujjat:** `ULTRA-AUDIT-ASCII-CELL-UZ.md` §125 (124 → **125 bo'lim**).
**BACKLOG:** `offline_tests` → 2403, `open_risks` 22 → **23**, `completion_claim`
tail "…inbox-window and device-cascade verdicts". **README:** 2403 test.

**Saboqlar:**
1. **Ikki kaskad — ikki to'plam.** "Bir xil ko'rinadi" ≠ "bir xil". Har bir
   to'plam o'z sinovini talab qiladi.
2. **`uncertain` — halollik holati**, qulaylik emas.
3. **Panjara tenglik bilan o'lchanadi** — "kattami" emas, "mos keladimi".
4. **Literal to'plamning nechta nusxasi borligini sanang** — bittasini
   o'zgartirish fan-out'ni jimgina buzadi.

---

## context-audit skilini o'rnatish (20:37)

**Repo:** https://github.com/Azamat1st/context-audit.git → o'rnatildi
`~/.workbuddy-ai/skills/context-audit/` (61K, 5 fayl, `.git` olib tashlandi).

**Nima qiladi:** agent kontekst/token sarfini audit qiladi — `codeburn`
o'lchovi (doctor/optimize, read-only) + lokal inventar (skilLLAR, CLAUDE.md,
MCP konfig) → A–D gradatsiya bo'yicha reja. SKILL.md rus tilida,
`scripts/audit.py` (540 qator) Python 3.11+.

**Xavfsizlik auditi (qo'lda, `skills-security-check` skili mavjud emas):**
- `eval`/`exec`/`os.system`/`shell=True`/base64/pickle/urllib/socket — **yo'q**.
- `subprocess.run` faqat 2 joyda, ikkalasi ham **ro'yxat** (shell yo'q):
  `node -v` va `codeburn <args>`; `FORBIDDEN_SUBCOMMANDS` bilan bloklangan
  (`optimize --apply`, `act`, `guard`, `sync`, `share`, `devices`, `menubar`).
- Bidi/zero-width unicode hiylalari — yo'q.
- Amalda tekshirildi: `--codeburn off` bilan yugurtirildi, **hech qanday fayl
  yozmadi**, `reports/` ham yaratmadi. `read_only: true`.

**Verdikt:** P2 (xavfsiz) → o'rnatildi.

**Eslatma:** Codex uchun yozilgan (`~/.codex/skills/`), WorkBuddy'ga
user-level qilib qo'yildi. Chaquv: `$context-audit` yoki `scripts/audit.py`.

**Topilma:** foydalanuvchi repo'si `codeburn`dan keyin yozilgan — SKILL.md
`codeburn` buyruqlariga tayanadi (`doctor --json`, `optimize --format json`).
`codeburn` hali o'rnatilmagan (Node v22.22.2 bor, 22.13+ talab bajarilgan).

## Fazza yigirma birinchi — throttle oynasi va rad chegarasi (ultra-audit)

`app/identity_store.py:80-88` — `throttle()`, parol tekshiruvidan **oldin**
ishlaydigan doimiy qat'iy oyna darvozasi. **Uchta** mustaqil chegara,
**bittasi** qadalgan edi.

**Chegaralar:**
1. **Rad:** `count > limit` — `count` **oshib bo'lgandan keyin** o'qiladi,
   shuning uchun `limit`-chi chaqiruv **o'tadi**. Mavjud sinov buni qadagan
   (fixture aynan chegarada — nodir holat), lekin **ichini** ochiq
   tasdiqlamagan.
2. **Oyna indeksi:** `int(time.time() // window_seconds)` — **umuman
   o'lchanmagan**, chunki mavjud sinov `reset()` chaqiradi (u faqat ulanishni
   qayta ochadi) va **soatni surmaydi**.
3. **Tozalash:** `DELETE WHERE window < window-2` — **qat'iy `<`**, uchta oyna
   qoladi. Ham o'lchanmagan.

**O'lchov:** soat `patch.object(s.time,'time',...)` bilan in'ektsiya qilindi —
oyna arifmetikasi **birinchi marta** kuzatildi. Off-by-one **to'g'ri**: 1..3
o'tadi, 4 rad; rad etilgan chaqiruv ham yoziladi (qaytarilmaydi).

**Mening ikki xato taxminim** (kod emas, fixture arifmetikasi):
- `base-1` → oyna `9999`, `10000` emas (`floor`);
- tozalash **uchta** oyna qoldiradi, **ikki** emas (`< window-2`, `<=` emas).

Ikkovini ham **o'lchov** tuzatdi, mulohaza emas.

**Sinovlar:** test_identity_hardening 31 → **35**
- `..._bound_is_exclusive_so_the_limit_call_itself_succeeds`
- `test_window_index_advances_exactly_on_the_tick`
- `test_cleanup_retains_the_previous_window_and_drops_older_ones`
- `test_throttle_buckets_do_not_share_a_counter`

**Revert:** `>`→`>=` → 1 fail + 4 error; oyna `+1` → 2; tozalash `<`→`<=` → 1;
bucket konstanta → 1 error; **hech narsa → 35/35 OK**.

**Probe:** 21-bo'lim `section_identity_throttle`; 259 → **272 xossa**.
Revert'da: rad 2 FAIL, oyna 1, tozalash 2; tiklanishda 0.

**Yakuniy to'plam:** `Ran 2407 tests` — `failures=1, errors=149, skipped=1`
(baseline imzo saqlandi). **Hujjat:** §126 (125 → **126 bo'lim**).
**BACKLOG:** `offline_tests` → 2407, `open_risks` 23 → **24**. **README:** 2407.
`production_release` = **NO_GO** (o'zgarmadi).

**Saboqlar:**
1. **"Chegara sinovi bor" ≠ "hamma chegara sinovi bor".** Uchta bor edi,
   bittasi qadalgan.
2. **Soatni surmagan sinov vaqt oynasini o'lchamaydi.** `reset()` — soat emas.
3. **Qoldiqni sanash kerak** — `<` va `<+1` bir xil ko'rinadi.
4. **O'z fixture arifmetikang eng ko'p xato qiladigan joy.**

---

## codeburn o'rnatildi + /context-audit birinchi audit (21:00)

**codeburn 0.9.24** global o'rnatildi (`npm install -g codeburn`, Node 22.13+ talab).

**Audit natijasi:** salomatlik **F (26) → D (41)**, haftalik sarf $1376, potensial 14.3%.

**Qo'llangan A gradatsiya (3 ta, hammasi `codeburn act undo <ID>` bilan qaytariladi):**
1. `c523163b` — 4 ishlatilmagan Hostinger MCP olib tashlandi
   (hosting, vps, reach, domains). Qoldirildi: dns, billing (~328.9M token).
2. `83afeaea` — 6 skil `.archived/` ga (brainstorming, domain-modeling, grilling,
   improve-codebase-architecture, subagent-driven-development, writing-plans).
3. `fe618f24` — `~/.bashrc` ga `BASH_MAX_OUTPUT_LENGTH=15000`.

**MUHIM TUZATISH:** codeburn `test-driven-development` va
`verification-before-completion` ni ham arxivladi — CLAUDE.md TDD qoidalariga
tegishli bo'lgani uchun **qo'lda tiklandi**. Kelajakda `unused-skills` apply
qilganda shu ikkalasini tekshirish SHART.

**Qo'lda B:** CLAUDE.md ga read-edit ratio qoidasi (o'lchov 0.7:1 → maqsad 4:1).
CLAUDE.md 879 → 1139 bayt.

**Backuplar:** `~/.claude.json.backup-20260920-205705`,
`~/.claude/skills-backup-20260920-205706`, `~/.bashrc.backup-*`,
`~/CLAUDE.md.backup-*`

**Hisobot:** `reports/context-audit.md`

**Qolgan:** mcp-low-coverage claude.ai connectorlari (lokalda yo'q, Settings >
Connectors dan), context-heavy-sessions (odat), redundant-rereads. 3 kundan keyin
`codeburn act report`.

**Saboq:** `codeburn optimize --apply --only <id> --yes` — `--only` ba'zi runlarda
"not-appliable" beradi chunki apply oldidan qayta tahlil qiladi va ro'yxat o'zgaradi.
Apply dan keyin `codeburn act list` ni har doim tekshirish kerak.

### Qo'shimcha tekshiruv (21:15)

**claude.ai ulanishlari:** `~/.claude.json` → `claudeAiMcpEverConnected` da 11 ta.
codeburn 5 tasini ishlatilmayotgan deb belgiladi (higgsfield, Figma, Gmail, OpenArt,
Google Drive). **Bular lokal faylda YO'Q** — claude.ai hisobi (server) tomonda
boshqariladi, shuning uchun `--apply` ham skript ham o'chira olmaydi. Qo'lda:
Claude Settings > Connectors.

**unused-skills nuansi (MUHIM):** codeburn tiklagan TDD skilLLARINI ham "chaqirilmagan"
deb ko'rsatadi. Tekshirdim: `~/.claude/projects` da ular yuzlab faylda uchraydi, LEKIN
bu `{"type":"skill_listing"}` attachment — har seansga yuklanadigan metadata, chaqiruv
emas. Demak codeburn texnik jihatdan haq. Narxi kichik: eng katta tavsif 333 bayt
(skill-creator), 7 tasi jami ~1.3 KB ≈ 300-400 token. "Og'ir" chegarasi 500 bayt —
hech biri yetmaydi. Qaror: TDD/verification skilLLARINI arxivlash — 400 token tejash
uchun loyiha qoidalarini qo'llab-quvvatlashdan voz kechish. Foydalanuvchi qaror qilsin.

**MCP topilmasi nega hali ko'rinadi:** o'lchov haftalik oyna sessiya tarixidan o'qiladi,
joriy konfigdan emas. 67 seans olib tashlashdan OLDIN yozilgan. Keyingi davrda
o'z-o'zidan yo'qoladi. **Natijani "topilma yo'qoldimi" bilan emas, salomatlik darajasi
va `codeburn act report` bilan o'lchash kerak.**

**Eslatma qo'yildi:** 2026-09-23 21:15 da avtomatik qayta o'lchash (automation ID
2b360c74). `codeburn act report` + yangi optimize + hisobotni yangilash.

## graphify o'rnatildi (21:12)

**Repo:** https://github.com/Graphify-Labs/graphify.git

**Muhim:** bu **skill emas, to'liq Python CLI loyihasi** (902 fayl, Apache-2.0,
muallif Safi Shamsi, v0.9.64). Kod bazasini bilim grafiga aylantiradi
(god nodes, community detection, query/path/explain).

**O'rnatish (3 qadam):**
1. `uv tool install graphifyy` — PyPI paketi **`graphifyy`** (ikki "y"!),
   CLI nomi `graphify`. Boshqa `graphify*` paketlar unga tegishli emas.
2. `graphify install --platform codebuddy` — skill yozildi `~/.codebuddy/skills/graphify/`
3. WorkBuddy papkasiga ko'chirildi: `~/.workbuddy-ai/skills/graphify/` (105K,
   SKILL.md + 8 references + `.graphify_version`)

**MUAMMO:** `graphify install` da `workbuddy` platformasi YO'Q. Platformalar:
claude, codex, opencode, kilo, aider, copilot, claw, droid, trae, gemini, cursor,
codebuddy, antigravity, kiro, pi, kimi, amp, agents, devin. `codebuddy` →
`.codebuddy/skills/`, lekin WorkBuddy `.workbuddy-ai/skills/` ni o'qiydi.
Shuning uchun codebuddy o'rnatib, keyin qo'lda ko'chirish kerak.

**Tozalash:** `~/.codebuddy/skills/graphify` va `~/.codebuddy/CODEBUDDY.md` (men
yaratgan dublikat) o'chirildi. `~/.codebuddy/` ning o'zi QOLDIRILDI — ichida
eski `logs/` va `diagnostics/` (19-sentabr) bor.

**Xavfsizlik auditi (qo'lda):** `eval`/`exec`/`os.system`/`shell=True` — yo'q
(`_eval` faqat lokal funksiya nomi). Network faqat: foydalanuvchi bergan URL
ingest (`urllib.parse`, `ingest.py`) va LLM chaqiruvlari (`llm.py`). Bidi/
zero-width hiylalar — yo'q. Apache-2.0. **Verdikt: P2 xavfsiz.**

**Funksional test:** `/tmp/gtest` da 2 fayl bilan sinov — 5 node, 7 edge, 2
community. `graphify query "..."` va `graphify explain "..."` to'g'ri call
graph qaytardi (`run() → main() → helper()`). Test papkasi o'chirildi.

**Eslatma:** CLI yo'li `~/.local/bin/graphify`, PATH da bor. `graphify-out/`
grafik natijalari loyihaga yoziladi.

**Diqqat:** codeburn skilidagi `graphify-missing` topilmasi (C gradatsiya) endi
hal bo'ldi — graphify o'rnatildi.

## Fazza yigirma ikkinchi — OAuth muddat o'quvchilari (ultra-audit)

`platform_runtime/oauth.py` — kredensialni vaqt bo'yicha **to'rtta** joyda
darvozalaydi, **ikki qarama-qarshi sezgi** bilan:

| O'quvchi | Predikat | Sezgi |
|---|---|---|
| State TTL (183) | `pending['expires'] <= now` | inklyuziv |
| Access headroom (246) | `row['expires'] > now+60` | eksklyuziv |
| Stale lease (304, 358) | `row['started']+120 > now` | eksklyuziv |
| Grant fence (270) | `row['expires'] <= now` | inklyuziv |

`120` literali faylda **ikki marta** uchraydi — bittasini o'zgartirish
ikkinchisini ham suradi.

**Mavjud qo'riqchilar hammasi uzoqdan sakraydi:** +601s (chegara 600),
`+= 3550` (headroom 60), +121s (lease 120), generation-only (fence).
To'rttasi ham chegarada hech narsa o'lchamagan.

**O'lchov (bir marta o'lchandi, to'g'ri chiqdi):**
- state: +599 o'tadi, **+600 rad** (tenglik allaqachon o'lgan)
- headroom: 61 → REUSE, **60 → refresh** (`60 > 60` yolg'on), 59 → refresh
- lease: 119 REFUSED, **120 ALLOWED**, 121 ALLOWED
- fence: +1 o'tadi, **0 REJECTS**, -1 REJECTS

**Nuqson topilmadi** — to'rttasi ham to'g'ri, lekin to'rttasi ham qadalmagan.

### Muhim: `cryptography` o'rnatildi

`cryptography` bu muhitda **yo'q edi** → `SecretVault` `VaultError` →
butun `test_oauth` moduli yiqilardi (149 error'ning **138** tasi shu, bu
`LOCAL-VERIFICATION-UZ.md` da qayd etilgan). Boshqariladigan venv'ga
(`~/.workbuddy-ai/binaries/python/envs/default`) o'rnatildi. Natija:

**`errors=149 → 11`**. Ya'ni 138 error **muhit** edi, kod emas — bu endi
**o'lchangan dalil**, taxmin emas. Qolgan 11: `os.O_NOFOLLOW` (6),
`os.mkfifo` (2), WinError 1314 symlink (2), `fcntl` (1) — hammasi POSIX-only.

**Sinovlar:** test_oauth 44 → **49** (4 yangi) — lekin 44 dan 5 tasi
`cryptography` bilan endi **haqiqatan yurdi**, shuning uchun modul 49/49 OK.

**Revert:** state `<=`→`<` → 1; headroom `+60`→`+61` → 1; lease `120`→`121` → 1;
fence `<=`→`<` → 1; **hech narsa → 49/49 OK**.

**Probe:** 22-bo'lim `section_oauth_deadlines`; 272 → **285 xossa**.
Revert'da: state 2, headroom 2, lease 2, fence 1; tiklanishda 0.

**Yakuniy to'plam:** `Ran 2411 tests` — `failures=1, errors=11, skipped=1`.
**Hujjat:** §127 (126 → **127 bo'lim**).

### Mening uchta harness xatoim
1. `begin()` `active` ulanish ustida ishlamaydi — orasida `revoke` kerak.
2. Refresh `expires` ni oldinga suradi — har raundda qayta o'qish kerak.
3. `recover_stale` generatsiya ko'taradi + envelopeni tozalaydi — yangi oqim kerak.

### Saboqlar
1. **Bir faylda ikki qarama-qarshi konvensiya bo'lishi mumkin** (`<=` va `>`
   yonma-yon). "Fayl izchil" deb faraz qilma.
2. **Bitta literal ikki joyda — ikki barobar xavf**; probe **sonini** tekshiradi.
3. **"Uzoqdan sakraydigan" qo'riqchi chegara haqida hech narsa demaydi.**
4. **Yo'q kutubxona "test yiqildi" emas — "o'lchov bo'lmadi".** 138 error
   o'rnatilgach **birinchi marta** haqiqiy chegara ko'rindi.

### Tuzatish: graphify-out va ignore masalasi (21:20)

**Men avval noto'g'ri aytdim** ("graphify-out/ ni .claudeignore ga qo'shish kerak").
graphify rasmiy hujjati (README "Ignoring files" + "Team setup") boshqacha deydi:

- `graphify-out/` — **commit qilish tavsiya etiladi** (jamoa uchun umumiy xarita).
  Faqat `graphify-out/cost.json` lokal qoladi.
- Agent/kontekst uchun to'g'ri fayl — **`.graphifyignore`** (`.claudeignore` emas!).
  Sintaksis `.gitignore` bilan bir xil, `!` negation ishlaydi.
- `.gitignore` avtomatik o'qiladi. `.graphifyignore` bor bo'lsa — ikkisi **birlashtiriladi**,
  `.graphifyignore` oxirgi baholanadi (konfliktda ustun). Faqat qo'shimcha chiqarib
  tashlaydi, hech qachon qayta qo'shmaydi.
- `--no-gitignore` — git-ignored kodni grafikga kiritish uchun.

**Bu loyihada holat:** `Agent-paltform` **git repo EMAS** (`git rev-parse` xato berdi).
Shuning uchun "commit qilish" maslahati bu yerda ma'nosiz.

**Foydalanuvchi qarori:** hozircha hech narsa qilmaslik (`.graphifyignore` ham,
grafik yaratish ham). graphify o'rnatilgan va ishlaydi, kerak bo'lganda ishlatiladi.

**Topilgan xato (kelajakda tuzatish kerak):**
`~/.workbuddy-ai/skills/context-audit/scripts/audit.py` 416-qatorda
"добавить graphify-out/ в .claudeignore" deb yozilgan — bu **noto'g'ri**
(to'g'risi `.graphifyignore`). Lekin bu uchinchi tomon skili (Azamat1st repo),
men uni o'zgartirmadim — egasiga xabar qilish kerak bo'lsa aytilsin.

### Qo'shimcha topilma: integration to'plami **birinchi marta to'liq yashil**

`cryptography` + `fastapi pydantic pyjwt pyyaml pytest httpx uvicorn` boshqariladigan
venv'ga o'rnatilgandan keyin `integration_tests` qayta yurildi:

- **Birinchi yurish:** `5 failed, 72 passed, 11 errors`
- **Qayta yurish:** **`95 passed`**

Ya'ni **15 failure + 11 error yopildi** va to'plam **95/95 yashil** —
`auth`/`oauth`/`google_data` HTTP yuzalarining birinchi haqiqiy tasdig'i.

**Hujjat yangilandi:** `LOCAL-VERIFICATION-UZ.md` — "o'lchanmagan taxmin" iboralari
**o'lchangan fakt** bilan almashtirildi (3 joyda), yurgizish buyruqlari venv bilan
ko'rsatildi, "Ochiq cheklovlar" bo'limi to'g'rilandi.

**Muhim:** tizim Python'ida deps **yo'q**; hamma narsa boshqariladigan venv bilan
yuriladi: `~/.workbuddy-ai/binaries/python/envs/default`.

## Fazza yigirma uchinchi — escalation va reengagement vaqt oynalari (ultra-audit)

**Ikki xil modul, bir xil uchta chegara:**

| Chegara | Predikat | Sezgi |
|---|---|---|
| Cooldown (esc 349, re 224) | `last_attempt + cooldown > now` → jim | eksklyuziv |
| Due o'qish (esc 612, re 353) | `... AND next_due <= clock` | inklyuziv |
| Interval surish | `next_due = now + interval` | — |

**Mavjud qo'riqchilar hammasi bir tomonlama:** esc cooldown'ni `sent` holati
bilan "qadagan" — lekin u **boshqa sababdan** terminal, cooldown'ni umuman
o'lchamaydi. Reengagement 700/7200 va 3601/3600 sakraydi. Due o'qishni hech biri
tenglikda ko'rmagan.

**O'lchov (hammasi to'g'ri chiqdi):**
- cooldown: −1s → SILENT, **aynan chegara → RETRY**, +1s → RETRY (ikkalasi)
- due: `next_due == now` → **due**; `== now+1` → due emas (ikkalasi)
- interval: `next_due − last_run` = **aynan** interval (3600 / 600)

**Nuqson topilmadi** — oltita nuqta ham to'g'ri, lekin **oltalasi ham**
chegarada qadalmagan.

**Nega muhim:** escalation **muvaffaqiyatsiz** yetkazishni qayta uradi;
reengagement **tirik mijozga** qayta yozadi — bir soniya kimga qachon
murojaat qilinishini o'zgartiradi.

**Sinovlar:** test_escalation 115 → **118**; test_reengagement 32 → **35**.

**Revert (har biri yakka):** esc cooldown `>`→`>=` → 2; esc due `<=`→`<` → 2;
re cooldown → 1; re due → 1; **hech narsa → 153/153 OK**.

**Probe:** 23-bo'lim `section_cooldown_windows`; 285 → **300 xossa**.
Revert'da har biri 2 FAIL; tiklanishda 0.

**Yakuniy to'plam:** `Ran 2417 tests` — `failures=1, errors=11, skipped=1`.
**Hujjat:** §128 (127 → **128 bo'lim**).

### Saboqlar
1. **Bir xil shakl ikki modulda — ikki barobar qadalishi kerak.** Ular mustaqil
   rivojlanadi.
2. **Bir tomonlama qo'riqchi — yarim qo'riqchi.** `sent` cooldown'ni o'lchamaydi.
3. **Intervalni satrdan qayta o'qib o'lchash kerak** — ikki barobarlik va nolni
   faqat shu ochadi.
4. **Oltita nuqta, oltita reverting** — hech biri ikkinchisiga tayanmasin.

---

## Fazza yigirma to'rtinchi — oversight oynalari va supervisor chegaralari (2026-09-20)

### Sirt

`api-python/platform_runtime/oversight.py` (298 satr) va
`api-python/platform_runtime/supervisor.py` (528 satr). To'qqizta chegara.

| # | Chegara | Joy | Shakl |
|---|---|---|---|
| 1 | activity oynasi boshi | oversight.py:115,119,130 | `created>=since` — inklyuziv |
| 2 | `since_seconds` | oversight.py:91 | `1..MAX_WINDOW_SECONDS` |
| 3 | `cost()` oynasi | oversight.py:164 | `_window` orqali meros |
| 4 | `max_seconds` | supervisor.py:299 | `60..86400` |
| 5 | `max_steps` | supervisor.py:283 | `1..12` |
| 6 | `max_hops` | supervisor.py:281 | `1..3` |
| 7 | `history` kesilishi | supervisor.py:420 | `total > len(rows)` |
| 8 | `_match` tenglik | supervisor.py:254 | `(len(kw), -index)` |
| 9 | nomzod tartibi | supervisor.py:208 | `ORDER BY id` |

### Natija

**To'qqiztadan sakkiztasi to'g'ri, lekin chegarada o'lchanmagan.** Faqat #7
(`history` kesilishi) haqiqatan qadalgan edi.

### Ikki nuqson

**A. Hujjat kodga zid (haqiqiy xato, tuzatildi).**
`_routable` docstring: *"in declaration order"*; `_match` docstring:
*"then declaration order"*. Kod esa `ORDER BY id` — **alifbo tartibi**.
O'lchandi: `zulu` birinchi, `alpha` ikkinchi e'lon qilindi; teng uzunlikdagi
kalit so'zda g'olib **`alpha`** — birinchi e'lon qilingan yutqazdi. Ikkala
docstring ham haqiqiy qoidaga tuzatildi.

**B. O'z sinovim ko'r edi (o'z-o'zini audit).**
`max_seconds` chegarasini `(59,60,86400,86401)` bilan o'lchadim; M5 reverting
(`86400 → 86401`) **yashil** qoldi, chunki 86401 shunchaki qonuniy bo'lib qoldi.
Sinovni bir qadam tashqariga surdim (`86402` + manba literali tasdiqlanadi) →
M5 qizil.

### Revert matritsasi (7 rejim, hammasi qizil)

| Buzilish | Test | Probe |
|---|---|---|
| oversight task+run `>=`→`>` | 2 fail | 2 |
| oversight audit `>=`→`>` | 1 fail | — |
| oversight floor `1`→`2` | 4 err | — |
| tie-break `-index`→`+index` | 1 fail | 2 |
| `86400`→`86401` | 1 fail | 2 |
| `MAX_STEPS_CEILING` 12→13 | 1 fail | 1 |
| `MAX_HOPS_CEILING` 3→4 | 1 fail | — |
| **hech narsa** | **0** (98/98) | **0** (334/334) |

### Raqamlar

- `test_oversight.py` **34 → 38**, `test_supervisor.py` **56 → 60**
- To'liq to'plam **2420 → 2428**, imzo o'zgarmadi: `failures=1, errors=11, skipped=1`
- Probe **300 → 334** xossa, 334/334 yashil
- Audit hujjati **§128 → §129**
- BACKLOG: `offline_tests` 2420 → 2428, `open_risks` 26 → 27

### Qo'shildi

- `test_oversight.py`: `test_the_window_start_is_inclusive_so_a_row_on_it_is_counted`,
  `test_all_three_activity_reads_share_the_inclusive_predicate`,
  `test_cost_reports_the_same_window_activity_does`,
  `test_a_longer_window_admits_rows_a_shorter_one_excludes`
- `test_supervisor.py`: `test_the_hop_ceiling_itself_is_an_accepted_cap`,
  `test_the_step_ceiling_itself_reaches_the_run`,
  `test_the_seconds_window_edges_are_the_documented_pair`,
  `test_equal_length_keywords_resolve_by_section_id_not_declaration_order`
- `test_supervisor.py` ga `fresh_supervisor()` harness yordamchisi (har urinish
  uchun yangi Engine, hop cap aralashmasin) + `shutil` import
- `scripts/probe_truncation_verdict.py` `section_oversight_and_supervisor_bounds()`
- `docs/development/ULTRA-AUDIT-ASCII-CELL-UZ.md` §129
- `docs/development/BACKLOG.json` yangilandi
- `README.md` 2420 → 2428

### Saboqlar (yangi)

1. **Hujjat ham o'lchanishi kerak.** `ORDER BY id` va "declaration order"`
   ikkalasi "birinchi" deydi, lekin boshqa birinchisini. Operator hujjatga
   qarab qaror qabul qiladi — hujjat **shartnoma**.
2. **`99` shiftni isbotlamaydi.** Chegaraning o'zi va undan bir qadam narisi
   sinalishi kerak.
3. **Chegaradan bir qadam narisi — shiftning o'zidan bir qadam narisi emas.**
   M5 xatosi shu edi.
4. **Bitta literal, uchta o'qish.** Uchtasi birgalikda chegarada sinalishi kerak.
5. **Meros olingan chegara ko'rinmas.** `cost()` ni faqat `activity` bilan
   sinash qo'riqlamaydi.
6. **Yozgan sinovni sindirib ko'r.** Yashil sinov — o'lchangan sinov emas.

### Muhit eslatmasi

- Python faylini Python bilan qayta yozish **CRLF/LF ni o'zgartiradi**. `oversight.py`
  LF, `supervisor.py` CRLF. Qaytarishda `bak` dan **bayt sifatida** (`read_bytes`/
  `write_bytes`) yozish kerak, `read_text`/`write_text` emas.
- `scripts/probe_truncation_verdict.py` ni ishga tushirish:
  `"$HOME/.workbuddy-ai/binaries/python/envs/default/Scripts/python.exe" scripts/probe_truncation_verdict.py`
- Revert matritsasi skriptida `encoding='utf-8', errors='replace'` va
  `PYTHONIOENCODING=utf-8` kerak — aks holda Windows xato matni UnicodeDecodeError
  beradi.

---

## Fazza 25 — ERP posting chegaralari (`erp.py`)

**Sirt:** o'n bitta chegara. **Natija:** to'qqiztasi to'g'ri, lekin **o'lchanmagan** —
har birini mutatsiya qilish 56 sinovli modulni yashil qoldirdi. Faqat
`ledger_page` kesilishi (III faza) va `_date_text` ning bir tomoni qadalgan edi.

**Nuqson (bitta):** `_date_text('13.13.2026')` ni "ambiguous: both parts could be a
month" deb rad etardi va "bir qismi 12 dan katta bo'lgan shakl yuboring" derdi —
**o'sha kiritmada allaqachon ikki qism 12 dan katta**. Uchinchi shox ajratildi
("names no month"), docstring "at least one" → "**exactly one**".

**O'lchov:** 54 da'vo; hammasi to'g'ri. **Revert:** 12 rejim, hammasi qizil.

**Yozilgan:** `test_erp.py` 56 → 68; probe §25 (334 → 374); audit §130.

## Fazza 26 — ifodalab bo'lmaydigan sonlar

**Shakl bo'yicha supurish** beshta joyni topdi: `float(value)` hujayrada, natija
mavjudmi deb so'ramasdan.

1. `inventory._number`, `oee._number`, `manufacturing._number`, `workforce._hours`
   — `float(10**400)` **`OverflowError` ko'taradi**, cheksizlik qaytarmaydi.
   To'rttasi ham "non-finite → o'qilmaydigan" deb hujjatlashtirgan.
2. **`business_graph.canonical`** — satr yo'li **jimgina `inf`** qaytaradi:
   `'1'+'0'*400` va `'2'+'0'*400` ikkalasi `('n', inf)` → **teng**. Ziddiyatni
   topadigan funksiya haqiqiy ziddiyatni **o'chirardi**.
3. `inventory.MAX_QUANTITY = 10**15` — hech kim o'qimagan, va u "hech narsani
   chegaralamaydigan konstanta" haqidagi izohning **tagida** turardi.
4. **Sinov topilmasi:** uchta "non-finite" sinovi **satr** `'inf'` beradi —
   `NUMBER_RE` uni guard'ga yetmasdan rad etadi. Guard olib tashlansa uchtasi ham
   yashil qoladi: ular **regexni** o'lchagan.

**Yozilgan:** +6 sinov; probe §26 (374 → **409**); audit §131; BACKLOG
(`offline_tests` 2428 → **2446**, `open_risks` 27 → **29**); README.

**To'liq to'plam:** `Ran 2446` — `failures=1, errors=11, skipped=1` (imzo
o'zgarmadi).

### Uchta o'zim qilgan xato (muhim)

1. **O'ldirilgan revert harness manbani mutatsiyalangan qoldirdi.** Foreground
   timeout `erp.py` ni M11 holatida tashladi; keyingi yurgizish **o'sha faylni
   baseline** deb o'qidi va "bayt-aynan qaytardi" deb **noto'g'ri** xabar berdi.
   Endi: pristine baseline fayldan o'qiladi + har mutatsiyadan oldin hash
   tekshiriladi + SIGTERM/atexit qaytaradi.
2. **`assertIn` qatorga bog'lanmagan edi.** `'MAX_AMOUNT_MINOR = 10 ** 15'` —
   `'... = 10 ** 15 + 1'` ning **qismi**; M3 yashil qoldi. Endi aniq qator
   a'zoligi. (§129 M5 sabog'ining ikkinchi uchrashuvi.)
3. **`if __name__` bloki ostiga qo'shilgan sinov yig'ilmaydi.** Fayl
   `if __name__ == '__main__': unittest.main()` bilan tugasa, oxiriga qo'shilgan
   metod o'sha blokning **ichiga** tushadi: parse bo'ladi, discovery `OK` deydi,
   sinov **hech qachon yig'ilmaydi**. Bir marta sodir bo'ldi.

## Fazza 27 — managed database kontrakti (`database/`)

15 fayl, 1 897 satr, o'nta backend, bitta kontrakt. **Ikki nuqson** `contract.py` da:

1. **Bir fakt, ikki o'quvchi.** `MAX_INTEGER = 2**53-1` — kontrakt "portativ"
   degan eng katta son, `scalar` uni qabul qiladi. Lekin `normalize` ning
   `expected_version` tekshiruvi **qat'iy `<`** edi -> shiftning o'zini rad etardi,
   `scalar` va **to'rt backend** (`<=`) esa qabul qilardi. O'lchandi:
   `9007199254740991` da kontrakt rad, backendlar qabul. Xabar ham yolg'on edi:
   "positive" — qiymat musbat, u shunchaki shiftda.
2. **Bir qiymat, ikki javob.** `json.loads` `udXXX` escape'idan yolg'iz surrogat
   yasaydi; uchta darvoza UTF-8 hajmi bilan o'lchaydi, o'lchovning o'zi
   `UnicodeEncodeError` ko'tarardi — `ValueError`/`Forbidden` kontraktli
   **validatsiya yo'lida**. `text` esa o'sha qiymatni **qabul** qilardi.
   `_utf8_size` yordamchisi bilan tuzatildi.

**O'zim qilgan xato:** birinchi sinovim `assertRaises(ValueError)` ishlatdi —
`UnicodeEncodeError` **`ValueError` ning avlodi**, shuning uchun sinov aynan
istisno qilmoqchi bo'lgan halokat bilan qanoatlandi. Revert matritsasi ochdi
(P2/P4/P5 yashil). `assertNotIsInstance(..., UnicodeError)` + xabar tekshiruvi
qo'shildi; shundan keyin 5/5 **QIZIL**.

**Yozilgan:** `test_managed_database.py` 48 -> 52; probe §27 (409 -> **438**);
audit §132; BACKLOG (`offline_tests` 2446 -> **2450**, `open_risks` 29 -> **30**);
README; LOCAL-VERIFICATION.

**To'liq to'plam:** `Ran 2450` — `failures=1, errors=11, skipped=1` (imzo o'zgarmadi).

### Yana bir muhit sabog'i (hujjatlar, kod emas)

`Path.write_text` Windows'da matn rejimida ochadi va yangi satrni `os.linesep` ga
aylantiradi. Natijada: (a) allaqachon CRLF bo'lgan satr **CR CR LF** bo'lib yozildi —
`LOCAL-VERIFICATION-UZ.md` va memory fayli; (b) **LF** bo'lgan
`ULTRA-AUDIT-ASCII-CELL-UZ.md` **CRLF** ga aylandi. Uchtasi ham bayt darajasida
qaytarildi. **Hujjatlar uchun ham `read_bytes`/`write_bytes` ishlatilsin.**

## Fazza 28 — agent loop chegaralari (`agent_loop.py`)

17 chegara; revert matritsasi **12 tasini yashil** qoldirdi.

1. **Budjetlar bir tomondan qadalgan.** Mavjud sinov `0` va `13` ni **rad** sifatida
   yuradi, shuning uchun **toraytirilgan** shift ko'rinmaydi: `MAX_STEPS = 6` butun
   to'plamdan o'tdi va o'n ikki qadamli runni jimgina rad etardi.
2. **Nuqson — bir fakt, uch joyda.** Faol holat to'plami: `ACTIVE` konstantasi +
   `create()` SQL nusxasi + `tick()` SQL nusxasi. `create()` nusxasidan
   `waiting_task` ni olib tashlash **30 sinovni yashil qoldirdi**; `tick()` da
   ushlandi. Ya'ni **navbat shiftini qo'riqlaydigan bayonot qarovsiz** edi va
   shift kutayotgan runlarni sanashni to'xtatardi. Tuzatish: qayta yozishni olib
   tashlash — ikkala SQL ham `_ACTIVE_PLACEHOLDERS` ni `ACTIVE` dan quradi.
3. Yana o'nta chegara to'g'ri, lekin qadalgan.

**Yozilgan:** `test_agent_loop.py` 30 -> 39; probe §28 (438 -> **475**); audit
§133; BACKLOG (`offline_tests` 2450 -> **2459**, `open_risks` 30 -> **31**);
README; LOCAL-VERIFICATION. **To'liq to'plam:** `Ran 2459` — imzo o'zgarmadi.

### O'zim qilgan to'rtta xato

1. `self.create('q-1', key='q-1')` — `create` ning birinchi pozitsion argumenti
   **`key`** ning o'zi; uch sinov `TypeError` bilan yiqildi.
2. `source.count('ACTIVE') == 3` — `_ACTIVE_PLACEHOLDERS` ichida `ACTIVE` bor,
   son **9** chiqdi. Aniq o'lchov kerak.
3. **`if __name__` bloki ostiga qo'shilgan sinov yig'ilmaydi** — **uchinchi**
   marta. 9 sinov hech qachon yig'ilmadi. Endi har qanday qo'shish skripti
   **avval** `__main__` qo'riqchisini qidiradi.
4. `tick` eng eski nomzodni tanlaydi — kutayotgan runni yolg'iz qoldirib tick
   qilish kerak edi.
5. **R16 mutatsiyasi birinchi urinishda noto'g'ri sababdan qizil edi** (SQL arity
   xatosi). Arity to'g'rilangach haqiqiy xossa o'lchandi.

## Fazza 29 — route sirti va UI qamrovi

93 route, 12 UI tab, 5 komponent. **O'lchov**, taassurot emas — va uchta o'lchov
quroli ham noto'g'ri chiqdi (`startswith` hammaga "ha" dedi; backtick shablon
qavssiz sinfda kesildi; qo'shtirnoq juftlash apostrofdan siljidi va `identity`
ham mount, ham route bo'lgani uchun o'chib ketdi). Shuning uchun har bir oila
**nomma-nom** grep bilan tekshirildi.

1. **Nuqson — UI o'zi taklif qilmagan chorani buyuradi.** `page.tsx:113`
   `uncertain` natija uchun "reconcile API orqali" der; route bor
   (`POST /steps/{step}/reconcile`), UI da **tugma yo'q**. `uncertain` — avtomatik
   qayta urinish **ataylab** bloklangan yagona holat, ya'ni operatorning yagona
   yo'li hujjatlashtirilgan, lekin **qurilmagan**. §129/§130 bilan **bir xil shakl**.
2. **20 ta tenant route — backend to'liq, UI yo'q:** reengagement (4), briefing (3),
   escalation (4), supervisor (4), schedules, mijoz sub-resurslari (3), knowledge
   delete.
3. **7 ta identity route — UI yo'q:** bootstrap, logout-all, sessions, workspace
   select, invitations, invitation accept, member revoke (oxirgi uchtasi —
   QOLGAN-ISHLAR belgilagan foydalanuvchi boshqaruvi).
4. **MANIFEST o'lchandi:** FAIL, 445 checked, 38 hash, 165 unlisted, repo ichida
   generator yo'q. `erp.py`/`inventory.py` kabilar manifestda **umuman yo'q edi**.
   Tegilmadi — reliz asbobsozligi.

**UI yozilmadi va sinov qo'shilmadi — ataylab.** Bu muhitda React typecheck/build
NOT_RUN (`bun` yo'q), ya'ni UI **tekshirilmagan** bo'lardi; route↔UI sinovi esa
satr solishtirishga tayanadi va uchta urinishning uchtasi noto'g'ri javob berdi.
O'rniga **probe** yozildi (475 -> **507**), u rad etilishi mumkin.

**Yozilgan:** probe §29; audit §134; BACKLOG (`open_risks` 31 -> **32**);
LOCAL-VERIFICATION. `offline_tests` **2459** (o'zgarmadi — sinov qo'shilmadi).

## Fazza 30 — xarajat budjeti va tool sxema chegaralari

`usage_budget.py` + `tools.py`, 17 chegara, 19 rejimli revert matritsasi.
Birinchi yurgizishda **to'rttasi yashil** qoldi (S5, S12, S13, S14) — men yozgan
sinovlar bu literal chegaralarni qamramagan edi; to'rtta sinov qo'shildi, keyin
**19/19 qizil**.

1. **Ikki matn darvozasi kelishmagan.** `usage_budget.bounded` va
   `database.contract.text` bir faktni qo'riqlaydi; `bounded` **DEL (0x7F)** va
   **bosh bo'shliq**ni **qabul** qilardi, `text` ikkalasini rad etardi. Bir xil
   qiymat qaysi eshikdan kirganiga qarab haqiqiy yoki noloyiq edi. Tuzatildi.
2. **Bitta literal, ikki birlik.** `tools.validate_schema` argument obyektini
   **belgi** bilan chegaralardi; runtime'dagi qolgan hamma serializatsiya chegarasi
   **bayt** o'lchaydi (`parse_request` 12000, `erp._observations` 12000/48000,
   managed backendlar 1024/16000/16000). O'lchandi: 20 000 belgidan kam, 20 000
   baytdan ko'p kirillcha obyekt **o'tardi** — taxminan **ikki barobar**. Tuzatildi.
3. **Qoldirilgan:** `string(maxLength)` belgi, `parse_request` bayt. 12000
   belgidan kam, 20000 baytdan kam, lekin 12000 baytdan ko'p tana → sxema qabul,
   kontrakt rad; ASCII egizagi ikkalasidan o'tadi. Ikkala son ham yakka holda
   to'g'ri — bu **siyosat qarori**, shuning uchun o'lchandi, tuzatilmadi.

**Yozilgan:** `test_usage_budget.py` 36 -> 46, `test_adapters.py` 16 -> 18;
probe §30 (507 -> **572**); audit §135; BACKLOG (`offline_tests` 2459 -> **2471**,
`open_risks` 32 -> **33**); README; LOCAL-VERIFICATION.
**To'liq to'plam:** `Ran 2471` — imzo o'zgarmadi.

### O'zim qilgan to'rtta xato

1. **Sinov fayl oxiriga qo'shildi va boshqa sinfga tushdi.** `test_usage_budget.py`
   da ikki sinf bor; EOF — ikkinchisining ichi.
2. **Ko'chirishda blokni ikki marta yozdim**, u `class` dan oldin tushib
   `IndentationError` berdi.
3. **Probe'da manifest sonini qotirdim** (`38`) — har faza uni o'zgartiradi. Endi
   shakl tekshiriladi, son emas.
4. **`pending('a', 100) == []`** — budjetda rezervlar bor, ro'yxat bo'sh emas.

## Fazza 31 — WhatsApp oynasi, imzo va shiftlar

15 chegara, 15 rejimli revert matritsasi. Birinchi yurgizishda **7 tasi yashil**.

1. **Bir fakt, ikki e'lon, assimetrik siljish.** `WINDOW_SECONDS` ikki modulda.
   Qayta yozish **ataylab** (izohda yozilgan: modul darajasidagi import chekkasi
   yo'q) — shuning uchun **olib tashlanmadi**. Lekin narxi to'lanmagan edi:
   saqlangan `window_until` **inbound** konstantasi bilan yoziladi; outbound
   o'qiydigan yo'l o'z nusxasini ayiradi, `window_state` qo'shadi — **juftlik
   qisqaradi**, ya'ni hodisalar yo'li **inbound** ga ergashadi, registr yo'li esa
   **outbound** ga. O'lchandi: outbound = 1 soat → hodisalar yo'li **24 soatda
   qoladi**, registr yo'li **1 soat**. Hech narsa yiqilmadi. Endi tenglik
   **majburiy** (sinov bilan), ikki o'qish yo'li alohida qadalgan.
2. **Yetti qadalgan chegara:** `MAX_BODY_BYTES` 1_000_000, `MESSAGE_ID_RE` 128,
   `PHONE_RE` 15, `MAX_ENTRIES` 50, `MAX_TEXT_CHARS` 4096, `MAX_EVENTS_SCANNED`
   500, `ERROR_OUTSIDE_WINDOW` 131047.

**Yozilgan:** `test_whatsapp.py` 120 -> 127, `test_whatsapp_inbound.py` 70 -> 74;
probe §31 (572 -> **627**); audit §136; BACKLOG (`offline_tests` 2471 -> **2482**,
`open_risks` 33 -> **34**); README; LOCAL-VERIFICATION.
**To'liq to'plam:** `Ran 2482` — imzo o'zgarmadi.

### O'zim qilgan to'rtta xato

1. **Dizayn qaroriga zid tuzatish.** `whatsapp_inbound` dan e'lonni olib tashlab,
   `whatsapp` dan import qildim — modul darajasidagi import chekkasi **ataylab
   yo'q**, va bu **fayl izohida yozilgan**. Fayl izohini o'qimasdan tuzatish —
   auditning o'zi qoralaydigan xatti-harakat. **Qaytarildi**.
2. **Harness yana CRLF ga urildi.** `whatsapp.py` — CRLF, naqshlarim LF edi;
   W4 "PATTERN-MISSING" berdi va **yashil ko'rindi**.
3. **W1 ni noto'g'ri modulda yurgizdim** (tenglik sinovi `test_whatsapp.py` da).
4. **W7 ni simvoldan yasalgan sinov bilan "qadaldi" deb o'yladim** — shift
   kengaysa sinov **birga suriladi**. **Uchinchi marta** shu tuzoq.

## Fazza 32 — telephony chegaralari va uchinchi o'lik konstanta

19 chegara, 19 rejimli revert matritsasi; birinchi yurgizishda **15 tasi yashil**.

1. **Uchinchi o'lik konstanta topilmasi.** Fazza I `inventory.MAX_NAMES` va
   `oee.MAX_NAMES` ni, Fazza 26 `inventory.MAX_QUANTITY` ni olib tashlagan edi.
   Endi yana uchtasi:
   - `telephony.MAX_QUEUE = 200` — izoh bloki uni *"how many callable rows one
     read may return at all"* deb hujjatlashtiradi, lekin o'qishni `MAX_ROWS`,
     elementlarni `MAX_QUEUE_ITEMS` chegaralaydi. **Izoh nomlagan shift mavjud
     emas edi.**
   - `telephony.MAX_WINDOW_DAYS = 31` — hech kim o'qimaydi.
   - `vision.MAX_WINDOW_DAYS = 31` — o'sha konstanta ikkinchi modulda, u yerda ham
     o'qilmaydi.
2. **Qadalgan chegaralar:** `MAX_PER_WINDOW`, `MIN/MAX_WINDOW_SECONDS`,
   `MIN_RETENTION_DAYS`, `MAX_DURATION`, `MAX_QUEUE_ITEMS`, `MAX_EVENTS`,
   `MAX_ROWS`, `MAX_REGISTERS`, `MAX_PURPOSE_CHARS`, `NAME_RE`, va lokator
   qoidasining **o'n uchta kengaytmasi**. `.wav` ni olib tashlash butun to'plamni
   yashil qoldirdi — ya'ni **bittasi ham** qadalgan emas edi.
3. **Yetishmayotgan chegara (tuzatilmadi):** `since`..`until` **masofasi** hech
   qayerda tekshirilmaydi; `2026-13-45` o'tadi va o'n yillik oyna ham. O'lik
   `MAX_WINDOW_DAYS` aynan shu shift uchun mo'ljallangan edi. Konstantalar olib
   tashlandi (yo'qligi ko'rinsin), lekin **shift o'zi yozilmadi** — sonni tanlash
   operatorning qarori. BACKLOG'ga ochiq risk sifatida yozildi.

**Yozilgan:** `test_telephony.py` 139 -> 147, `test_vision.py` 49 -> 50;
probe §32 (627 -> **697**); audit §137; BACKLOG (`offline_tests` 2482 -> **2491**,
`open_risks` 34 -> **36**); README; LOCAL-VERIFICATION.
**To'liq to'plam:** `Ran 2491` — imzo o'zgarmadi.

### O'zim qilgan ikki xato

1. **`normalise` `None` qaytaradi deb o'yladim** — u **bo'sh satr** qaytaradi, va bu
   docstring'da yozilgan. O'lchov apparatim xato edi.
2. **`maximum=MAX_PURPOSE_CHARS`** deb qidirdim; haqiqiy qator
   `len(item) > MAX_PURPOSE_CHARS`. Manbani **o'qimasdan** naqsh yozdim.

## Fazza 33 — connector qatlami

`connectors.py` + `connector_contract.py` + `connector_authority.py`, 22 rejimli
revert matritsasi; birinchi yurgizishda **14 tasi yashil**. Vakolat darvozasi
(`connector_authority`) yaxshi qadalgan; qolgan ikki modul deyarli o'lchanmagan.

1. **Nuqson — uchta darvoza, ikki qoida.** `authorize_read_connection` ulanish
   nomini **faqat uzunlik** bilan tekshirardi; `connector_contract._string` va
   `database.contract.text` esa bo'shliqni ham, boshqaruv belgilarini (DEL) ham rad
   etadi. O'lchandi: `' erp'`, `'erp '`, `'e<DEL>rp'`, `'e<C0>rp'` — uchtadan
   ikkitasi rad etadi, bittasi o'tkazadi. Bu **§135 dagi ajralishning ikkinchi
   uchrashi**; bir xil tuzatildi.
2. **Qadalgan chegaralar:** identifikator 63, nom 128, allowlist 40, so'rov
   ustunlari 40, `limit` 100, filtr 1000, qator 16 000 / jami 80 000, SQLite
   limitlari, progress 1000 chaqiruv / 2 s, `_string` default 128, `_strings`
   default 100, read-only write-cap qoidasi, va **schema ↔ runtime mosligi**.
3. **Erishib bo'lmaydigan chegara (nuqson emas):** so'rov tomonidagi `<= 40`
   hech qachon ishlamaydi — `columns` allowlist ning qismi, allowlist esa 40 bilan
   chegaralangan. Literal bilan qadaldi, sababi izohda.

**Yozilgan:** `test_connector_authority.py` 37 -> 47, `test_connector_contract.py`
4 -> 8; probe §33 (697 -> **780**); audit §138; BACKLOG (`offline_tests` 2491 ->
**2505**, `open_risks` 36 -> **37**); README; LOCAL-VERIFICATION.
**To'liq to'plam:** `Ran 2505` — imzo o'zgarmadi.

### O'zim qilgan beshta xato

1. **Bayt framing'ini noto'g'ri hisobladim:** `encode({'blob': ''})` — **11** belgi,
   men 10 dedim. Endi framing **o'lchanadi**.
2. **Allowlist shiftini jadvalni o'zgartirmasdan sinadim** → `DatabaseError`.
3. **`'c\\x7frm'`** — yozuvchi orqali o'tganda bu **matn**, DEL emas; `chr(127)` kerak.
4. **`'name'` allowlist da bor** — filtrni "ruxsat etilmagan" deb sinadim.
5. **Sinov fayliga ikki marta qo'shdim** → **dublikat sinf**. Skript idempotent
   bo'lishi kerak.

## Fazza 34 — knowledge chegaralari va o'lishni rad etgan qo'riqchi

`knowledge.py`, 20 rejimli revert matritsasi; birinchi yurgizishda **15 tasi yashil**.

1. **Nuqson — qo'riqchining o'zi o'ladi.** `vector()` elementni
   `... or not math.isfinite(n) or abs(n) > 1e6` bilan tekshirardi.
   `math.isfinite(10**400)` **OverflowError ko'taradi**, shuning uchun float
   oralig'idan tashqari butun son `abs(n) > 1e6` ga **yetib bormaydi** — holbuki
   aynan o'sha tekshiruv uni to'g'ri rad etardi. O'lchandi: `10**400` ->
   OverflowError, `10**308` -> ValueError (chegara ushlaydi), `10**6` -> o'tadi.
   **§131 shaklining ikkinchi uchrashi**, va o'tkirligi shunda: u yerda qo'riqchi
   **yo'q** edi, bu yerda **qo'riqchining o'zi** yiqiladi.
   Tuzatish: **tartibni almashtirish** — `abs()` aylantirmasdan ishlaydi.
2. **Tuzatish qadalgan emas edi.** Uni qaytarish **hech qanday sinovni
   yiqitmasdi**; endi tartibni qadaydigan sinov bor.
3. **Yana 15 chegara** to'g'ri, lekin o'lchanmagan.

**To'rtinchi marta** bog'lanmagan `assertIn`: `'... < 2**31'` — `'... < 2**31 + 1'`
ning **prefiksi**, shuning uchun mutatsiya bilan qanoatlanadi. **Har doim aniq qator.**

**Yozilgan:** `test_knowledge.py` 34 -> 45; probe §34 (780 -> **857**); audit §139;
BACKLOG (`offline_tests` 2505 -> **2516**, `open_risks` 37 -> **38**); README;
LOCAL-VERIFICATION. **To'liq to'plam:** `Ran 2516` — imzo o'zgarmadi.

### O'zim qilgan beshta xato

1. `create_collection(dimension=1024)` ni **modelsiz** chaqirdim.
2. `'https://e.uz/'` — **13** belgi, men 12 dedim; **ikki marta** xato hisobladim.
3. `expected_version=0` ni bir hujjat uchun **qayta ishlatdim**.
4. Probe'da chunk shiftini **bo'sh bo'lmagan** kolleksiyada sinadim.
5. Yuqoridagi bog'lanmagan `assertIn`.

## Fazza 35 — vision chegaralari va `truncated` yolg'oni

`vision.py`, 19 rejimli revert matritsasi; birinchi yurgizishda **9 tasi yashil**.

1. **Nuqson — `truncated` hech kim ushlab qolmagan qatorlarni sanagan.** `_scan`
   bayroqni `matched` dan chiqarardi, u esa oynadan o'tgan **har bir** qatorni
   sanaydi — **intern tugun** stansiyali qatorlarni ham, ular aktivga bog'lanmaydi
   va hodisa bo'lmaydi. O'lchandi (3 bog'lanadigan, 7 intern, limit 5):
   `returned=3 unbound=7 count=10 truncated=True`. Ro'yxatda **hamma** bog'lanadigan
   hodisa bor, limit yetilmagan, lekin bayroq operatorni **bir xil ro'yxat**
   qaytaradigan so'rovni qayta yurishga yuborardi. **Bu — probe'ning o'zi uchun asos
   bo'lgan xato.** `summary` to'g'ri shaklda edi (`len(ordered) > limit`).
   Tuzatish: `matched - unbound > len(events)`.
2. **V9: tuzatishni qaytarish hech narsani yiqitmasdi** — nuqson qayta kiritilishi
   mumkin edi. Endi `truncated` **nimani sanashini** qadaydigan sinov bor.
3. **Yana 8 chegara** to'g'ri, lekin o'lchanmagan (sinf shiftlari, `NAME_RE`,
   nomsiz **20** registr capi, ustun capi, oyna chetlari, summary bayrog'i).

**Yozilgan:** `test_vision.py` 50 -> 58; probe §35 (857 -> **914**); audit §140;
BACKLOG (`offline_tests` 2516 -> **2524**, `open_risks` 38 -> **39**); README;
LOCAL-VERIFICATION. **To'liq to'plam:** `Ran 2524` — imzo o'zgarmadi.

### O'zim qilgan olti xato

1. `sensitivity='person'` ni `person_classes` **bilan** rad etiladi deb o'yladim —
   aslida u to'g'ri.
2. `confidence_column=''` ni xato deb o'yladim — u **ixtiyoriy**.
3. `parse_path` **nomni emas, chuqurlikni** tekshiradi — "bog'lanmaydigan" fixture
   uchun **intern tugun** kerak.
4. `sheets_registers` blokini noto'g'ri yozdim.
5. Probe'da uchta qatorni **bir stansiyaga** qo'ydim — bir stansiya kesilmaydi.
6. V8 naqshi ikki marta uchradi; kontekst bilan bog'lash kerak edi.

## Fazza 36 — aktiv ierarxiyasi va hech narsa ushlanmagan `truncated`

`assets.py`, 20 rejimli revert matritsasi; birinchi yurgizishda **8 tasi yashil**.

1. **Nuqson — `tree` ushlanmagan sahifani e'lon qilardi.** U o'zi yasagan **har bir**
   tugunni qaytarardi, lekin bayroqni `len(nodes) > MAX_CHILDREN` dan chiqarardi.
   O'lchandi (`MAX_CHILDREN`=2, 6 tugun): `nodes=6 count=6 truncated=True`. Hech
   narsa kesilmagan, va `tree` **`limit` qabul qilmaydi** (u `depth` bilan
   chegaralanadi, uni chaqiruvchi tanlaydi) — ya'ni bayroq **ham yolg'on, ham
   harakatsiz**. `MAX_CHILDREN` — **children** tool'ining shifti.
   `children` va `descendants` to'g'ri edi. Tuzatish: `'truncated': False`.
2. **Bu shaklning uchinchi uchrashi:** II (`len >= limit`), 35 (hech qachon hodisa
   bo'lmaydigan qatorlar), 36 (hech qachon bo'lmagan kesish). Uchtasi ham bir xil
   savolga noto'g'ri javob beradi: *"ko'proq bormi, qayta so'raymi?"*
3. **A11: tuzatishni qaytarish hech narsani yiqitmasdi** — uchinchi marta shu holat
   (§139 K7, §140 V9).
4. **Yana 7 chegara** to'g'ri, lekin o'lchanmagan.

**Yozilgan:** `test_assets.py` 59 -> 65; probe §36 (914 -> **977**); audit §141;
BACKLOG (`offline_tests` 2524 -> **2530**, `open_risks` 39 -> **40**); README;
LOCAL-VERIFICATION. **To'liq to'plam:** `Ran 2530` — imzo o'zgarmadi.

### O'zim qilgan uch xato

1. Buzuq konstruksiya (`with x := (lambda: None):`) — skript sintaksis xatosi.
2. Path-length chegarasini `prefix_path` orqali sinadim — 512 belgili yo'l har doim
   4 tadan ko'p segmentga ega, shuning uchun **chuqurlik** qoidasi birinchi ishlaydi.
   `_segments` kerak edi.
3. Probe'da `'len(nodes) > MAX_CHILDREN' not in src` — **olib tashlashni qayd etuvchi
   izoh** o'sha iborani keltiradi, shuning uchun tekshiruv izoh bilan qanoatlanadi
   (Fazza 26 dagi `MAX_QUANTITY` tuzog'i).

## Fazza 37 — sheets registri va javob tashlab yuborgan qator

`sheets.py`, 22 rejimli revert matritsasi; birinchi yurgizishda **8 tasi yashil**.

1. **Nuqson A — bir limit, ikki xil javob.** `_read` ikkala tool'ga xizmat qiladi
   (`sheets.read` — xom matritsa, `sheets.rows` — kalitlangan). `shape_rows` sarlavha
   qatorini hisobga oladi (`values[:limit + 1]`), kalitlanmagan kesim esa yo'q
   (`values[:limit]`) — holbuki `header_row` **sukut bo'yicha `True`**. O'lchandi
   (`limit=3`): 2 qator -> 2/`False` (to'g'ri), **3 qator -> 2/`False` (YOLG'ON)**,
   4 va 5 -> 2/`True`. Ya'ni kesim `limit - 1` **ma'lumot** qatori qaytarardi,
   jadvalda aynan `limit` ta bo'lganda **oxirgisi tashlab yuborilardi** va bayroq
   buni **inkor etardi**. `returned` ham bir joyda matritsa, boshqasida ma'lumot
   qatorini sanardi.
2. **Nuqson B — e'lon qilingan `max_rows` har bir oddiy o'qishni yiqitardi.**
   `registers` 1000 gacha qabul qiladi, o'qish shifti `MAX_ROWS`; e'lon qilingan
   qiymat to'g'ridan-to'g'ri `bounded_int(..., 1, MAX_ROWS)` ga uzatilardi, shuning
   uchun `max_rows: 500` e'lon qilgan registrda **`limit` umuman bermagan** har bir
   o'qish `ValueError` berardi. Tuzatish: **kesish**, ko'tarish emas.
3. **Bu shaklning to'rtinchi uchrashi:** II, 35, 36 — va endi 37. Birinchi uchtasi
   kesishni **o'ylab topardi**; 37 **haqiqiy yo'qotishni inkor etdi** — xavfliroq yo'nalish.
4. **S19 halol yashil:** `shape_rows` ichki kesimi **erishib bo'lmaydigan**
   (`capped` allaqachon `matrix` ni chegaralaydi). Aniq qator bilan qadaldi, sababi
   manbada ham, sinovda ham yozildi.
5. **Sakkiztasi birinchi yurgizishda yashil** edi — oltitasi sinovlar konstantani
   **moduldan qayta o'qigani** uchun (mutatsiya ikkala tomonni surardi).
6. **`AUDIT-CORRECTIONS-UZ.md` dagi ikki da'vo falsifikatsiya qilindi:** "sheets
   `truncated` ni to'g'ri hisoblaydi" va "`max_rows` 200 dan oshsa rad etiladi".

**Yozilgan:** `test_sheets.py` 29 -> 66 (yangi `SheetsBoundaryTests`); probe §37
(977 -> **1043**); audit §142; `AUDIT-CORRECTIONS-UZ.md` tuzatildi; BACKLOG
(`offline_tests` 2530 -> **2567**, `open_risks` 40 -> **41**); README;
LOCAL-VERIFICATION. **To'liq to'plam:** `Ran 2567` — imzo o'zgarmadi.

### O'zim qilgan uch xato

1. **`returned` ni ma'lumot qatori deb o'yladim** — kalitlanmagan yo'lda u
   **matritsa** qatorlarini sanaydi. Aynan shu noto'g'ri taxmin **nuqsonni ochdi**.
2. **Birinchi matritsada S4/S5 ni "yashil" deb o'qidim** — aslida ular
   `PATTERN-MISSING` edi, ya'ni **o'lchanmagan**. `REGISTER_RE` va `RANGE_NAME_RE`
   bir xil naqsh, shuning uchun literal ikki marta uchraydi; e'lon qatoriga bog'landi.
3. **S19 ni tuzatishga urindim**, holbuki u nuqson emas — **himoyaviy ortiqchalik**.
   Buni **o'lchov** bilan aniqladim.

## Fazza 38 — hujjat qatlami va uchta qo'riqchi noto'g'ri narsani o'lchagan

`documents.py`, 26 rejimli revert matritsasi; birinchi yurgizishda **18 tasi yashil**.

1. **Nuqson A — qo'riqchi boshqa satrni chegaralagan.** `bank_account` `[0-9 ]{4,34}`
   bilan tekshirilardi, **keyin** bo'shliqlar olib tashlanardi, xabar esa "4 to 34
   digits" derdi. O'lchandi: `'    '` mos kelib `''` bo'lib saqlandi; `'1   '` mos
   kelib **bir raqamli** hisob saqlandi. Bo'shatilgan qiymat eng xavflisi:
   `fraud_signals` **MATERIAL** `bank_account_changed` signalini `if
   doc['bank_account']:` bilan darvozalaydi — ya'ni bo'shliq maydon nazoratni
   **yiqitmadi, olib tashladi**. U signal yetkazib beruvchi hisobini almashtirishni
   (klassik AP firibgarligi) ushlash uchun bor.
2. **Nuqson B — shakl tekshiruvi sana tekshiruvi o'rniga o'tgan.** `2026-13-45`,
   `2026-02-30`, `2026-00-00` qabul qilinardi, saqlanardi, keyin `date()` ularga
   **`fraud_signals` ichida** xato berardi — u yerda istisno tutiladi, shuning uchun
   hujjat hafta kuni signalisiz qolardi va **sabab aytilmasdi**.
3. **Nuqson C — pul float orqali o'tgan, uni taqiqlagan yagona modulda.**
   `_median` = `int(statistics.median(values))`; juft sonda float qaytadi, float esa
   2⁵³ gacha aniq. `MAX_AMOUNT` USD uchun 10¹⁷ ga yo'l qo'yadi. O'lchandi:
   `_median([10**16, 10**16+2])` → 10¹⁶ (aniq 10¹⁶+1). Va bu son dalilga **"the
   median"** deb chop etiladi — modulning o'z docstring'i "mislabelled median is a
   lie" deydi, **aynan ikki paragraf yuqorida**.
4. **Bir assimetriya qayd etildi, tuzatilmadi:** pul **satri** 15 butun raqamga yo'l
   qo'yadi (10¹⁵ ni chiqarib tashlaydi), **oraliq** va **int** yo'li esa qabul qiladi.
5. **`'statistics' not in source`** tuzog'i — yangi docstring `statistics.median` ni
   nomlaydi. **Beshinchi** uchrash (26 `MAX_QUANTITY`, 36 `tree`).

**Yozilgan:** `test_documents.py` 100 -> 117 (`BoundaryTests`); probe §38 (1043 ->
**1147**); audit §143; BACKLOG (`offline_tests` 2567 -> **2584**, `open_risks` 41 ->
**42**); README; LOCAL-VERIFICATION. **To'liq to'plam:** `Ran 2584` — imzo o'zgarmadi.

### O'zim qilgan to'rt xato

1. **Int yo'lini minor birlik deb o'yladim** — `_amount_minor(10**17, 'USD')` ni
   "qabul" deb kutdim; aslida int — **major** birlik va 100 ga ko'paytiriladi.
   Kutilmam noto'g'ri edi, kod emas.
2. **`supplier_history` ni rad etadi deb o'yladim** — u **kesadi** (to'g'ri shakl).
3. **Shakl chegarasini 16 ta to'qqiz bilan sinadim** — uni **oraliq** tekshiruvi
   ushlaydi, ya'ni sinov noto'g'ri chegara haqida edi (D6 yashil qoldi). Ajratuvchi
   kirish — aynan `10**15`.
4. **`'statistics' not in source`** — yuqoridagi 5-band.

## Fazza 39 — CRM chegarasi va ochiq turgan yagona darvoza

`crm_contract.py` + `crm_gateway.py`, 30 rejimli revert matritsasi; birinchi yurgizishda
**25 tasi yashil**.

1. **Nuqson A (xavfsizlik) — CRM uchun yagona darvoza siyosatni yumshoq o'qigan.**
   `_check_agent_allowed` = `if allowed_conns and connection not in allowed_conns`, ya'ni
   **bo'sh** `allowed_connections` ("hech narsa" hamma joyda) bu yerda **"hammasi"**
   bo'lardi. O'lchandi: guard jimgina qaytdi va `crm.lead.search` **adapterga yetib
   bordi**. Bu aynan shu yerda kritik, chunki CRM ulanishi **faqat shu joyda**
   tekshiriladi — engine'ning allowlist tekshiruvi `{connectors.read, database.read,
   database.plan_write, database.write}` ni qamraydi va **birorta `crm.*`** yo'q. Bu
   §137 (`sheets`) shaklining **ikkinchi** uchrashi va runtime'dagi **oxirgi** yumshoq
   o'quvchi.
2. **Nuqson B — yalang'och `int()` validatsiya emas.** `duration_seconds=None` ->
   `TypeError`; `1.9` -> 1. `bounded_price(1.9)` -> **1** (kasr narx jimgina
   kichraytirilardi); `float('inf')` -> **`OverflowError`**. Xabar esa "integer amount"
   der edi. Yangi `bounded_int` — bool/float/satrni rad etadi, ikki uchni chegaralaydi,
   faqat `ValueError`.
3. **Nuqson C — yalang'och, chegaralanmagan `float()` NaN va inf'ni qabul qilgan.**
   `'nan'`/`'inf'` — haqiqiy float, xato bermaydi, quyida ularni rad etadigan hech narsa
   yo'q. O'lchandi: `scheduled_at='nan'` qabul qilinib **`nan` bo'lib qoldi**. Shift —
   validator'ning **o'z** standarti (`10**12`), o'ylab topilgan son emas.
4. **Bir nomzod o'lchandi va rad etildi:** `limit` yalang'och `int()` bilan o'giriladi,
   lekin **ikki marta yutiladi** (sxema 1..50 + har adapter `min(max(1, limit), 50)`).
5. **Ikki sirt qayd etildi, o'zgartirilmadi:** `safe_relative_path` `?` va
   percent-encoding'ni (`/a%2e%2e%2fb`) qabul qiladi.
6. **C1 (tuzatishni qaytarish) yashil edi** — bo'sh allowlist holati umuman sinovsiz
   edi, ya'ni xavfsizlik tuzatishi **qayta kiritilishi mumkin** edi.

**Yozilgan:** `test_crm_contract.py` 13 -> 33, `test_crm_gateway.py` 9 -> 23; probe §39
(1147 -> **1298**); audit §144; BACKLOG (`offline_tests` 2584 -> **2618**,
`open_risks` 42 -> **43**); README; LOCAL-VERIFICATION. **To'liq to'plam:** `Ran 2618` —
imzo o'zgarmadi.

### O'zim qilgan to'rt xato

1. `bounded_int(None)` ni rad etadi deb o'yladim — u **default** qaytaradi (modulning
   o'z an'anasi: `bounded_price(None) == 0`).
2. `CUSTOM_HTTP_OPERATIONS` ga placeholder ro'yxatini ko'chirdim.
3. `dig(..., 'a.a.a.a')` ni skalyar deb o'yladim — u to'rtinchi darajadagi **obyekt**.
4. `dig({'a-b': 1}, 'a-b')` ni `None` deb o'yladim — chiziqcha **ruxsat etilgan**.

## Fazza 40 — to'rt CRM adapteri va to'rt marta yozilgan chegara

`bitrix24` / `kommo` / `onec` / `custom_http`, 27 rejimli revert matritsasi; birinchi
yurgizishda **21 tasi yashil**.

1. **Nuqson A — ishonchsiz provider maydoni butun o'qishni yiqitardi.** `onec` va
   `custom_http` ning `_shape_lead`/`find_contacts` i **ko'taradigan**
   `normalize_phone`/`normalize_email` ni chaqirardi. O'lchandi: `phone: 'n/a'` ->
   `ValueError: Empty phone number`; `email: 'not-an-email'` -> `ValueError: Invalid
   email address`. Ya'ni **bitta buzuq lead yonidagi hamma yaxshi leadni yo'q qilardi**
   — holbuki `_rows` filtrlaydi, `_map` default qaytaradi (toqat **dizayn qarori**).
   Yangi `optional_phone`/`optional_email` qatorni saqlaydi.
2. **Nuqson B — provider narxi `int(float(...))` bilan o'girilardi.** `'abc'` ->
   ValueError, `'nan'` -> ValueError, `'inf'` -> **OverflowError**, `'450000.99'` ->
   **450000** (float orqali jimgina qisqartirilgan). Ya'ni **pul float orqali o'tgan**.
   `provider_price` chegaralaydi, float ishlatmaydi, yaroqsizga 0 qaytaradi.
3. **Nuqson C — `timeout_seconds` ikkitasi uchun bor, ikkitasi uchun yo'q.** O'lchandi:
   `onec`/`custom_http` transportga `timeout=45` uzatadi; `bitrix24`/`kommo` **umuman
   uzatmaydi** (default 15), kalitni o'qimaydi ham, tekshirmaydi ham. Bitta kalit —
   ikki xil ma'no.
4. **Nuqson D — hajm rad etilishi "transport xatosi" deb xabar qilinardi.** Rad etish
   `try` ichida ko'tariladi va keng `except Exception` uni yutadi; `onec`/`custom_http`
   `except <O'zXato>: raise` qiladi, `bitrix24`/`kommo` qilmagan edi.
5. **Bir assimetriya qayd etildi:** `provider_price` ning **satr** shakli 10¹⁵ ni
   chiqarib tashlaydi, **butun son** shoxobchasi qabul qiladi — §143 dagi ayni assimetriya.

**Yozilgan:** yangi `test_crm_adapter_boundaries.py` (13 test); probe §40 (1298 ->
**1431**); audit §145; BACKLOG (`offline_tests` 2618 -> **2631**, `open_risks` 43 ->
**44**); README; LOCAL-VERIFICATION. **To'liq to'plam:** `Ran 2631` — imzo o'zgarmadi.

### O'zim qilgan to'rt xato

1. **Response-map pointer'i hal qilinmadi** (`'phone': 'Phone'`, qatorda `'phone'`) —
   buzuq qiymat umuman ko'rinmadi va birinchi o'lchov **"nuqson yo'q"** dedi. Noto'g'ri
   salbiy faqat to'g'ri pointer bilan qayta o'lchaganda chiqdi.
2. **E12/E13 ni sintaksis xatosi bilan "qizil" qildim** — xossani o'lchamagan mutatsiya
   natija emas; qayta kesildi.
3. **`provider_price(str(10**15))` ni 10¹⁵ deb kutdim** — satr shakli 16 raqamni rad
   etadi.
4. **`python -c` one-liner'da teskari belgi** shell tomonidan bajarildi va izohni buzdi.

### Matritsa yangilandi

`replace_all` rejimi qo'shildi: bir xil literal **har bir `find_*` metodida** takrorlansa
(kommo 2 marta, onec/custom_http 3 marta), mutatsiya **hammasini** almashtiradi va
**nechta** almashtirganini chop etadi.

## Fazza 41 — qayta aloqa sikli va va'da qilingan talabni tekshirmagan qo'riqchi

`reengagement.py`, 27 rejimli revert matritsasi; birinchi yurgizishda **18 tasi yashil**.

1. **Nuqson — qo'riqchi "is required" derdi, bo'shliq esa o'tardi.** `_identifier` **xom**
   qiymatni bo'shligiga qarardi, **kesilganini** qaytarardi. O'lchandi: `_identifier(' ',
   'agent')` -> **`''`**; `'   '` va `'\t'` ham. `configure(agent=' ')` **agenti bo'sh
   satr bo'lgan jadvalni saqladi**, va har bir sikl tool darvozasida yiqilib siklni
   **tushunarsiz `feed_denied:Forbidden`** bilan o'chirardi — xato *"rad etish"* emas,
   *"o'zini o'chirgan integratsiya"* bo'lib chiqdi. Uzunlik chegarasi **ataylab xom**
   qiymatda: avval kesish bo'shliq bilan to'ldirib chegaradan o'tishga yo'l berardi.
2. **Ikki jadval bir qadamda turishi kerak.** `RUN_TO_LEDGER` ↔ `agent_loop.TERMINAL`
   bugun **teng** (tekshirildi — yo'q kalit tirik nuqson ko'rinardi va **u emas**). Agar
   loop terminal status qo'shib, xarita qo'shmasa, `queued` qator **abadiy** qolardi —
   va `queued` `_claim` **yagona** takrorlanadigan deb hisoblaydigan status.
   `LIMITS` ↔ API modeli ham yettala chegarani ikkinchi marta yozadi; **hammasi mos**.
3. **Ledger to'rt holat e'lon qiladi, sakkiztasini yozadi** — ataylab; qayd etildi.
4. **Ikki halol yashil:** `_identifier` dagi `not value` ni quyidagi bo'shliq tekshiruvi,
   `sync_ledger` dagi `AND status=?` ni yuqoridagi SELECT qamraydi. Aniq qator bilan qadaldi.
5. **Probe va matritsani BIR VAQTDA yurgizdim** — matritsa mutatsiya qilgan faylni probe
   o'qib **12 yolg'on yiqilish** ko'rsatdi. O'lchov va mutatsiya **navbatma-navbat** bo'lishi
   shart.

**Yozilgan:** `test_reengagement.py` 35 -> 84; probe §41 (1431 -> **1555**); audit §146;
BACKLOG (`offline_tests` 2631 -> **2680**, `open_risks` 44 -> **45**); README;
LOCAL-VERIFICATION. **To'liq to'plam:** `Ran 2680` — imzo o'zgarmadi.

### O'zim qilgan besh xato

1. **Probe va matritsani parallel yurgizdim** — yolg'on nuqsonlar (yuqorida).
2. **`tick` jadvalni ham suradi** — har safar 301s qo'shib "har siklda" deb o'yladim;
   aslida `next_due` `interval_seconds` ga suriladi, ya'ni **har ikkinchi** tick ishlaydi.
   `max_attempts=10` da 7 ta urinish oldim.
3. **Cooldown chegarasini `tick` orqali o'lchadim** — jadval chegarani yashirdi; `_claim`
   ni to'g'ridan-to'g'ri chaqirish kerak edi.
4. **`ledger_all()[0]`** tartibsiz qatorni oldi va bazani subtestlar orasida tozalamaganim
   uchun keyingi subtest **oldingisining qatorini** o'lchadi.
5. **`policy()` `NotFound` ko'taradi**, `None` qaytarmaydi.

## Fazza 42 — supervisor va oversight: o'n birga surilgan shift va uchta o'lik konstanta

`supervisor.py` + `oversight.py`, 30 rejimli revert matritsasi; birinchi yurgizishda
**22 tasi yashil**.

1. **Nuqson A — shift hosil qilinadigan qiymatda o'lchanmagan.** `route` `request_key` ni
   256 bilan chegaralardi, lekin run kaliti `f'supervisor:{request_key}'` — va
   `AgentLoop.create` **kalitni** 256 bilan chegaralaydi. O'lchandi: 245 -> ok (kalit 256),
   **246 -> `Invalid run identity`** (kalit 257). Amaldagi shift **245**, xabar esa 256
   derdi. Endi `RUN_KEY_PREFIX` + `MAX_REQUEST_KEY = 256 - len(RUN_KEY_PREFIX)` — bitta
   ifoda.
2. **Nuqson B — uchta o'lik konstanta, uch xil muomala.** `MAX_SECTIONS` (haqiqiy chegara,
   ulanmagan — 21 bo'lim saqlanardi) -> **ulandi** (yangi bo'lim rad etiladi, mavjudini
   qayta e'lon qilish ruxsat); `MAX_RUNS` (mavjud bo'lmagan narsani chegaralaydi, `activity`
   `GROUP BY` qiladi) -> **olib tashlandi**; `VIEWS` (o'quvchisiz e'lon qilingan to'plam)
   -> **qo'riqchi qilib ulandi**.
3. **Nuqson C — qo'riqchi bir shaklni tekshirib boshqasini qaytargan.** `_text` kesilganini
   tekshirib xomini qaytarardi; `_known(' sales ')` -> `Invalid agent id`, holbuki
   `supervisor._name(' sales ')` -> `'sales'`. Endi `_text` o'zi tekshirganini qaytaradi.
4. **Uch tuzatishning uchtasi ham dastlab yashil edi** (G1, G4, G26) — ya'ni qayta
   kiritilishi mumkin edi.

**Yozilgan:** `test_supervisor.py` 60 -> 131, `test_oversight.py` 38 -> 85; probe §42
(1555 -> **1683**); audit §147; BACKLOG (`offline_tests` 2680 -> **2800**, `open_risks`
45 -> **46**); README; LOCAL-VERIFICATION. **To'liq to'plam:** `Ran 2800` — imzo o'zgarmadi.

### O'zim qilgan besh xato

1. **`_depth` ni hisobga olmadim** — u **nishon agent bo'yicha** sanaydi, shuning uchun
   oldingi yozuvlar keyingi `route` ni ikkinchi hop qilib qo'ydi va **yetti yozuv**
   "Forbidden" bo'lib chiqdi (nuqson deb o'qishim mumkin edi).
2. **`setUp` `seed()` ni chaqirmasligini unutdim** — 20 emas, 17 bo'lim.
3. **`history(TENANT, 0)` bir qator qaytaradi deb o'yladim** — bo'sh jadvalda nol.
4. **`type(_dispatch(...)).__name__ == 'ok'`** deb yozdim — u **dict** qaytaradi.
5. **`_text`/`_known` ni import qilishni unutdim** — oversight sinovlari `NameError`.
