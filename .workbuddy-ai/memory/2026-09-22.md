# 2026-09-22 — §155: `app/` qatlami va o'qilmaydigan to'plam

## Nima qilindi

**§155 — `app/` qatlami chegaralari.** Oxirgi audit qilinmagan qatlam.
31 mutatsiya, 7 modul, **31 RED, 0 GREEN, 0 o'lchanmagan**.

- `auth.py` — `TTL_SECONDS`, `MIN_TOKEN_TTL_SECONDS`, `SESSION_TOKEN_TTL_SECONDS`
  (sessiya umri ternary ichida yalang'och `900` edi).
- `identity_store.py` — 19 chegara, jumladan **scrypt parametrlari**
  (`SCRYPT_N/R/P/DKLEN`, `SALT_BYTES`) — bular sozlama emas, **xarajat**:
  `SCRYPT_N` 16384 → 1024 = parolni buzish 16× arzon, va hech narsa o'qimasdi.
- `limits.py` — `DEFAULT_DAILY_LIMIT`, `MIN_DAILY_LIMIT`, `COUNTER_TTL_SECONDS`,
  `WARN_FRACTION` (0.8 ikki joyda yozilgan edi).
- Yangi: `runtime_tests/test_app_layer_bounds.py` (**48 sinov**),
  `scripts/audit_app_layer_bounds.py`.

## To'rt asosiy topilma

1. **O'qilmaydigan to'plamdagi qadam — qadam emas.** `MAX_PERSONA_CHARS` va
   `MAX_QUEUE` `tests/` da "qadalgan" edi, lekin `.github/workflows/verify.yml`
   faqat `runtime_tests` (15-qator) va `integration_tests` (33-qator) ni
   yurgizadi. `api-python/tests/` — **hech qayerda yo'q**.

2. **§154 ning o'z yozuvi ikki marta xato edi.** Bloklangan yuza **12 emas, 13**;
   `O_NOFOLLOW` **7 emas, 6**; `test_root_symlink_replacement_denied` ro'yxatda
   umuman yo'q edi; `test_escape_symlink_denied` `O_NOFOLLOW` deb yozilgan, lekin
   o'z `setUp` ida `os.symlink` da o'ladi. Sababda **nomi yo'q** edi:
   `symlink_privilege` (`OSError`, `WinError 1314`), **2 ta** sinov.

   **Saboq (umumiy):** modul shartning **rost** bo'lishini tekshirardi,
   **aynan sabab** ekanini emas. Predikat rost bo'lsa noto'g'ri qator ham
   o'tib ketadi. Yopildi: `test_each_recorded_reason_is_the_actual_cause` har bir
   bloklangan sinovni **yurgizib**, istisnoni qatordagi sababga solishtiradi.
   Mutatsiya bilan tekshirildi → **3 qizil**.

3. **`verify_offline.py` tugata olmaydigan gate edi, va men unga socket soldim.**
   Gate `UnicodeDecodeError` (0x97 — kirill `WinError 1314` matni) → o'quvchi
   thread o'ladi → `stdout` `None` → `TypeError`. Tuzatish: `errors='replace'`
   + `or ''`. Keyin gate **yurgizildi** va **mening** uchta
   `TelegramBodyCeilingTests` im `RuntimeError: Offline verification: network
   disabled` bilan qizil bo'ldi: `runtime_tests` da `TestClient` hech qayerda
   ishlatilmagan, chunki to'plam audit hook ostida **socket'siz** yurishga
   mo'ljallangan. To'plam socket'siz **bo'lgani uchun** emas — **hech kimga socket
   kerak bo'lmagani uchun** socket'siz edi.
   `asyncio.run` ham ishlamadi: **Windows'da event loop'ning o'zi socket**
   (`socket.socketpair()`). Yechim: korutina qo'lda `send(None)` bilan yuritiladi.
   Yangi qo'riqchi: `OfflineSuiteTests` — `runtime_tests` da `TestClient`/`httpx`
   importini taqiqlaydi (`urllib.request` ataylab ochiq: u faqat
   `OpenerDirector.open` ni patch qilish uchun import qilinadi).

4. **`MANIFEST.sha256` — gate o'zi aytgan joyda qizil edi.** Modul docstring'i
   *"Run on a freshly extracted release ZIP"* deydi, va toza `git archive`
   eksportida **403/498 hash mismatch** (hammasi matn fayllari). Sabab
   `core.autocrlf`: manifest CRLF tutgan Windows daraxtida yasalgan, bloblar esa
   LF. CI buni **birinchi** qadamda yurgizadi (`verify.yml:14`). Tuzatish:
   `digest()` endi CRLF → LF normallashtirib xeshlaydi (bir baytli `carry` bilan,
   chunk chegarasi uchun). Endi **ikkala joyda ham PASS**; 4 yangi sinov,
   mutatsiya bilan **3 qizil**.

## Instrument va muhit

- **`revert_matrix.drop_sidecar()`**: matritsa `os.remove(sidecar)` da yiqilib,
  birinchi moduldan keyin to'xtardi (31 dan 3 tasi o'lchandi) va **chiqish kodi 0**
  edi. Sabab: host ommaviy o'chirishni to'sadi. Endi `OSError` yutiladi.
- **`tzdata`**: `requirements.txt:9` da e'lon qilingan, lokal venv'da yo'q edi →
  o'lchov CI muhitiga mos kelmasdi. O'rnatildi. `tests/`: 35 → **33 qizil**,
  170 → 172 yashil.
- **`scripts/append_doc_section.py`**: marker bilan **bir marta** qo'shadi
  (ikki marta bajarilish muammosi uchun mexanik qo'riqchi).

## O'lchangan holat

| | |
|---|---|
| `runtime_tests` | **3 000 sinov**, `failures=1, errors=12, skipped=1` |
| Bloklangan yuza (Python) | **13** (12 error + 1 failure) |
| Bloklangan yuza (Node) | **11 / 24** — `apps/runner/test.js`, **qadalmagan** |
| `api-python/tests/` | **33 qizil, 172 pass, 4 skip** — **gate yurgizmaydi** |
| `integration_tests` | 95/95 pass |
| `MANIFEST.sha256` | **PASS**, 502 fayl — va **toza LF eksportda ham PASS** |
| `tests/` | 33 qizil, 172 pass, 4 skip — **gate yurgizmaydi** |

`tests/` ning 33 qizilligi: 10 × `410 Gone` (ataylab bekor qilingan marshrut),
15 × eskirgan javob shakli, 4 × legacy runner WebSocket, 3 × kontrakt surilishi,
1 × auth statusi. **Qaror talab qilinadi**: tuzatish yoki nafaqaga chiqarish.

## Keyingi qadam

`tests/` taqdirini hal qilish; `approvals.py` (`PHONE_MASK` `{9,16}` — PII
niqoblash yuzasi, `limit=100`, `[:200]`, `[:12]`) va `ladder.py`
(`min_tasks=30` + `max(30, ...)` da **ikkinchi** 30 — ikki literal mos kelishi
shart, hech narsa majburlamaydi) → §156.
