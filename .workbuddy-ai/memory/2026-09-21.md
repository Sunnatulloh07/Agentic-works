# 2026-09-21 — optimallashtirish sessiyasi (hujjat, test, skill)

Foydalanuvchi topshirig'i: avval oldingi sessiyalar ishini chuqur tahlil qilish,
keyin **ikki eslatmani** bajarish (to'plam vaqti; skill hajmi), hujjatlar va
testlarni birlashtirish, keyin ochiq qismlarga o'tish.

## 1. O'lchangan holat (taxmin emas)

| Ko'rsatkich | Qiymat |
|---|---|
| Registry tool'lari | **88** (`known_tool_names()` 89) |
| Runtime modullari | 42 fayl, 16 226 satr |
| `app/` modullari | 40 fayl, 4 483 satr |
| Offline to'plam | **2800 test** (`def test_` 2310 — qolgani meros) |
| To'plam vaqti | **~315 s (5.3 daq)**, foydalanuvchi aytgan 14 daq emas |
| docs/development | 33 fayl / 12 670 satr → **16 fayl** |

## 2. To'plam vaqti — ildiz sabab o'lchandi

`cProfile` (`test_supervisor.py`, 132 test, 38 s):

- `Engine.__init__` — **14.5 s (38%)**, shundan `executescript` **10.1 s**,
  `close` 8.6 s.
- Bitta faylda **144 ta `Engine()`** qurilishi → 1728 `executescript` chaqiruvi.
- Ya'ni asosiy xarajat — **har bir qurilishda 11 modul sxemasini qaytadan
  bajarish**, modul mantig'i emas.

**KDF emas.** `identity_store` `scrypt(n=16384)` ishlatadi = **204 ms/chaqiruv**,
lekin butun to'plamda faqat **91 chaqiruv = 18.6 s = 6%**. Bundan muhimi:
arzon-KDF patch'i **95 testni yiqitadi** (errors 149 → 244), ya'ni u **xavfsiz
optimallashtirish emas** — hash formati va parametrlari haqidagi da'volarni
buzadi. Yo'q qilindi.

## 3. Topilgan haqiqiy nomuvofiqlik (testlarni birlashtirishdan oldin)

17 modulda `register_*` bor. **Ko'pchilik** mavjud tool'ni o'tkazib yuboradi
(idempotent), lekin **5 tasi yo'q**: `business_graph`, `google_adapters`,
`oversight`, `sheets`, `workforce`. Ya'ni qayta ro'yxatga olish
`Invalid tool registration` beradi — sababni aytmaydigan xabar.

Va **`test_oversight.py` aynan shu chetlanishni qadalgan**:
`assertRaises(ValueError)` bilan. Ya'ni istisno **ataylab** ko'rinadi. Bu
skill'ning "bir tushuncha, ikki modul" darsining yangi nusxasi.

## 4. Hujjatlar — bajarildi

- 20 ta `V05*-IMPLEMENTATION-UZ.md` → **3 ta mavzuli fayl**
  (`V05-BLOCKS-CORE/MESSAGING/OPS-UZ.md`), har biri mundarija bilan.
  **Yo'qotish nol**: skript har bir manba satrini natijada qidiradi.
- `§147.9` dagi takroriy darslar (7, 8, 9 = 1, 2, 4) olib tashlandi.
- Yangi asbob: `scripts/docs_tools.py` (merge-blocks, update-refs,
  duplicate-lessons, add-index) — 3 ta bir martalik skript o'rniga bittasi.

## 5. Skill — bajarildi

`boundary-audit/SKILL.md` **394 → 73 satr** (har safar yuklanadigan qism 82%
kichraydi). Darslar mavzu bo'yicha 5 ta `references/` fayliga ajratildi:
`bounds.md`, `guards.md`, `contracts.md`, `tests.md`, `harness.md`.
`SKILL.md` ga "yangi darsni qayerga qo'shish" qoidasi qo'shildi — aks holda
u yana o'sadi.

## 6. ⚠️ MENING XATOM — hujjatdan 242 satr o'chirib yuborildi

`add-index` skripti `ULTRA-AUDIT-ASCII-CELL-UZ.md` ga mundarija qo'shdi, keyin
uni **qaytarish** skripti "keyingi `# ` sarlavhagacha" yurdi va I fazaning
(§1–§10) **242 satrini** ham olib ketdi. Repo **git emas**, ya'ni tiklash yo'q.

**Tiklash:** `.workbuddy-ai/memory/2026-09-20.md` dagi o'lchov yozuvlaridan
qayta tiklandi (5-bo'limdagi jadval, 11 modul ro'yxati, raqamlar). Hujjatga
**ochiq izoh** qo'yildi: bu tiklash, so'zma-so'z emas, ~83 satr qisqa.

**Saboq:** bu hujjatning o'zi §147.6 da "oraliqni tasdiqla" deb yozgan — va men
uni skriptda bajarmadim. `del lines[a:b]` **chegarani oldindan tasdiqlashi**
kerak edi (`assert lines[b-1]` kutilgan matn). Bundan buyon: oraliq o'chiruvchi
har bir skript **ikkala uchni ham assert qiladi**.

## 7. Keyingi qadam

`Engine.__init__` sxema-qayta-bajarishini o'tkazib yuborish (sxema
barmoq-izi bo'yicha) — bu testlar ham, ishlab chiqarish ham tezlashtiradi.
Keyin ochiq qismlar: `oauth.py`, `briefing.py`, `escalation.py`,
`oee.py`/`manufacturing.py` chuqurroq, `crm_reconcile.py`, `app/` qatlami, UI.

---

## 8. YAKUNIY O'LCHANGAN NATIJALAR (sessiya oxiri)

### Muhit haqidagi muhim topilma

Hujjatlashtirilgan imzo (`failures=1, errors=11, skipped=1`) **faqat boshqariladigan
venv bilan** takrorlanadi — unda `cryptography` bor. Yalang'och interpretatorda 138
test `VaultError` beradi va imzo `errors=153` bo'lib o'qiladi. Bu **muhit fakti**,
kod fakti emas. `scripts/measure_suite.py` endi qaysi interpretatorni ishlatganini
**chop etadi**, shunda ikki imzo tasodifan solishtirilmaydi.

To'g'ri buyruq:
```sh
cd api-python
PY="$HOME/.workbuddy-ai/binaries/python/envs/default/Scripts/python.exe"
PYTHONPATH=".;$(python -c 'import site;print(site.getsitepackages()[0])')" \
  "$PY" -m unittest discover -s runtime_tests -t runtime_tests
```

### A/B (bir xil muhit, venv bilan)

| | Testlar | Vaqt | Imzo |
|---|---|---|---|
| Eski dvigatel (har qurilishda sxema) | 2784 | **515.7 s** | `failures=1, errors=11, skipped=1` |
| Yangi dvigatel (barmoq-izi + birlashgan skript + NORMAL) | 2784 | **443.0 s** | **aynan bir xil** |

**−72.7 s (−14%)**, imzo o'zgarmadi. Komponent o'lchovi: sovuq `Engine()` qurilishi
**183 → 44 ms (4×)**, issiq 13 ms.

### Test soni: 2800 → 2784 = −20 + 4

13 ta takroriy test olib tashlandi, lekin ba'zilari **meros orqali ikki marta**
sanalardi (`SupervisorBoundaryTests(SupervisorTests)` kabi 10 ta sinf), shuning uchun
−20; o'rniga **4 ta kuchliroq** shartnoma testi qo'shildi. Aritmetika aniq tekshirildi.

### Yakuniy holat

- `build_registry()` = **88**, `known_tool_names()` = **89** (o'zgarmadi)
- 8 ta config checker → **hammasi OK**
- 17 ta `register_*` → **hammasi idempotent** (avval 5 tasi emas edi)
- Hujjat: `docs/development` 33 → **16 fayl**
- Skill: `SKILL.md` **394 → 73 satr**
- Yangi asboblar: `docs_tools.py`, `measure_suite.py`, `time_test_files.py`,
  `measure_kdf_cost.py`

### Ochiq qolgan (keyingi sessiya uchun)

1. To'plamning qolgan vaqti — **har bir test yangi `Engine()` + yangi SQLite fayl**
   quradi. Keyingi yutuq **umumiy baza fixture**idan keladi (jadval tozalash bilan),
   lekin bu 48 test faylini o'zgartirishni talab qiladi va ehtiyotkorlik bilan
   o'lchanishi kerak.
2. Audit davom etadi: `oauth.py` (**diqqat: `cryptography`siz uning testlari
   yurmaydi**), `briefing.py`, `escalation.py`, `oee.py`/`manufacturing.py` chuqurroq,
   `crm_reconcile.py`, `app/` qatlami (54+14+9+4 route), UI ro'yxati (§134).
3. `scripts/` da 44 ta fayl bor — ularning ba'zilari bir martalik bo'lishi mumkin;
   keyingi tozalash nomzodi.

---

## 9. Yangi sessiya (2026-09-21, 18:30) — qolgan ishlar tahlili, o'lchov bilan

Foydalanuvchi so'radi: PRD'larni chuqur o'rganib, **qilinmagan ishlar ro'yxatini**
chiqarish. Kod o'zgartirilmadi — faqat o'lchandi va tasdiqlandi.

### Tasdiqlangan holat (o'zim o'lchadim)

| Ko'rsatkich | Qiymat |
|---|---|
| To'plam | **2792 test**, `failures=1, errors=11, skipped=1`, **413 s** |
| Imzo | hujjatlashtirilgan baseline bilan **aynan bir xil** |
| Registry | 88 tool |
| Runtime | 42 modul / 16 332 satr |
| `app/` | 40 modul / 4 479 satr |
| `scripts/` | 48 fayl |
| `runtime_tests/` | 68 fayl; `integration_tests/` 8 fayl (95 passed, alohida gate) |
| `docs/development` | 16 fayl |
| Ultra-audit | **43 faza** (§1–§148) |
| `production_release` | **NO_GO** |

### Sessiya yakunlanmagan qismlar (topildi)

1. **§148 (OAuth) fazasi commit qilinmagan.** `git status`: `oauth.py`,
   `test_oauth.py`, `ULTRA-AUDIT`, `audit_oauth_bounds.py`, `revert_matrix.py`
   o'zgargan + `probe_oauth_boundaries.py` yangi. Bitta commit bor (18:03),
   oauth ishi 18:04–18:24 da bo'lgan.
2. **`BACKLOG.json` `offline_tests` = 2784, haqiqatda 2792** (+8 OAuth
   `DeclaredBoundTests`). Yangilanmagan.
3. **`PROGRESS-UZ.md` da §148 haqida birorta yozuv yo'q** — audit hujjatida bor,
   progressda yo'q.
4. `api-python/tmp*` — **4 ta** temp papka qolgan (test yurishlaridan).

### Audit qamrovi — o'lchandi (modul × probe)

Auditdan o'tmagan modullar: `agent_planner.py` (89), `google_oauth.py` (150),
`mcp.py` (58), `model_response.py` (42), `model_transport.py` (61),
`postgres_connector.py` (127), `secret_vault.py` (99), `speech.py` (100),
`crm/crm_reconcile.py`, va **`app/` qatlamining katta qismi** (40 modul).

Nomlangan konstanta yo'q + katta literal bor (skan natijasi, nomzod):
`tools.py` (13 literal), `secret_vault.py` (6), `llm.py` (4), `mcp.py` (4),
`google_reconcile.py` (3), `model_transport.py` (2), `model_response.py` (2).

### PRD v0.5 ↔ kod

P8…P14 **hammasi** `LOCAL_CONTRACT_TESTED` (P14 — `STAGE_C`). Ya'ni PRD yo'l
xaritasi **kodda bajarilgan**; qolgani — **mahsulotlashtirish**. 12 ta ochiq
item: `voice_call_processing`/`billing`/`windows_desktop`/`production_operations`
= TODO; `identity_mfa_recovery`/`knowledge_full_rag`/`voice`/`mac_desktop`/
`ui_full_product` = PARTIAL; `oauth_api_ui`/`crm_reengagement_feed` = SOURCE_ONLY;
`wa_window_until_graph_attribute` = ataylab rad etilgan.

### UI bo'shlig'i (§134, o'lchangan)

**20 tenant route + 7 identity route** — backend to'liq, UI **nol**. Plus
`reconcile` tugmasi yo'q (UI matni uni buyuradi, `page.tsx` da chaqiruv yo'q).

---

## 10. Shu sessiya — 1- va 2-qadam bajarildi (commit qilindi)

### Qadam 1 — §148 OAuth fazasi yopildi (`d0e80cd`)

Faza tugallangan edi, lekin **commit qilinmagan**. Bajarildi:

- `PROGRESS-UZ.md` ga **V05T** bo'limi (§148 yozuvi — auditda bor edi, progressda
  yo'q edi), `ULTRA-AUDIT` §148.8 (o'lchangan baseline), `BACKLOG.json`
  (`offline_tests` 2784 → 2792, claim + oauth verdict), `README.md` (2792).
- **`tmp*` papkalar O'CHIRILMADI** — tekshiruv paytida ma'lum bo'ldi: ular
  `LOCAL-VERIFICATION-UZ.md` 2-bo'limida **tuzatilgan nuqsonning dalili** sifatida
  keltirilgan. O'chirish dalilni o'chirardi. O'rniga hujjatga **"ataylab
  saqlanadi"** izohi qo'shildi, shunda keyingi sessiya ularni yana o'chirmaydi.

### Qadam 2 — `tools.py` auditi (§149, commit `6c33a50`)

Modul tanlandi, chunki skan mexanik: **13 katta literal, 0 nomlangan konstanta**.

- **Birinchi matritsa 15 mutatsiyadan 10 tasini YASHIL qoldirdi** — ya'ni 10
  chegara qadalmagan. Eng qimmati `string()` yordamchisi (har bir tool
  maydonining sukut chegarasi).
- **Nuqson A:** `Registry.add` ikki xil sababni (takroriy nom, noma'lum risk)
  bitta `Invalid tool registration` bilan rad etardi. Repo buni **allaqachon
  shikoyat qilgan** (`register_once` docstring'i) — ya'ni da'vo to'g'ri edi va
  kod hali ham uni bajarayotgan edi. Endi ikki xabar.
- **Nuqson B:** `validate_schema` sxema bo'ylab rekursiya qiladi; **1500 daraja →
  `RecursionError`** (ValueError shartnomasi emas), va `mcp.call` orqali **yetib
  boriladi**. Tuzatish `MAX_SCHEMA_DEPTH = 64` — **o'lchangan** raqam (88 tool'ning
  eng chuqur sxemasi **3**).
- **Ikkinchi shift o'lchandi va RAD ETILDI:** `encode()` 2998 da ishlaydi, 2999 da
  yiqiladi; 12000 belgi ko'pi bilan 1999 daraja tashiydi → **yetib borilmaydi**.
- **17/17 qizil**, probe **66/66 pass**, baseline **2803** test
  (`failures=1, errors=11, skipped=1`) — imzo o'zgarmadi, +11 test.
- **Asbob yaxshilandi:** `revert_matrix.py` endi bir nechta test faylini bitta
  yurishda o'lchaydi; bitta fayl qo'shni fayldagi qadalgan chegarani **yolg'on
  YASHIL** ko'rsatardi.

### Qadam 3 — `secret_vault.py` auditi (§150, commit `1503970`)

Xuddi shu mexanik sabab: **6 katta literal, 0 nomlangan konstanta**. Farqi —
bu yerda **rad etish xabarlarining o'zi ham chegara**.

- **Birinchi matritsa 13 mutatsiyadan 8 tasini YASHIL qoldirdi**, va ular **uch
  guruhga** bo'lindi: **qadalmagan** (5), **qo'riqchi ko'rinmas** (1),
  **yetib borilmaydigan** (2). Guruhni ajratish uchun o'lchash kerak bo'ldi —
  bu fazaning markaziy topilmasi.
- **Nuqson:** `seal` ning keng `except Exception` i `_aad` ning o'z `VaultError`
  ini ham yutardi — chaqiruvchining **o'z argumenti** xatosi
  `Secret encryption failed` bo'lib qaytardi. Tor istisno (`except VaultError:
  raise`) keng istisnodan **oldin** qo'yildi.
- **`open` da bu ataylab TESKARI qoldirildi:** `_aad` **kalit id tekshiruvidan
  keyin** chaqiriladi, shuning uchun kontekst xatosini nomlash "kalit id
  noma'lum" dan ajralib qolardi — **qaysi kalit id lar sozlanganini oshkor
  qiladigan oracle**. To'rttasi ham bir xil xabar beradi, va bu **test bilan
  qadalgan** (aks holda keyingi dasturchi "izchillik uchun" tuzatib, orakulni
  qaytaradi).
- **Ikki chegara yetib borilmaydigan — tuzatilmadi:** eng katta to'g'ri shaklli
  konvert **85 479 belgi** (shift 90 000), va kodlangan maydon shiftiga hech
  qayerdan yetib bo'lmaydi (muhit o'zgaruvchisi shifti shu hostda **32 747**,
  Linux'da 131 072). Ikkalasi ham **mexanizm** bilan qadaldi (`json.loads` /
  `base64.b64decode` **chaqirilmasligi** assert qilinadi).
- **Uch chegarani xulq-atvor qaday olmaydi:** `NONCE_BYTES`, `ENVELOPE_VERSION`,
  `AAD_FORMAT` ham yozuvchi, ham o'quvchi tomonidan o'qiladi — kengaytirilsa
  ikkala tomon birga suriladi va round-trip o'tadi. Yagona pin — **literal**.
  `AAD_FORMAT` productionda o'zgarsa, saqlangan har bir credential ochilmaydigan
  bo'ladi.
- **17/17 qizil**, probe **99/99 pass**, baseline **2814** test
  (`failures=1, errors=11, skipped=1`) — imzo o'zgarmadi, +11 test.
- **Asbob yaxshilandi:** `revert_matrix.py` da `verify()` va `--check` rejimi —
  matritsa ~2 daqiqa, naqsh tekshiruvi millisekund. (Naqsh xatosi **ikki marta**
  to'langan edi.)

**Skill yangilandi:** `boundary-audit` — `SKILL.md` ga **yashilning uch sababi**
jadvali; `tests.md` ga "rad etishni o'lchaydi, sababini emas" va "natija
ko'rinmasa mexanizmni qadash"; `bounds.md` ga platforma va chet-el ishlab
chiqaruvchi chegaralari; `contracts.md` ga "ataylab asimmetriya ham qadalishi
kerak"; `harness.md` ga `--check`.

### Keyingi (o'lchangan nomzodlar)

`app/` qatlami (40 modul / 4 479 satr), `agent_planner.py`, `google_oauth.py`,
`postgres_connector.py`, `speech.py`, `mcp.py`, `model_transport.py`,
`model_response.py`, `crm/crm_reconcile.py`.
`production_release` **NO_GO** bo'lib qoladi.

## §153–§154 — boshqaruv tekisligi chegaralari va Windows baseline

- **§153**: `app/platform_api.py` dagi **88 inline `Field` chegarasi** nomlangan
  konstantaga aylantirildi (48 shakl, 81 chaqiruv joyi). Invariant mutlaq: modulda
  bitta ham `Field(...)` ichida raqamli literal qolmadi (`NoInlineBoundTests`).
  Qaytarish matritsasi: **61/61 qizil**, 0 yashil, 0 o'lchanmagan.
  Blast radius **o'lchandi**: `MAX_REENGAGEMENT_PER_CYCLE` 20→200,
  `MAX_ESCALATION_PER_CYCLE` 50→500, `MAX_COOLDOWN_SECONDS` 2.6M→26M —
  `test_reengagement`/`test_escalation`/`test_briefing` uchun **YASHIL**, ya'ni
  avtonomiya shiftlari runtime kontrakt modullariga ko'rinmas edi.
- Strukturaviy test **uchta haqiqiy xatoni** ushladi: umumiy naqsh almashtirish
  bir xil qiymatli, lekin boshqa ma'noli maydonlarni birlashtirgan
  (`MAX_DISPLAY_NAME_CHARS`, `MAX_EXTERNAL_ID_CHARS`, `MAX_QUERY_CHARS`).
- **§154**: Windows uchun bloklangan yuza **11 emas, 12** (11 error + 1 failure).
  Yagona sabab `os.O_NOFOLLOW` — **7 tasini** bloklaydi. `test_platform_baseline.py`
  ro'yxatni **sabab bo'yicha** qadaydi (Windows'da sabab amal qilishi, POSIX'da
  amal qilmasligi shart).
- **Topilgan nuqson**: `integration_tests` **umuman yig'ilmasdi** —
  `test_connector_authority_http.py` `app.platform_api` ni `ENV` o'rnatilishidan
  oldin import qilardi. `integration_tests/conftest.py` qo'shildi → **95/95 pass**.
- **Baseline**: **2949 test**, `failures=1, errors=11, skipped=1` — imzo §150 bilan
  aynan. Devor vaqti 576.8 s → 425.9 s.
- **MANIFEST**: 498 fayl, **PASS**. UI build `next@14.2.35` da **PASS**
  (sandbox safe-delete shim `.next/export` tozalashida yolg'on ishlamaydi —
  build o'zi o'tadi).
- Commit: `6213f31`.

**Muhit saboqi**: bu sessiyada ba'zi Bash buyruqlari **ikki marta** bajarildi
(PROGRESS-UZ.md ikki marta qo'shildi). Qo'shish (append) operatsiyalariga har doim
idempotentlik qo'riqchisi qo'yish kerak.
