__pycache__/
*.py[cod]
.pytest_cache/
.env
outbox/
leads/
traces/
approvals/
ladder.json
data/
*.db*
node_modules/
.next/
*.tsbuildinfo
dist/
*.matrix-baseline
*.log
owner-token.local.txt
config/integrations.json
apps/runner/allow.json
apps/runner/journal/
*.local.txt
.workbuddy-ai/
# Pristine copies written by mutation probes before they edit a source file.  A probe
# that is killed leaves one behind and heals from it on the next run; it must never be
# committed, because it is a copy of a file that may currently be mutated.
scripts/probes/.mutation_backups/
