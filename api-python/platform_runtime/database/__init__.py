"""Versioned, capability-based customer database access.

SQLite is locally executable. Network adapters require optional dependencies and
live acceptance. Catalog entries without transports are explicitly non-executable.
"""
